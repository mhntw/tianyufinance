# 本地专用 / 不部署到云端
node_modules/
.cloudfunctions/
cloudfunctions/
.DS_Store
*.log

# 仅在本地运行，不参与静态托管
serve.py
启动.command
启动.bat
发布新版.command

# 数据红线：账套/备份/导入等数据文件绝不进入静态托管（数据在本地 sj/ 与云端数据库）
data/
books/
backups/
imports/

# Tauri 打包产物与依赖（前端 dist 为构建时生成，target 为 Rust 编译产物）
tauri/dist/
tauri/node_modules/
tauri/src-tauri/target/

# ===== 推送 GitHub 前的数据红线（一律不进入远程仓库）=====
# 真实账套数据目录（含 cwrj/books 下的账套文件）
cwrj/
# 历史备份目录（早期版本快照，含数据痕迹，无外推价值）
backups_*/
# 重构期临时备份（bak_kd_to_ty_* / bak_kingdee_* 等），不进远程仓库
bak_*/
# 工具导出产物（tools/_out 含真实账套 JSON 导出 = 客户财务数据）
tools/_out/
# 内部审计 / 上线文档（含真实账套指标与账套名）
docs/
# 金蝶界面参考截图（第三方版权界面，非源码，仅本地作比对规格用）
金蝶截图/
# 运行时探针与助手会话数据
_ais_probe.json
.codebuddy/
.workbuddy/

# 临时探针脚本（AIS 导入探测、一次性验证）
tools/_*.js
tools/scan_ais_templates.js

# 临时调试文件
full-page.html

# 变更记录含真实账套数据，不进仓库（现位于本仓库根目录 CHANGELOG.md，仅本地保存）
# 【务必保留本规则】该文件含真实账套指标，一旦提交即会公开到 GitHub ——
#   切勿用 git add -f CHANGELOG.md 绕过忽略；也勿在 git clean 时加 -x（-x 会连忽略文件一起删）。
CHANGELOG.md

# 编辑器 / 工具自动生成的备份与临时副本（避免误提交进仓库，如 style.css.bak1）
*.bak
*.bak1
*.bak2
*.tmp
*~
*.swp
*.swo
