# 本地版(rj)与云端版(rj-cloud)数据共享配置

> 目的：本地版做了哪些操作，云端版能直接读到，无需网络/接口同步。

## 原理
本地版(rj)已改为**纯前端存储引擎**(`js/storage.js`)，优先将账套写成真实文件：
- `data/books/<id>.json` —— 账套主文件
- `data/backups/<ts>.json` —— 自动备份(环形 7 份)
- `data/changelog.json` —— 增量变更日志(时间+账套+操作)
- `data/exports/` —— 用户手动「导出账套」产出的可携带 .json 文件

云端版(rj-cloud)本地也是 `serve.py` 读 `data/`，且其 `store.js` 已有 `Cloud` 上传通道。
**只要让两边指向同一个物理 `data/` 目录**，云端版就能直接读取本地版产出的文件并上传同步，零额外开发。

## 配置方式（二选一）

### 方式 A：云端版 FISCAL_DATA_DIR 指向本地版 data/
1. 在云端版 `rj-cloud/` 启动时设置环境变量：
   ```
   set FISCAL_DATA_DIR=<本地版绝对路径>\data
   ```
2. 云端版 `serve.py` 即读取本地版 `data/books/*.json`，经 `Cloud` 上传。
3. 本地版照常双击 `启动.bat` 使用，写的文件云端版立即可见。

### 方式 B：符号链接（适合固定装机）
```
mklink /D "<rj-cloud>\data" "<rj>\data"
```
将云端版 data 目录软链到本地版 data，两边物理同一目录。

## 注意事项
1. **不要两边同时写同一个账套**：建议本地版负责日常录入，云端版仅用于上传同步（只读或上传完成后写回）。
2. **file:// 模式下降级**：本地版若未获文件权限（如直接双击 html 且浏览器限制），数据存于 IndexedDB，不会写 `data/` 文件。此时云端版共享目录无新数据——需在本地版用 `http://localhost` 打开并授权目录，或定期"导出全部账套"落到文件。
3. **数据安全三层兜底**：
   - 第一层：真实文件 `data/books/`（清缓存不丢）
   - 第二层：`data/backups/` 自动备份（环形 5 份，防误删/损坏）
   - 第三层：用户手动「导出备份」到 `data/exports/` 拷 U 盘 + 云端版 `Cloud` 上传（跨设备/防本地文件损坏）
4. **本说明不改动 rj-cloud 任何代码**，仅描述配置方法。
