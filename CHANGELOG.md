# 变更记录（Changelog）

本文件记录历次功能优化与缺陷修复，按时间倒序排列。

---
## 2026-09-14 — 金蝶差异「分类定档」+ 落实两批（多栏账 3 处 / 凭证汇总表张数 / 折旧两表分工）

### 🧹 版本号信息彻底清理（维护者选 **C 方案**：删除旧品牌线 tag）
- **起因**：维护者问「怎么彻底清理之前的版本号信息，防止打包时再出问题」；
- **清点：版本号散在 4 个文件，其中 1 处是旧号**
  | 位置 | 清理前 | 谁在读 |
  |---|---|---|
  | `tauri/src-tauri/tauri.conf.json` | 0.5.1 | tauri-action → **安装包名 + GitHub Release**（权威源） |
  | `tauri/src-tauri/Cargo.toml` | 0.5.1 | Rust `CARGO_PKG_VERSION` → `app_version` → **界面「软件版本」+ js/update.js 的本地版本** |
  | `tauri/src-tauri/Cargo.lock`（`name="ty"` 块） | 0.5.1 | cargo |
  | **`tauri/package.json`** | **1.1.2** ⚠️ | **全库无人读**（npm 壳），但旧号会误导人 → **已改 0.5.1** |
  - **顺带排除两个担心**：`dist` 里的 `?v=` **不是版本号**，是 `build-dist.mjs` 用 `computeFingerprint()`（sha1 前 12 位）注入的**内容指纹**，按内容变、不会漂；界面「软件版本」是**运行时**从 Rust `app_version` 取，没有硬编码。
- **新增 `tools/version.js`（版本号单一入口）**：
  - `node tools/version.js` → 打印四处 + 一致性（只读，永不失败，供发版脚本第 1.6 步用）；
  - `node tools/version.js --check` → 四处一致 + semver 合法 + **该 tag 尚不存在**，任一不过非零退出；
  - `node tools/version.js --set 0.5.2` → **一次改四处**，只替换「顶层/本包」那个版本字段（`Cargo.lock` 精确定位到 `name = "ty"` 块，不误伤依赖版本），写完立刻回读核对。
  - 实测有效：它当场抓出 `package.json` 的旧号；`--check` 的「tag 已存在」守卫也当场拦住了刚发布的 v0.5.1。
- **`发布新版.command` 接入（注意：该文件在 `.gitignore` 第 12 行，本补丁只在本机，不入库）**：
  - 第 1.6 步：发版前**只读**打印四处版本现状（不判定成败）；
  - 第 4 步：原来的 `sed`（**只改 tauri.conf.json** —— 正是 `7a674ce`「版本号统一」要消灭的那类不一致）换成 `node tools/version.js --set`，`git add` 四个文件；并在**打 tag 前**加 `--check` 卡口（防止重复打同一 tag 让云端工作流去「更新既有 Release」而非发新版本）。
- **C 方案：删除 10 个旧品牌线 tag（本地 + 远程）**——维护者明确选择「彻底」。「添钰财务」新线自 `v0.5.0` 起算，旧品牌（心中有数/`com.chen.xzys`）线不再保留 tag。
  - 删除前后对照：`git tag --sort=-v:refname | head -1` 由 **v1.1.6 → v0.5.1**；`git describe` 由 **v1.1.6 → v0.5.1-2-g…**；
  - 保留：**`v0.5.0` / `v0.5.1`**（执行前有硬守卫：待删列表必须正好 10 个、且绝不含 `v0.5.*`）；
  - ⚠️ **连带影响**：GitHub 的 Release 与 tag 是**两个对象** —— 删 tag 不会删 Release 与安装包（旧包仍可下载），但旧 Release 会**失去 tag 关联**，在 Release 页看起来「没有 tag」；
  - 📌 **tag → 提交映射（留档，需要时可随时重建：`git tag <tag> <sha>` + `git push origin <tag>`）**：
    | 旧 tag | 提交 | 说明 |
    |---|---|---|
    | `v1.0.0` | `7910228` | ci: release 自动正式发布（releaseDraft=false） |
    | `v1.0.1` | `7ae822c` | ci: 重构自动发布（Windows 先建 Release、macOS 后附加） |
    | `v1.0.2` | `bfad28f` | chore: bump version to 1.0.2 |
    | `v1.1.0` | `1d0b82a` | 新增云备份/云同步（WebDAV），统一系统设置页排版 |
    | `v1.1.1` | `f6a9fd8` | 凭证打印/空白凭证、账表凭证字科目穿透补全、账务健康体检去噪 |
    | `v1.1.2` | `f29fbe1` | 明细账滚动布局优化、科目树自适应宽度、凭证保存防误触 |
    | `v1.1.3` | `572a724` | 科目栏编码+全路径名、搜索入口统一、内置凭证模板库、资金余额口径修复、发版对账卡口 |
    | `v1.1.4` | `8b3e8d2` | 常用功能默认顺序调整、录凭证设为固定项 |
    | `v1.1.5` | `707da93` | chore: bump version to 1.1.5 |
    | `v1.1.6` | `e2b6ad0` | 去金蝶化命名统一为 ty，清理死代码与重构期开发工具 |
  - **提交引用未丢失**：被删的只是「指向这些提交的指针」，上述提交仍在 `main` 历史里（`git log --oneline | grep 7910228` 等均可查到）。
- **执行过程中的一次失误（已如实记录）**：首次执行时按 bash 习惯用未加引号的 `$DEL` 做参数拆分，而本机登录 shell 是 **zsh**（不做默认词拆分），导致所有命令报错 —— **其实什么都没删**（已即时复查确认 12 个 tag 完好），随后改为**显式列举**写法并加守卫后重跑成功。

### 🚀 发版 v0.5.1（含 2026-09-13 与 09-14 两个工作日的全部改动）
- **流程**：两道发版卡口 → 提交 → 推送 → 打 tag `v0.5.1`；tag 触发 `.github/workflows/build-release.yml`：
  Windows job 先跑并**创建/更新 Release**（`releaseDraft: false` → **直接发布，非草稿**），macOS job `needs` Windows 完成后对同一 tag 附加 `.dmg` —— 双平台产物进同一 Release，约 10~20 分钟；
- 卡口结果：`node tools/audit_books.js` **全部账套通过**；`node tools/check-period-contract.js` **16 个期间控件全过**；
- 提交：`cd5b581`（主改动，26 文件 / +2195 −1051）+ `61eb20f`（chore: bump version to 0.5.1）；
- 📌 **版本线说明（易误判，务必留档）**：当前版本 **0.5.1**，而历史上还有到 **v1.1.6** 的 tag —— **这不是版本倒退**：
  - `7a674ce` 的提交说明明确写了 `tauri.conf.json / Cargo.toml: 1.1.6 → 0.5.0`「**与 git tag v0.5.0 对齐**」；
  - 且 `v0.5.0` 是**在 v1.1.6 之后**打的（指向 `8fbdc0e`，属于 `v1.1.6..HEAD` 这段）—— 即「添钰财务 + 新标识符 `com.chen.ty`」这条**新线从 0.5.0 起算**，1.0.x/1.1.x 是**旧品牌线**；
  - 因此旧线不会也不能升级到新线（**标识符不同**，本就装成两个 App；`js/update.js` 的 `compareSemver` 对 1.1.6 用户会判「已是最新」）—— 这是换血期的既定代价，**不是 bug**；旧装用户需手动换装。数据无虞：数据目录是**固定锚点** `~/Library/Application Support/添钰财务/`，不随标识符/品牌变化（见 `tauri/src-tauri/src/lib.rs` 的 `APP_DATA_DIR_NAME`）。
- ⚠️ **发现 `发布新版.command` 的一个缺口**：它只 `sed` 了 `tauri.conf.json`，**没有同步 `Cargo.toml` 与 `Cargo.lock`** —— 而这正是 `7a674ce`「版本号统一」要消灭的那类不一致。本次是**手工把三处一起改到 0.5.1**（`tauri.conf.json` / `Cargo.toml` / `Cargo.lock` 里 `name = "ty"` 那个包块）。**建议给脚本补上这两处**，否则下次发版又会漂回去。**待维护者确认。**

### 决策：把「全部对齐金蝶」拆成四类，其中一类明确**不对齐**
- **起因**：上一轮把 16 页与金蝶的差异逐项 diff 完，问「全部对齐后收益大吗」。**结论是收益不大，且有约 1/3 不该对齐** —— 判据不该是「金蝶有没有」，而应是「**会计干活时会不会碰到**」；
- **📌 明确不对齐清单（写进记录，防止以后又有人来「补齐」）**：
  | 差异 | 我方 | 金蝶 | 为何不对齐 |
  |---|---|---|---|
  | 折旧两表多「期末净值」列 | ✓ 有 | 无 | 我方多给一列有用信息（原值−累计折旧−减值），金蝶要用户自己算 |
  | 查凭证多「复选」「审核人」列 | ✓ 有 | 截图未见 | 功能更全（批量审核、审核人） |
  | 总账期间列格式 | `2026-08` | `202604` | 我方更易读（金蝶 `YYYYMM` 无分隔） |
  | 税金表月格文案 | `8期` | `8月` | 我方更准确（是会计期间、不是自然月）；**且金蝶自己这页用「月」、那页用「期」** |
  | 期间文案格式 | 统一 `2026年08期` | **自身 5~6 种**（`2026年第8期`/`2026年08期`/`2026年6期`/`2026-08-01`/`8月`） | 金蝶自己都不统一，对齐等于主动降质 |
  | 明细账「方向/余额」列顺序、查凭证「原单编号/原单据编号」、多栏账表头是否带编码 | — | — | 纯外观，收益近零 |
- **不做的（不是「不能」，是「不值」）**：查询端「期间起止」（8 页）。
  - 是**便利性**不是能力缺失：我方「单期 + 本年累计两列」已覆盖大部分查账需求，加起止的净增益只是「任意跨月区间一次看完」；
  - **成本被低估**：金蝶 `gl`/`dl` 在区间下是「每期 3 行汇总 + 逐笔分录」混排，而 `generalLedger(month)` / `detailLedger(code, month)` 是**单月**的、汇总行按月算 → 渲染器要按 N 期循环且每期各自重算汇总行，**不是套个 `monthList` 循环**；且与刚固化的单期契约（规则④⑤）冲突；
  - **收益递减 vs 成本持续**：「和金蝶像」的收益主要来自**迁移期**（会计操作习惯不用改、信任感高），用几个月就适应；而多一套语义/分支的成本是**永久**的 → 应「分批做最值钱的」，而非「一次全对齐」；
  - **改为按页按需**：哪一页真出现「不得不逐月切」的痛点，就只给那一页加。**待维护者给出真实痛点页清单后再动**。
- **附：另一笔账（迁移期一致性）** —— 你们正从金蝶 KIS 迁移（添钰来客、绅蓝之星），所以「像金蝶」确有真实价值，但它是**一次性、随使用递减**的；本轮只挑「小、有验收标准、一眼可见」的做掉。

### 本批实施（1/2）：多栏账 `ml` 四处显示层差异（**显示层现已与金蝶齐平**）
- **来源**：金蝶多栏账截图当规格 × `Ledger.js` `renderMl` 源码。此前 diff 出 5 处，本轮做掉显示层的 **4 处**（仅剩 ⑤ 查询端期间起止，属「不做」那一类 —— 见文末）；
  | # | 项 | 金蝶 | 我方改前 | 改后 |
  |---|---|---|---|---|
  | ① | 期初行**日期** | **`2026-07-01`**（区间首月 1 号） | **空** | `month + '-01'`（期初是「区间首月月初」这个时点，标出日期才看得出锚在哪天） |
  | ② | 期初行**分栏列** | 各下级科目期初余额有值 | **全空**（信息缺失） | 填各分栏列的期初余额（`S.generalLedger(month)` 按 code 取 `obDr−obCr`，与分栏列「命中本列及其下级」的取数范围一致） |
  | ③ | 分栏列**符号** | 贷方发生时显示**负数** | `money(r.dr \|\| r.cr)` **恒正**（丢方向） | `money(num(r.dr) − num(r.cr))`（借正贷负） |
  | ④ | 分栏**表头** | **两行**：父表头「借方」+ 列头「编码 + 名称」 | 单行、只有名称、无父表头 | 两行：基础 7 列 `rowspan="2"` + 父表头 `th.ml-col-parent colspan=N`「借方」，第二行列头为「编码 名称」 |
- **改动点**：`js/pages/ledger/Ledger.js` `renderMl`（表头块 / 期初行块 / 分栏数据列块）；`css/style.css` 新增 `.ml-col-parent`（居中 + 字重 500）；
- **✅ 验证（强证据，非自证）**：写临时脚本用**真实账套**跑恒等式，4 个账套全过：
  - **期初分栏列数值与金蝶截图逐字相符** —— 添钰来客 2026 账套 1002 银行存款的 4 个分栏列期初 = `182,095.62 / 0.05 / 282,185.40 / 202,375.98`，与金蝶多栏账截图（记于上文 diff 表 ②）**完全一致**；
  - 恒等式「每行各分栏有符号金额之和 == 该行 借−贷」：添钰 6/67 行、绅蓝 6/76 行，**失配 0**；
  - 恒等式「各分栏列期初之和 == 父科目期初净额」：4 账套全等（如绅蓝 2026：`522,456.07 == 522,456.07`）；
  - 符号方向抽查：绅蓝 2026 `1002007 公司用陈总中信银行账户` 期初 `-10,000.00`（贷方余额 → 负号），方向口径正确。

### 本批实施（2/2）：凭证汇总表 `sum` 补「张数」副标题
- 金蝶副标题：`凭证总张数：53张；附件总张数：0张` → 我方 `sum` 此前**无此统计**；
- 改动：`index.html` 在 `#sumGrid` 上方加 `<div class="sum-stat" id="sumStat">`；`Voucher.js` `renderSum` 在逐月累加时同时统计「凭证条数」与 `num(v.attach)` 之和（**用 `attach` 字段，与凭证上「附件 ___ 张」同值**，不是 `attachments[]` 元信息数组长度）；CSS 新增 `.sum-stat`（含 `:empty` 兜底，未选期间时不留空隙）；
- ⚠️ **已知边界**：`sumStat` 在表格**外**，而 `tyPrint` 只克隆 `table.grid`（见 app.js 打印契约）→ **此项屏幕可见、打印件不含**。若以后要纸面也带，需单独设计，不在本轮。

### 本批实施（3/3）：折旧汇总表 / 明细表确立「汇总 vs 明细」分工
- **问题**：我方 `das` 与 `dad` **列完全相同**（都逐资产列示），用户看不出区别 —— **等于少了一张表**；金蝶是「汇总表按类别看大盘、明细表逐资产看」；
- **改后**：
  | | 列 | 行结构 |
  |---|---|---|
  | `das` 折旧汇总表 | **8 列**：`类别`（「按部门汇总」勾选时表头变`部门`，`th#dasGroupTh` 动态切） + 7 金额列 | **一行一类别/部门**（只出合计）+ 表末 `合计` |
  | `dad` 折旧明细表 | 11 列：`类别\|编码\|名称\|部门` + 7 金额列 | **按类别分组**：组内逐资产明细行 + **组末「小计」**（`.subtotal-row`）+ 表末 `合计` |
- **收敛**（避免口径在两处再漂移）：新增 `DEPR_AMT_COLS`（列定义）、`DEPR_FIELD`（列键 → `_assetDeprRows` 真实字段名的**唯一映射点**）、`deprVal` / `deprSum` / `_deprGroups`；**屏幕上两表与两处导出都从这一组定义取**；
- **改动点**：`js/pages/asset/Asset.js`（`refreshDas` 的 `setRptHead` 列数 11→8、`btnDasExport` 改为汇总口径、`renderDas` / `renderDad` 重写、`btnDadExport` 改用共用列定义）；`index.html` `dasGrid` 的 `colgroup` + `thead`（11→8 列）；`css/style.css` 新增 `.subtotal-row`（浅灰底 + 次级文字色；**必须排在斑马纹规则之后**，同特异性后者生效）。

### 📌 顺带修出的两个**真实缺陷**（都不是本轮引入的）
1. **折旧明细表「合计」行 7 列里 6 列是 `NaN`**（`Asset.js` 原 `renderDad`）：
   - 原代码 `Object.keys(tot).forEach(k => { tot[k] += r[k]; })`，而 `tot` 的键是 `ab/md/yd/ae/im/ne`，`_assetDeprRows` 返回对象上的字段名却是 `accumBegin/monthDepr/yearDepr/accumEnd/impair/netEnd` → `r[k]` 全是 `undefined`，`0 + undefined = NaN`；**只有 `orig` 恰好同名所以对**；
   - 显示后果：`money(NaN)` → `num(NaN)` 的 `NaN` 分支不触发（`typeof NaN === 'number'` 提前 return），`toLocaleString` 把 NaN 格式化成 **字面 `NaN`** 打到纸面/屏幕上（不是 0.00，比 0.00 更刺眼）；
   - 修复：金额一律走 `deprSum(g.rows, k)`（经 `DEPR_FIELD` 映射），并用临时脚本对旧实现取证 —— 7 列里 **6 列 NaN**、原值列 1,180,000 正确；
2. **`renderDad` 的行渲染 4 个文本列未转义**（`_catName(r.cat)` / `r.code` / `r.name` / `r.dept` 直接拼 `innerHTML`，而同文件的 `renderDas` 是走 `esc` 的）→ 资产名称/部门含 `<` `&` `"` 会破坏单元格结构；本次重写时统一为 `esc(...)`。

### 验证与回归
- `node --input-type=module --check`：`Asset.js` / `Ledger.js` / `Voucher.js` **语法全过**；
- `node tools/check-period-contract.js`：**16 个期间控件全过**（默认值已声明、回调已注册、页面无直接赋值、**两端恒等**、无 `data-single` 双模开关）—— 本轮未碰期间控件，规则④⑤ 不受影响；
- `node tools/empty_audit.js`：3 个场景无异常、无 NaN；
- 临时核验脚本（用**真实账套** + 合成固定资产，跑完已删）：
  - 折旧两表恒等式「每列 明细合计 == 汇总合计」7 列全等；手算锚点 `原值 1,180,000` / `期末净值 1,133,600 = 原值−累计折旧−减值` 全中；
  - 多栏账 3 条恒等式见上文；
- ⚠️ **未被覆盖（已由维护者补验，见下）**：4 个真实账套**固定资产均为空**，故 `das`/`dad` 只能用合成资产验证口径恒等式，脚本层面**未能用真实资产数据验收**；
- ✅ **维护者已目视验收（2026-09-14）**：**导入固定资产卡片后，折旧两表与金蝶一致**。
  - 起因是维护者先提出疑问「折旧汇总表 —— 我们没有金蝶的编码和名称」，核实后确认**问题不在表，而在数据**；
  - **根因（已核实）**：**KIS `.ais` 导入不带固定资产** —— `js/kis-import.js` 只解析科目/凭证/余额，固定资产那行是写死的 `fixedAssets: []`（同 `salary: []`）。故 KIS 迁移过来的账套折旧三表必然全空，与本次改动无关；
  - **补数据的路径**：固定资产页「导入」走 Excel（`TyIo.parseAssetWorkbook`，已带金蝶风格表头别名），**不是** KIS .ais 那条路；
  - 结论：**本次对 `das`/`dad` 的改动无需再改**，本条闭环。

### 未做 / 待定（下一轮的输入）
- ⚠️ **待查（新发现的风险，非本轮引入）：「资产类别」的值域可能被导入成「名称」而非「编码」**：
  - 契约是**编码**：`S.addFixedAsset` 明确注释 `fa.category = fa.category || ''; // 类别（类别编码）`，`_catName(code)` 再把它映射成名称；
  - 但 **`TyIo.parseAssetWorkbook` 对「类别」列只是 `get('category', arr)` 原样取值**，**不做值域归一**（golden 同文件里 `life` 有 ÷12 折算、`salvage` 有「原值×残值率」补算，唯独 category 没有）；
  - 后果（若金蝶导出的「类别」列是**名称文本**）：① 类别列**显示正常**（`_catName` 查不到编码就原样返回入参）→ 所以肉眼看仍是「和 金蝶 一致」；② 资产卡片页**按类别筛选筛不到**这些资产（`_assetFilterList` 是 `fa.category !== cat`，`cat` 取自下拉 option 的 **value = 编码**）；③ **编辑该卡片时类别下拉选不中**（option 的 value 是编码）→ 保存后 `category` 被清成 `''` → 该资产掉进折旧汇总表的「未分类」行；
  - **10 秒自查**：打开任一张**导入**的资产卡片 → 看「类别」下拉是否已选中；或在卡片页用「类别」筛选一次能否筛出导入的资产；
  - 若确认，修法方向：导入时按「编码 → 名称」双查**归一为编码**；名称对不上已有类别时，按名称映射或按名称新建类别（需定策略，故列为待定）；
- `ml` 第 ⑤ 处：查询端期间起止（属上面「不做」的那一类，按需再说）。**显示层 4 处已全部做完** —— 维护者 2026-09-14 目视确认「我方多栏账与金蝶无可见差异」，故第 ① 处（期初行日期）按「补齐最后一处可见差异」的判断**直接做掉**，未再单独等确认（1 行、零风险，不想要可一行回退）；
- `das` 列头文案（金蝶「本期折旧额」父表头 + 子列 `2026年7月`）**本轮未动**，仍是 `2026-08折旧`；与「查询端起止」同属一条依赖，起止不做则不动；
- 查询端起止 / `sum` 按日期汇总 / `q` 双控件：**等维护者给出真实痛点页**。

### 附：跑测试时发现的两处**既存**告警（本轮未碰，登记备查）
- `node tools/test_stale_dom_refs.js` 失败：`js/pages/salary/Salary.js:42/54/57/64` 存在**未守卫**的 `$('salPeriodEnd').value`（该 id 由 `generatePeriodRanges()` 运行时生成，静态扫描判为失效引用；同文件其它页是 `var e = $(...); e ? e.value : ...` 的守卫写法）。属上一轮期间控件收敛的收尾，非本轮引入；
- `node tools/check_no_native_dialog.js` 失败：`js/file-save-bridge.js:93/164` 有裸 `alert()`（该文件是桥接层自身，非业务页面）；
- `node tools/test_typrint.js` 失败 3 项（**本轮新发现**）：`rptHeadPartsHtml is not defined` / `collectPrintBody is not defined`。原因是该脚本用「正则 + 花括号配对」从 `js/app.js` **抽取单个函数源码**再执行，而 `buildPrintHtml` 现在依赖 `rptHeadPartsHtml` / `stdRptHeadHtml` / `collectPrintBody`，抽取器只取了一个函数 → 依赖不在作用域内。**属脚本与被测代码脱节（测试支架过期），不是打印功能坏了** —— `js/app.js` 本轮未改。

### 顺带（同日，维护者提出）：固定资产卡片操作列改「蓝色可点击文字」
- **维护者原话**：编辑 / 复制 / 删除 / 清理 这几个，**金蝶是图标**，我们**简化显示成蓝色可点击文字**；
- **现状诊断（读代码后）**：这 4 个动作**本来就是文字**，但 `.link-edit` / `.link-copy` / `.link-clean` / `.link-unclean` **全库没有任何 CSS**，而标签是**无 `href`** 的 `<a>` → 浏览器不给链接配色 → 渲染成**与正文同色的黑字、无手型光标**，看着不像可点（是同模块 `.link-del` 有红色样式、`.link-toggle` 有蓝色样式，唯独这 4 个漏了）；
- **改动**：
  - `css/style.css`：给这 4 个类补上「蓝色 + 手型 + hover 下划线」；
  - `css/style.css`：新增 `.asset-ops a`（卡片操作列）—— 间距 **8px**（与原单页 `.op-link` 同规格）、并让本列的「删除」也随其余动作变蓝（`.asset-ops a` 特异性 0,1,1 > `.link-del` 0,1,0；但 < `.link-del.disabled` 0,2,0，故「不可删除」的置灰态不受影响）；
  - `js/pages/asset/Asset.js`：操作 `<td>` 加 `class="asset-ops"`；顺带去掉源码里用**空格**分隔动作的写法（`...</a> <a...` 末尾还留了一个尾随空格），改由 CSS `margin` 统一间距；
- **副作用（有意为之，非回归）**：`#subjGrid`（科目页）与**资产类别页**的「编辑」用的是同一个 `.link-edit`，此前同样是黑字 → 一并变蓝；
- ⚠️ **未改、待定**：`#dvGrid`（折旧凭证页）操作列的「**生成**」**复用了 `.link-del`** → 这个**非破坏性**动作现在显示成**红色危险色**（类名字义与用途不符）。本轮未动，等维护者定「一起变蓝」还是「换类名」；
- 打印态不受影响：`.grid a { color: inherit !important }` 仍把纸面文字压成黑色。
- **验证**：`Asset.js` 语法过；`css/style.css` 花括号平衡。

### 固定资产：修「资产类别」编码/名称错配 + 「新增资产凭证」改为**关联**已有凭证（用维护者真实账套验定）
- **起因**：维护者报「固定资产卡片页『新增资产凭证』我们是空的，金蝶有相关的记账凭证」；
- **第一步查证**：`fa.addVoucher` **全库只读不写**（读点 5 个：表格列 / `fAddVch` 筛选器 / 导出列 / 导入别名 / 删除守卫；写点 **0 个**）→ 该列必然恒空。`impairVoucher`（减值准备凭证）、`otherVoucher`（其他变动凭证）同病；**只有 `cleanVoucher` 有生成器**（`genCleanVoucher`）。

#### 📌 差点做错：这一列**不能**用「生成凭证」实现
- 用维护者**真实账套**（添钰来客，1037 张凭证 / 10 张卡片）逐张核对：**10 张卡片的购入凭证早已在账里** —— 按「借 1601 = 卡片原值」精确命中 **10/10**（记-48/49/50/51/56/58/78/79/47/28），且**贷方科目 == 卡片 `purchaseAcct`**（如洗碗机 `224104 丁家河店`）；
- 若按「生成凭证」做 → **固定资产重复入账（翻倍）**，正是本项目最忌讳的错误。**故改为「关联」**（金蝶对应位置也是卡片与凭证的关联）；
- **附：此前那个「原值含不含税」的问题，由数据回答了** —— 这 10 张购入凭证都是「借 1601 **全额** / 贷 对方科目 **全额**」、**无税行**，且全账套 1037 张里**没有一张借过 `22210101`**（卡片上配的进项税额）→ **原值即含税全额，1 借 1 贷**，不需要再加「税额」字段。

#### 📌 顺带查出并修掉的真缺陷：「资产类别」存的是**名称**不是**编码**
- 契约（`store.js` `addFixedAsset` 注释）是**类别编码**；实测导入数据 `category` 存的是**名称**（`器具、工具、家具` 7 张 / `电子设备` 1 张 / `其他固定资产` 2 张），而档案编码是 `001~006`；
- **显示看不出来**：`_catName(code)` 查不到编码就**原样返回入参** → 所以维护者此前目视「和 金蝶 一致」，把这个缺陷盖住了；
- **真实后果三条**（均已验证）：① 卡片页「类别」下拉**筛不到**（`_assetFilterList` 拿 `fa.category !== cat`，而 `cat` 取自 option 的 **value = 编码**）；② 左侧类别树同样筛不到；③ **编辑卡片时类别下拉选不中 → 保存后 `category` 被清空** → 该卡片在折旧汇总表掉进「未分类」；
- **根因**：预置类别档案**只写在前端 `Asset.js` 的 `assetCats()` 里（懒创建）**，而「存编码」这条契约只写在 store 的注释里 —— 两处分离，导入路径（`TyIo.parseAssetWorkbook` → `addFixedAsset`）**没有任何归一**。同文件里 `life` 有 ÷12 折算、`salvage` 有「原值×残值率」补算，**唯独 `category` 是原样直取**。

#### 改动清单
1. **默认类别档案搬到 store**：新增 `DEFAULT_ASSET_CATS` 常量、`emptyState().assetCats`、`S.assetCats()`；前端 `Asset.js` 的 `assetCats()` 收敛为**一行引用** —— 归一与回填必须与数据在同一层；
2. **新增 `S.normalizeAssetCategory(v)`**：空→`''`；命中**编码**→原值；命中**名称**→对应编码；**都命中不了 → 按该名称新建一条档案**（迁移金蝶自定义类别时不丢信息）；幂等；
3. `addFixedAsset` / `updateFixedAsset` 两个入口都过归一；
4. `normalizeState` **回填老账套**（新增瞬态计数 `_assetCatBackfilledN` 供脚本断言；只改内存、随下次写盘落库 —— 与 `backfillIncomeRowIds` 同款语义）；
5. **新增 `S.linkAssetAcquisitions()`**：**只关联、不生成**。匹配规则（需同时满足）① 凭证未删除；② 含一条分录「科目 = 卡片固定资产科目 且 借方 = 卡片原值」；③ 该凭证贷方含「卡片 `purchaseAcct`」（与卡片配置**互证**，防同额错配；`purchaseAcct` 为空时只按 ②）。多命中时**优先未被其它卡片占用的凭证**，再按「凭证日期离开始使用日期最近」、最后按字号升序。**幂等**，可反复点；
6. **新增 `S.unlinkAssetAcquisitions(ids)`**：错配回退，只清卡片的 `addVoucher`/`addVoucherId`，**绝不碰凭证本身**（凭证是账，不能因解关联而消失）；
7. **新增字段 `fa.addVoucherId`**：存**凭证 id**（格式 `word-no@YYYY-MM`，唯一）供跳转 —— 因为凭证号按「月」编，`word-no` 跨月会重号（**实测该账套里有 3 个「记-48」、3 个「记-28」**），只存 `word-no` 点不准；`addVoucher` 保留为**显示值**（列 / 导出 / 筛选读它），语义不变；
8. 「新增资产凭证」单元格渲染为 `.link-voucher` 链接（点击打开该凭证，走 app.js 已有的全局委托）；只有显示值而无 id 时退化为纯文本；
9. UI：工具栏新增「**关联新增凭证**」按钮；「批量操作」菜单新增「**解除凭证关联**」；
10. **`removeFixedAsset` 的删除守卫移除 `addVoucher`**：它存的是「卡片 ↔ 已有凭证的**关联**」（凭证来自迁移、不是本系统生成的，也不会因删卡片而消失），不构成删除障碍 —— 否则导入卡片**一经关联就再也删不掉**，与「外部导入的卡片可删」相悖。`deprMonth`（已计提折旧）/ `cleanVoucher`（已生成清理凭证）仍拦，守卫文案同步改为「（含折旧凭证）」。

#### 验证（用维护者真实账套；全程**只读不写盘** —— 脚本里把 `S.persist` 替换成空函数）
- **类别回填**：`器具、工具、家具 / 电子设备 / 其他固定资产` → `003 / 005 / 006`，**10/10**；类别档案仍 **6 条**（没有误建新类别）；`normalizeState` 幂等（再跑不改）；
- **筛选修复验证**：按编码筛 `003 → 7 张`、`005 → 1 张`、`006 → 2 张`（回填前**全是 0 张**）；
- **关联**：**10/10 全部命中**，与人工逐张核对结果一字不差；**凭证张数 1037 → 1037、分录总数 7096 不变**（铁证：没生成任何凭证）；每张卡片的 `addVoucherId` 都指向「借 1601 = 原值 且 贷 `purchaseAcct`」的那张凭证，且显示值与凭证字一致；再点一次 → **0 张新关联**（幂等）；
- **解除关联**：清空两个字段、**凭证张数仍 1037**；
- **回归**：`check-period-contract`（16 控件全过）、`verify_invariants`（**10/10 PASS**）、`empty_audit`（无异常无 NaN）、`audit_books`（**全部账套通过**）。

#### 边界 / 未做（下一轮输入）
- **`impairVoucher`（减值准备凭证）、`otherVoucher`（其他变动凭证）两列仍恒空** —— 本轮只做维护者所指的「新增资产凭证」。这两项要不要做、以及「生成」还是「关联」，待定；
- **Excel 导出→再导入会丢 `addVoucherId`**（导出列只有显示值 `addVoucher`）→ 重新导入后该单元格**退化为纯文本**（不能点击跳转）。此时 `addVoucher` 有值，`linkAssetAcquisitions` 会**幂等跳过**，故需先「解除凭证关联」再点「关联新增凭证」才能恢复跳转。属已知边界；
- ⚠️ **`tools/test_import_e2e.js` / `tools/test_book_e2e.js` 会写用户真实数据目录**：两者都依赖该目录下的 `default.json`（不存在）→ **必然失败**，且 `test_import_e2e` 会在**用户真实数据目录**里写入 `IMPORT_*.json` 垃圾账套（本次运行产生了 1 个，**已清理**）。本轮未动这两个脚本，登记备查。

### 固定资产：「使用部门」与金蝶对不上 —— 同「类别」一个病根（部门档案没跟过来 + 按编码比名称）
- **维护者报**：「固定资产卡片 部门和金蝶对不上，同一个账套」；
- **先定位在哪一层**：
  - **卡片上的部门值本身是对的** —— 我们 10 张卡片用的是 `厨房 / 客房 / 酒店 / 酒店洗衣房`，与金蝶折旧明细表截图里那一列是同一套（截图所见即这几个）；
  - **错的是「部门档案」**：本系统 `state.depts` 一直是内置默认种子 **`001 前台 / 002 客房 / 003 餐厅`**；而 **KIS `.ais` 导入完全不带部门档案**（`js/kis-import.js` 全库 **0 处**提及 `depts`/部门）；
  - ⇒ 资产页左侧「部门」树与「部门」筛选下拉里**只有 前台/客房/餐厅**，卡片里写的 厨房/酒店/酒店洗衣房 在里面根本不存在 → 看起来就是「跟金蝶对不上」；
- **顺带查出第二重错配（与「类别」同型、但方向相反）**：筛选是 `fa.dept !== dept`，而 `dept` 取自 `fDept` option 的 **value = `d.code`**（`'002'`），卡片里存的却是**名称**（`'客房'`）→ **按部门筛选恒为 0 张**（左树同理，传的也是 `d.code`）；
- **⚠️ 为什么这一处要归一到「名称」，而类别归一到「编码」（方向相反，别照抄）**：
  | | 卡片表单控件 | store 契约注释 | 归一方向 |
  |---|---|---|---|
  | 资产**类别** | `<select>` 下拉（option value = code） | `addFixedAsset`: 「类别（**类别编码**）」 | → **编码** |
  | 使用**部门** | `<input>` **自由文本** | `addFixedAsset`: 「部门」（无编码契约） | → **名称** |
  且部门的全库消费方（左树 / `fDept` / 折旧汇总表「按部门汇总」）都是按**名称**比对，故本处把「按编码比」改成「按名称比」，不动卡片数据。

#### 改动清单
1. **`S.depts()`**：部门档案的单一事实源（缺省自动种子）。同时**删掉 `Salary.js` 里那份逐字重复的 `DEFAULT_DEPTS`**（与默认类别档案当初的重复定义同源）；
2. **`S.normalizeDept(v)`** → 归一为**名称**：空→`''`；命中名称→原值；命中**编码**→对应名称；都命中不了→**按名称新建档案**（金蝶的部门不在默认种子里，不新建就永远选不到）；幂等；
3. `addFixedAsset` / `updateFixedAsset` 入口过归一（手填的新部门会自动补进档案）；
4. `normalizeState` **回填老账套**（瞬态计数 `_assetDeptBackfilledN`）：把 厨房/酒店/酒店洗衣房 补进 `depts`，卡片 `dept` 值本身不变；
5. **Asset.js**：左侧部门树与 `fDept` 下拉改为传/比**部门名称**（`d.name`）——这是让筛选真正生效的关键一行；
6. `Salary.js` 的部门职员页/新增部门改走 `S.depts()`；
7. **卡片表单的「使用部门」由自由文本 `<input>` 改为下拉 `<select>`**（维护者确认要改）：option 的 value 就是**部门名称**，与 `fa.dept` 同口径。填充规则与旁边的「类别」下拉完全同款 —— 新增时只列启用部门；**编辑时补回该卡片当前部门（含已停用）**；并加一条**兜底**：卡片上的部门若不在档案里（老数据/绕过归一手工塞进去的），也把它列出来，否则 `<select>` 会回落到「请选择」、一保存就把部门清空。校验提示同步改为「请选择使用部门」。

#### 验证（真实账套，只读不写盘）
- 回填前 `depts = 001前台/002客房/003餐厅` → 回填后 **`004厨房/005酒店洗衣房/006酒店` 已补进档案**（只补缺的 3 个，条数 3→6，无重复）；卡片 `dept` 取值不变；
- **按部门筛选**（下拉 value = 名称）：`客房 → 5 张`、`厨房 → 1 张`、`酒店洗衣房 → 1 张`、`酒店 → 3 张`、`前台/餐厅 → 0 张（确无资产）`，合计 10 = 全部卡片 ✓（**修复前恒为 0 张**）；
- 归一方向：`normalizeDept('002') → '客房'`、`'客房' → '客房'`、`'' → ''`、未知部门按名称新建档案 ✓；`normalizeState` 幂等；
- 回归：`check-period-contract` 全过、`verify_invariants` **10/10 PASS**、`empty_audit` 无异常、`audit_books` 全部账套通过；
- **下拉改动的专项验证（跑的是真实源码，不是复刻）**：用 `tools/test_typrint.js` 同款做法从 `Asset.js` **抽出 `_openAssetModal` 函数源码**，配最小 DOM 桩（`<select>` 的 `value` 只在 options 里存在时才生效，与真浏览器一致）跑真实账套：
  - **10 张卡片编辑时部门都能回填，失配 0**（这是最要紧的一条：改下拉不能把历史部门弄丢）；
  - 新增 → 列出全部 **6** 个部门（= 档案条数）；
  - 把一个部门**停用**后：编辑用它的卡片**仍能选中**，新增时**不再列出**；
  - 卡片部门**不在档案里**（老数据）→ 兜底列出、`value` 不回落到「请选择」，保存不会清空。

### 固定资产：补齐「部门」的**编辑入口** + 资产页直达部门弹窗
- **背景（维护者问「在哪新增部门？及修改」）**：查证后确认 —— **新增有、修改没有**：
  - 入口只有一处：**工资页 → 工具栏「部门职员」→ 弹窗 →「新增部门」**（两步 prompt：名称 + 编码，编码默认 = 条数+1 三位补零）；它原本是独立页 `page-department-staff`，后来被「收敛」进工资页弹窗；
  - 该弹窗表格是 `编码 | 名称 | 类型 | 上级 | 操作`，而**操作列只有一个「启用/停用」开关，没有编辑** —— 于是本轮自动建出来的 `004 厨房 / 005 酒店洗衣房 / 006 酒店` **改名、改编码都做不到**；
  - 且**资产页没有部门入口**，而资产卡片恰恰要用部门（「使用部门」就在卡片弹窗里）—— 得跑去工资页改，路径绕。

#### 改动清单
1. **「部门职员」操作列加「编辑」**（`.link-edit`，与资产类别页同款蓝字链接），与「启用/停用」并列；
2. **新增 `S.renameDept(idx, name, code)`** —— 基础资料编辑的**唯一入口**，把「改档案」与「回写卡片」**绑在一起做**：
   - ⚠️ **为什么必须回写**：部门是按**名称**存的（与类别存编码相反，理由见 `normalizeDept`），只改档案名而不动 `fa.dept`，历史卡片就变成「档案里查无此部门」→ 按部门筛选掉出、编辑卡片时下拉回落成「请选择」——**那正是本次要修的那个病的翻版**，所以两件事不能拆开；
   - 守卫：名称/编码非空、编码在档案内**唯一**、下标有效；返回 `{ ok, renamed, touched }`；
   - 只改编码不改名时 `renamed=false → touched=0`，不白动数据；
3. **Salary.js**：`dsBody` 上加一条委托点击处理「编辑」——与「新增部门」同款两步 prompt，默认值**预填当前值**（想改哪个改哪个，其余回车即可），任一步取消即整体放弃；保存后 toast 明确告知「**并同步了 N 张资产卡片的部门**」（否则用户不知道改档案还动了数据）；
4. **资产页工具栏加「部门」按钮**（与「资产类别」并列）：**复用同一个「部门职员」弹窗**，不另建一套（部门基础资料全库只有一份）。渲染走 `main.js` 已挂的 `__renderDeptStaff`；关闭按钮与行内「编辑/启用停用」的绑定仍在 Salary.js，此处**只负责打开**，不重复绑。

#### 验证（真实账套，只读不写盘）
- 改名「厨房」→「厨房部」：`renamed=true`、**回写 1 张卡片**（= 原用该部门的卡片数），档案与卡片同步；**改名后每张卡片的部门仍能在档案里按名称找到**（筛选不掉出）；
- 只改编码不改名 → `renamed=false, touched=0`（没白动数据）；
- 守卫：编码重复 / 名称空 / 编码空 / 下标不存在 → **全部拒绝，且无副作用**（复查确认编码未被改坏）；
- 改回原名 → 又把卡片同步回去（可逆）；
- 回归：`check-period-contract` 全过、`verify_invariants` **10/10 PASS**、`empty_audit` 无异常、`test_stale_dom_refs` **未新增失效引用**（仍只有 Salary.js 那 4 处 `salPeriodEnd` 既存问题）。

### 固定资产卡片页：工具栏**按组重排 + 只留一个主色 + 消除两处命名打架**（纯 UI 重排）
- **起因**：维护者列出整排 9 个按钮问「怎么优化一下更好」，并采纳建议「先按你的建议优化」；
- **诊断出的 5 个问题**：
  1. **三个主色按钮**（`新增` / `生成凭证` / `导入` 都是 `btn-primary`）→ 一排三块蓝，没有视觉落点；
  2. **四种性质混在一排**：对卡片做事 / 改基础资料 / 数据进出 / 批量操作；
  3. **「生成凭证」名不副实**：实为**双用途**（没勾已清理卡片→计提折旧；勾了→生成清理凭证），靠隐式条件切换，谁都猜不到；
  4. **一对反操作被拆散**：「关联新增凭证」在顶层、「解除凭证关联」在批量菜单里；
  5. ⚠️ **命名互相打架**：「生成**凭证**」（造新凭证）vs「关联**新增凭证**」（挂已有凭证）——**字面相近、语义相反**，最易误点的一对；
- **改后排布**（按使用频率从左到右，竖线分三组）：
  ```
  [新增]  [计提折旧]  [批量操作 ▾]   │   [导入] [导出] [打印]   │   [资产类别] [部门]
  ```
  | 改动 | 理由 |
  |---|---|
  | 「生成凭证」→「**计提折旧**」 | 95% 的用法就是这个；名字含糊的根因是隐含双用途 |
  | 「生成清理凭证」→ 移入「批量操作 ▾」 | 它本来就要先勾选已清理卡片；且与「批量清理」是**同一条业务链的两步**（先清理→再生成凭证） |
  | 「关联新增凭证」→「**关联凭证**」，移入「批量操作 ▾」 | 与「解除凭证关联」**成对挨着**（问题 4）；名字短一半，避开与「生成凭证」的字面混淆（问题 5） |
  | 只留「**新增**」为 `btn-primary` | 主操作只应有一个；「生成凭证」「导入」取消主色 |
  | 「导出」去掉 `ty-dropdown` 假箭头 | 见下条 |
- **批量操作 ▾ 菜单结构**（用一条分隔线分成两组）：
  ```
  生成清理凭证 / 关联凭证 / 解除凭证关联        ← 写"凭证"类
  ────────────────
  批量清理 / 批量删除                          ← 改卡片状态类
  ```
- **交互改动（原提的 A/B 二选一，采纳 B）**：「关联凭证」**不再要求先勾选** —— **勾了就只关联勾选的，没勾就关联全部未关联的**（为此给 `S.linkAssetAcquisitions(ids)` 加了可选筛选参数，不传=全部，保持向后兼容）。菜单里其余各项仍需勾选，通用守卫「请先勾选要操作的卡片」下移到 `link` 分支之后；
- **「生成清理凭证」保持原行为的两个细节**：① 只挑「已清理且未生成清理凭证」的卡片，挑不到给 warn 提示（原实现是"挑不到就默默去计提折旧"，拆开后改成了明确提示）；② **不加二次确认**（与「计提折旧」一致 —— 本页写凭证的动作历来不确认），`genCleanVoucher` 自身对已生成的卡片跳过（幂等）；
- **改动点**：`index.html`（工具栏重排 + 菜单加两项与分隔线 + 删 `#btnAssetLinkVch`）；`Asset.js`（删 `btnAssetLinkVch` 处理器、`btnAssetGenVoucher` 收敛为只做计提折旧、批量处理器新增 `cleanvch` / `link` 两个分支）；`store.js`（`linkAssetAcquisitions(ids)` 可选筛选）；`css/style.css`（新增 `.orig-btn-sep` 工具条竖线、`.orig-batch-menu .orig-batch-sep` 菜单分隔线）。
- **验证**：
  - HTML 的 `data-batch` 取值（cleanvch/link/unlink/clean/del）与 JS 分支**逐一对应**（`del` 走最后的 else），无悬空项；`btnAssetLinkVch` 全库残留引用 **0 处**；
  - `linkAssetAcquisitions(ids)` 可选筛选（真实账套，只读不写盘）：传 2 个 id → **只关联那 2 张**；不传 → 关联全部未关联的 **8 张**（已关联的幂等跳过）；传空数组 = 传全部；传不存在的 id → 什么都不做；**凭证张数 1038 → 1038 不变**（只关联不生成的铁证）；
  - 回归：`check-period-contract` 全过、`verify_invariants` **10/10 PASS**、`empty_audit` 无异常、`audit_books` 全部账套通过、5 个改动文件语法全过。
- **📌 顺带取到了两次"线上实证"**（维护者已在真实 app 里点过，磁盘账套可查）：
  1. **关联功能端到端生效**：10 张卡片的 `addVoucher` / `addVoucherId` 全部落库，且 id 是含月份的完整形式（`记-48@2025-07`、`记-78@2025-09`、`记-28@2026-06`）—— 正是为了跨月不重号才引入的格式；
  2. **类别回填生效**：卡片的 `category` 已是编码（`003` / `005` / `006`）。
  - ⚠️ 但 **`depts` 仍是默认 3 条**（001前台/002客房/003餐厅）—— 因为部门回填是 `normalizeState` 做、**只改内存、随下一次写盘落库**（与 `backfillIncomeRowIds` 同款语义）；维护者上次点击发生在本轮部门修复**之前**，重启后档案即会补齐为 6 条。

### 顺带：资产卡片页「导出」的装饰箭头 + 全站死标记 `<i class="tyicon tyicon-arrow-down">` 清零
- 维护者看到「导出」也有个小三角：「没用就删除」；
- **「导出」与「生成凭证」同款问题**：带 `ty-dropdown` → CSS `.btn.ty-dropdown::after` 画出三角（暗示有次级操作），但 `btnAssetExport` 的点击处理是**直接导出** `_assetFiltered`，背后没有任何菜单。去掉 `ty-dropdown` 类（连带 `padding-right: 24px`）与那个死 `<i>`；
- **顺手把死标记清零**：`<i class="tyicon tyicon-arrow-down">` 全站出现 3 处（生成凭证 / 导出 / 批量操作），而 **`tyicon-arrow-down` 全库没有任何 CSS / JS 定义**，渲染不出任何东西。前两处随各自的箭头一起去掉；第三处「批量操作」是**真下拉**（有 `assetBatchMenu`），所以**保留 `ty-dropdown`**（CSS 三角是它的真箭头），只删里面那个死 `<i>`——与「模板」按钮的写法（只带 `ty-dropdown`、不带 `<i>`）统一；
- **结果复核**：全站 `ty-dropdown` 只剩 **2 处，都是真下拉** ——「模板」(`vchTplMenu`) 与「批量操作」(`assetBatchMenu`)；死标记 `tyicon-arrow-down` 的实际标记 **0 处**（仅存于注释里）；
- **同页按钮排布的进一步优化建议（已讨论，待维护者拍板，本轮未动）**：现在 9 个按钮混了四种性质（动卡片 / 改基础资料 / 数据进出 / 批量），且有 **3 个 `btn-primary`**（新增、生成凭证、导入），主次不分；两处命名还互相打架 ——「生成**凭证**」（造新凭证）vs「关联**新增凭证**」（挂已有凭证）**字面相近、语义相反**。建议排布：`[新增][计提折旧][批量操作▾] │ [导入][导出][打印] │ [资产类别][部门]`，其中「生成凭证」改名「**计提折旧**」、「生成清理凭证」与「关联凭证」移入「批量操作▾」（与「解除凭证关联」成对）。**待确认**。

### 顺带：去掉资产卡片页「生成凭证」按钮的**装饰箭头**
- **起因**：维护者点该按钮后问「什么时候点、什么逻辑」，核对时发现箭头是假的；
- 按钮带 `class="btn btn-primary ty-dropdown"` → CSS `.btn.ty-dropdown::after` 画了个三角，**暗示有次级操作，但点击直接执行动作、背后没有任何菜单**（同页「批量操作」才是真下拉，有 `assetBatchMenu`）。这与查凭证页早先删掉的那批装饰 `▾` 是同一类问题；
- **额外发现（死标记）**：它还挂了 `<i class="tyicon tyicon-arrow-down">`，而 `tyicon-arrow-down` **全库只有 index.html 这 3 处引用、无任何 CSS/JS 定义** —— 真正可见的箭头只来自 `.ty-dropdown::after`，那 3 个 `<i>` 是渲染不出东西的死标记；
- **改动**：`#btnAssetGenVoucher` 去掉 `ty-dropdown` 类（连带去掉它附加的 `padding-right: 24px`）与那个死 `<i>`；
- ⚠️ **同页 `#btnAssetExport`（导出）有一模一样的假箭头 + 死 `<i>`**（同样点击直接导出、无菜单）。维护者本次只指了「生成凭证」，故**只改这一个**，导出那个待确认；
- **按钮职责仍是双用途、未改名**（未勾选「已清理」卡片 → 计提折旧；勾选 → 生成清理凭证）—— 维护者本次只选择去箭头，故名字与隐式切换保持原样，登记待议。

### 📌 附：本轮问答中顺带完成的一次**账实核对**（维护者真实账套）
- 维护者点了资产卡片页的「生成凭证」（= `depreciateMonth('2026-08')`）→ 生成 `转-1 @2026-08-31「计提2026-08固定资产折旧」`（20 条分录，借=贷=10,866.63），并回写 10 张卡片的 `deprMonth/periodUsed/accumDepr`；
- **核对该操作是否正确**：卡片导入时的 `periodUsed` 恰好等于「已计提至 2026-07」——按「购入凭证月份次月起提」逐张验证：洗碗机（记-48 @2025-07-31，12 期）、空调冷却塔（记-78 @2025-09-30，10 期）、电梯（记-47 @2025-11-25，8 期）、沙发折叠床（记-28 @2026-06-23，1 期）… **10 张全部自洽**；且 2026-07 已结账、2026-08 未结账 → **2026-08 正是该提的下一期，操作正确**；
- **账实核对结果（分毫不差）**：
  | 科目 | 总账 | 卡片合计 | 差 |
  |---|---|---|---|
  | 1601 固定资产 | 861,097.35 | 861,097.35 | 0 |
  | 1602 累计折旧（计提**前**） | 108,786.87 | 108,786.87 | 0 |
  | 1602 累计折旧（计提**后**） | 119,653.50 | 119,653.50 | 0 |
- 结论：**金蝶导过来的卡片与凭证完全对得上**；此前「导入凭证里 1037 张没有一张 `kind=depr`」只是**我们的业务标记**没打（金蝶的折旧凭证有 12 条 1602 贷方分录，确实入过账），不是数据缺失。

---
## 2026-09-13 — 期间选择器契约收敛（修「选了期间没反应」、默认值声明化、契约自检）

### 起因：总账页改「开始期间」后查询数据不变
- **现象**：总账页选期间 → 点查询 → 表格毫无变化；
- **诊断（实证，非推测）**：搭桩 DOM 跑完整交互链路，结果是「点右侧『结束期间』→ 表格指纹变化 ✓；只改左侧『开始期间』→ 指纹完全不变 ✗」，而控件本身（开弹窗、点格子、写值、触发回调）全部正常；
- **根因**：控件渲染成**范围式**（左「开始期间」/右「结束期间」，触发器可显示「X期 ~ Y期」），但 16 个控件里 13 个是**单期口径**（只读结束期间）——改左列必然无反应。`Report.js` / `TrialBalance.js` 原注释「口径保持单期间（用结束期间），仅 UI 对齐参考实现 range picker」说明这是**代码里明确写过的取舍**；
- **真正引入点（后续查证补充）**：原设计里这 13 个页面**本不该出现两列** —— 控件有一套由 `data-single-period` 声明的单期/范围**双模式**，单期页只渲染一列「选择期间」并强制 `start = end`。**`2adc17b` 的 popover 重构丢掉了该属性的渲染分支**，于是所有页面都被渲染成范围式，才产生「改了没反应」；同时 3 个真范围页面（查凭证/凭证汇总表/费用明细表）的双面板入口也被一并静默移除 —— 即后来所说「能力休眠」的由来。**这个 bug 是那次重构引入的，不是原作者留了个没用的取舍**；
- 附带问题：快捷按钮「今年/去年」设的是「年初~年末」，对单期页面而言只取年末月，用户以为在看全年，实际只看 12 月一期，属同类误导。

### 参考 jinbooks 的结论（本次方案的决策依据）
- jinbooks 前端**全站 0 处「开始期间 / 结束期间」UI**（已全库检索确认）；
- 其模型统一为「**粒度 + 报告期**」（`periodType` = month/quarter/halfYear/year + `reportDate`），区间由后端 `parse()` **自动派生**（如 QUARTER → `dateRange=[y-01, y-03]`），用户从不手选起止；
- 分工清晰：**查凭证**用「年份 / 月份 / 日期」三个独立筛选（属检索，连起止都没有）；**凭证汇总表 / 报表类 / 总账科目余额表**用粒度；其中 `subjectBalance` 在 `allMonths.size() > 1` 时走 **`groupCodeSubjectBalance` 区间聚合**——说明金蝶口径下账簿按区间聚合是成立的（期初=区间首月月初、本期=区间各月合计、期末=区间末月月末、本年累计=年初至区间末月）；
- 结论：需要「任意起止」的只有**检索类**；会计期间类应当用**粒度**表达。此结论作为后续「范围期间」方案的依据（见文末待办）。

### 期间控件重写为单期（由维护者完成），本轮补齐与修复
- 组件精简为只做一件事：点触发器 → 弹 12 月 grid → 选一期 → 写值并调 `data-on-change`；index.html 改为声明式占位符 `<div data-period="gl" ...>`，由 `generatePeriodRanges()` 启动时展开；
- 组件侧优化：`eval(onChange + '()')` → `globalThis[cbName]`（eval 有 CSP unsafe-eval 限制、字符串注入面、严格模式作用域差异）；期间文案恢复**期数补零**（`2026年08期`），与报表抬头 `setRptHead` 的「YYYY年MM期」一致；`state.startYear` → `state.year`（已无起止两列）；`updatePeriodRangeTrigger` 补空值保护；
- 修 `Home.js` 跳转：`__plJumpToRow(plRows, p.from, p.to)` → `(plRows, p.to, p.to)`——利润表是单期口径只渲染结束期间，传 from~to 会让触发器显示「2026年01期 ~ 2026年08期」范围文案；改后**数字完全不变**（整段累计落在「本年累计」列）而文案回归单期。

### 两个静默失效的 bug（费用明细表「选了期间没反应」）
- **① 回调名未注册**：`index.html` 写 `data-on-change="refreshExpenseDetail"`，而实际挂在全局的是 `__renderExpenseDetail`（`main.js`），故回调永远查不到。原 `eval` 写法同样会抛 ReferenceError，只是被 catch 静默吞掉；改 `globalThis` 查找后才会明确 warn；
- **② 初始化无条件覆盖用户选择**（真正根因，改①无效）：`ExpenseDetail.js` 的 `initEDFilters()` 是旧版 `<select>` 时代残留——`startSel.innerHTML = opts`（对 hidden input 无效）且 `startSel.value = cp; endSel.value = cp;` **无条件重置**。完整失效链：用户选期 → 组件写值 → 回调 → `initEDFilters()` 又重置回默认期 → 渲染读到的永远是默认期；
- 已确认这是**全库唯一一处**无条件重置：其余 15 个页面本来就用的正确口径（`x.value = x.value || def`）。

### 默认值从「各页面决定」收敛为「一处声明」（结构性根治）
- **问题**：期间控件的「UI 交互」是全局共用的，但「默认期间是什么 / 怎么填」一度由 16 个页面各自决定，其中 **8 处是逐字拷贝**——典型「改一处漏五处」。代码注释里其实早就记过这事（「此前 6 个页面各存一份逐字相同的实现，改一处漏五处，故收敛为引用」），但只收敛了一半；
- **收敛**：默认值改由 `index.html` 的 `data-default` **声明**（5 个 `bs/pl/cf/tx/ed` 用 `lastClosedPeriod`，其余 11 个用 `currentPeriod`），组件单点解析（新增 `__PERIOD_DEFAULT_OF__`），`app.js` 的 `periodRangeValue(prefix, def)` 的 `def` 改为**可选**（省略时回查声明）；
- **解析时机刻意放在「页面读取时」而非「DOM 展开时」**：展开发生在 `main.js` 早期，此时账套可能尚未加载完成，`currentPeriod()` 会拿到空值；
- 16 个页面统一走 `periodRangeValue(prefix)`：消除 **8 处内联拷贝**（`TrialBalance.js` 1、`Asset.js` 3、`Voucher.js` 2、`Report.js` 2）+ 去掉 **7 处多余的 `def` 参数**（gl/dl/ml/sal/sst/cf/tx）；
- 顺带修一个脏值隐患：账套未加载时兜底可能返回 `null`，而 `input.value = null` 会被 WebIDL 转成字符串 `"null"` 写进输入框，已加固为返回空串。

### 删除死 change 监听（8 个 addEventListener / 7 段代码块）
- 组件重写后**不再派发原生 `change` 事件**（只写值 + 改文案 + 关弹窗 + 调回调），故页面上对期间输入的 `addEventListener('change', ...)` 全部永不触发，成为死代码，且注释「组件 applySelection 后 hidden input 派发 change 事件」已与实现相反、会误导后人；
- 已删除 8 个 `addEventListener`（7 段代码块）：`Asset.js` das/dad/acl 各 1、`ExpenseDetail.js` ed 2 个（含已无意义的 `start<=end` 校正）、`Salary.js` sal+sst 1 个、`Voucher.js` sum 1 个与 q 1 个；
- 被删监听引用的 `dasMonth/dadMonth/aclMonth/qRender/refreshSalaryStats` 经核实另有使用处，均保留；`qRender` 仍由科目输入的 change 监听使用；
- 说明：若将来为 `查凭证 / 凭证汇总 / 费用明细` 恢复区间 UI（改用原生 `<select>` 或日期控件），原生控件本身会派发 `change`，届时按需重新绑定即可。

### 新增契约自检并接入发版卡口
- 新增 `tools/check-period-contract.js`，三条规则：① 每个 `data-period` 必须声明**合法**的 `data-default`；② 每个 `data-on-change` 必须在 `main.js` 注册为全局函数；③ `js/pages/` 下**不得直接给期间输入赋值**（跳转函数 `*Jump*` 除外，属正常用途）；
- **反向测试 4/4 通过**（证明不是空过）：缺 `data-default` / 默认值非法 / 回调未注册 / 页面直接赋值，四类违规均被捕获，且测试后 `index.html` 按校验和恢复；
- 期间还修掉一个误报：`codeInput` 含子串 `eInp`（c-o-d-**eInp**-ut），已改为 `\b` 词边界匹配；
- 已接入 `发布新版.command` 作为第 1.5 步卡口（与 `audit_books.js` 同样支持输入 `y` 强制继续）。

### 期间选择器终局方案定稿 + Phase 1 清理（清 9 项残留、规则④⑤、查出 2 处真实契约违反）
- **背景**：上一轮把控件重写为单期后，代码里仍留着「范围 / 单期双模」的一整套化石 —— 它们已无调用点，但会让人误以为"范围模式还支持"，从而把起止端点加回来、重演「选了期间没反应」。本轮把残留清干净，并把「控件只产出一个报告期」这条契约**变成机器可查的不变量**；
- **本轮定格（Phase 1 范围内）**：控件只产出一个「报告期」(`YYYY-MM`)。依据：① 报表已用「本期 + 本年累计」**两列**表达了区间（5 张报表列头实证）；② 13/16 个页面只读结束期间，**保留两个可改端点 = 保留这个 bug 类别**；③ 起止在**我们的**资产负债表列结构（期末余额 \| 年初余额）下需要**改列语义**才能成立 —— 金蝶/jinbooks 的做法正是列头随粒度在 `年初余额`/`期初余额` 间切换，所以那是"要改列义"，**不是"无解"**；
  ⚠️ 「永不产出起止区间」是**本轮的范围决定**，不等于"范围模式不可行"—— 后者已被推翻，见下方勘误；
- **清理清单（9 项，均为零调用点）**：

  | # | 残留 | 位置 | 判定依据 |
  |---|---|---|---|
  | 1 | `data-single="1"` ×15 | `index.html` | 组件透传为 `data-single-period`，**当前**全库无消费方（唯独 `ed` 没写）—— 但它是"被弄丢的机制"的残骸，见下方 ⚠️ |
  | 2 | `safeFillPeriod` | `app.js` | 被 Report/Salary/Asset 只 import、**从未调用** |
  | 3 | `fillPeriodSelect` + `allMonths` | `app.js` | 旧 `<select>` 填充分支；前者是后者唯一调用方，删后 `allMonths` 亦无消费方 |
  | 4 | `periodRangeOptions` / `periodRangeOptionsOri` | `report/_shared.js` | 被 `Original.js` import 但**零调用** |
  | 5 | `allBookedMonthsOri` | `report/_shared.js` | 无任何 import；且是 `store.allMonths` 的重复实现 |
  | 6 | `getSubjectNameByCode` | `report/_shared.js` | 定义并导出但零调用 |
  | 7 | 范围文案分支 `s ~ e` | `PeriodRangePicker.js` | 组件恒写相等值 → **不可达** |
  | 8 | `data-single-period` 透传 | `PeriodRangePicker.js` | 同第 1 项 |
  | 9 | `periodRangeValue(prefix, def)` 的 `def` | `app.js` | 16 个调用方**无一传参** |

  （`bookKey` 经查仍被 `Ledger.js` 的两个下拉守卫使用，保留）
  ⚠️ **关于第 1 项的重要更正**：`data-single` 并非"废弃想法的化石"，而是**按页面声明口径**这一设计的标记 —— 见本节末尾的勘误。原作者用它表达：13 个单期页 → 控件只渲染一列「选择期间」并强制 `start = end`；3 个真范围页 → 渲染双面板「开始期间/结束期间」。它之所以沦为"无消费者的死属性"，是因为 `2adc17b` 的 popover 重构丢掉了该属性的渲染分支。**删除它未造成功能回归**（popover 版早已对全部页面按单期运行，3 个范围页的取数层也完好），但**口径声明的落点被一并删除了**；将来若恢复范围模式，需重新加回该声明（一个 `data-*` 属性，成本很低）。
- **`Original.js` 有一整行 import 是死的**：第 3 行的 8 个符号零调用，只有第 2 行的 `currentPeriod`/`$`/`S` 等是活的 —— 已删除死的那一行。这也解释了为何该文件此前查不出问题：符号在 import 行上"出现过"，肉眼扫一眼会以为在用；
- **`monthsBetween` 同名不同语义（本项最易踩）**：同一个名字下并存**三份**实现 —— `store.js` 返回「相差整月数」(number)、`report/_shared.js` 返回「月份列表」(array)、`Voucher.renderSum` 内联又一份列表（且带一个多余的 `U.monthsBetween &&` 守卫，括号里的 IIFE 并没用到它）。现统一为**差月数 = `monthsBetween`、列表 = `monthList`**：列表实现下沉到 `store.js` 的 `monthList`（经 `util` 暴露），`_shared.js` 转发、`Voucher.renderSum` 改调用、`ExpenseDetail.js` 的 2 处调用点改名；
- **📌 更正（Phase 1 的遗漏）：内联重复实现不止 3 份，实为 7 份 —— 本轮补齐**。上一轮称"三份实现已收敛"，其实只覆盖了**具名**的那三份。事后按**语义**重搜（搜代码特征 `while (y < ey || (y === ey && m <= em))` 配合 `list.push(y + '-' + …)`）又查出 **4 份匿名内联**：

  | 位置 | 用途 |
  |---|---|
  | `Voucher.js` 的 `queryVouchers(start,end,code)` | 查凭证逐月取数 |
  | `Home.js` 的 `monthsOf(p)` | 首页期间区间展开 |
  | `store.js` 的趋势方法（截断到 `lastVoucherMonth` 后再展开） | 折线图月份轴 |
  | `PeriodRangePicker.js` 的 `allAvailablePeriods()` fallback | 可选月份列表兜底 |

  四者现已全部改调 `monthList`，**全库只剩 `store.js` 的 `monthList` 一处实现**（`store.monthsBetween` 保留为"差月数"语义，两者不再重名冲突）；
- **教训（方法论，比修好本身更值得记）**：**收敛重复实现要按语义搜，不能按名字搜** —— 上一轮我 grep 的是 `monthsBetween`（**名字**），于是只找到 3 份具名拷贝，对 4 份**匿名内联**完全视而不见。正确做法是搜**代码特征**（循环结构 + 赋值模式）。这条已按上面的方式重做并补齐；
- **查出并修掉 2 处真实的契约违反**（由新加的规则④发现）：`__glJumpTo` 与 `__plJumpToRow` 都带着旧范围设计的第三个参数，首页「本年/去年」模式会传 `from~to`，使 Start=年初、End=年末 —— **两端真的不等**（总账/利润表只读 End，所以数字是对的，但 Start 是个假值，正是「改了没反应」那类坑的源头，且因函数名含 `jump` 被规则③豁免）。两处签名收敛为单参、两端写同一个值，`Home.js` 的两个调用点同步改为只传区间末月；
- **自检脚本新增规则④⑤**：④ **两端必须恒等** —— 扫描 `js/` 下所有给期间输入的赋值，把被赋值变量名归一成 `<SELF>` 后，要求 Start 侧与 End 侧的表达式集合完全相同；只改一端或给两端写不同的值即报错，并指出哪一侧有独有表达式及其位置。⑤ **不得重新引入双模开关** —— `index.html` 或组件里若再现 `data-single` 即报错，提示"要跨期请加粒度、不要把起止端点加回来"；
- **反向测试 6/6 通过**（证明规则不是空过）：删 `data-default` / 默认值非法 / 回调未注册 / 页面直接赋值 / 两端赋值不等 / 重新引入 `data-single`，六类违规全部被捕获且退出码为 1；测试后文件按原内容恢复，基线复跑仍通过；
- **`monthList` 等价性验证**：与**改动前的三份实现**逐例对比，9 个有效输入（单期 / 同年跨月 / 两位月 / 跨年 / 整年 / 逆序）结果**完全一致** —— 取数范围一个字未变；8 个非法/空输入统一返回 `[]` 且不抛错。其中 `2026-13` / `2026-00` / `0000-00` 属**刻意收紧**（旧三份实现会把这些值原样返回、等价于去查一个不存在的月份），真实调用方（`Voucher.renderSum` / `ExpenseDetail`）传的都是 `currentPeriod()` 或控件值，不涉及该边界；`_shared.js` 的兜底分支与 `store.monthList` 结果亦逐例一致；
- **回归**：契约自检通过；I1–I10 不变量 10/10 PASS；结转口径审计 4 账套全部通过；端到端快照全部通过；14 个改动文件语法检查通过；
- **顺带校准 `store.js` 头部索引注释**：「内部工具函数」子块的行号已校准到实际位置（原索引整块偏离约 80~100 行，如 `pad2 L54` 实为 L137），并补上 `monthList`。**该索引其余部分（S 方法区，如 `addLog L3715` 实为 L3817）仍然过时**，留待专门一轮：要么整体校准，要么改为只列名字不写行号（手工维护的行号必然随编辑漂移，留着就是"注释骗人"）。

### 云同步卡片注脚与上一行贴合（间距塌陷）
- **现象**：设置页「云同步」卡片中，「云备份：本机 → 云端；云同步：云端 → 本机……」这段注脚几乎贴着上方的「历史」行；
- **根因**：该段 `<p class="set-hint cs-footnote">` 里的 **`cs-footnote` 在 CSS 中从未定义**（全库无任何样式），只继承 `.set-hint` 的 `margin: 0 0 8px`（顶距 0，且被后续 `.set-hint { margin-top: 0 }` 再次归零）；而它紧跟在 `.set-row` 之后，`.set-row` 是 `padding: 12px 0 0`（下内距 0）——**两侧都是 0，间距完全塌陷**；
- **排查确认只此一处**：9 个独立段落式 `.set-hint` 中，5 个此前已用手写内联 `margin-top` 打补丁（10px / 6px）、2 个用显式 margin 处理，余下 1 个紧跟 `.card-head`（自带 `padding: 12px 0`）本身间距正常，故未误改；
- **修复**：为 `.cs-footnote` 补上正式定义 `margin: 12px 0 0`——与卡内行距（`.set-row` / `.card-head` 均为 12px）一致；下方不留边距，由 `.card` 的 16px 底部内距收尾。规则刻意置于 `.set-hint { margin-top: 0 }` 之后（两者特异度相同，靠后者生效），已用级联模拟核对生效者确为 `.cs-footnote`（12px）；
- 无需手工 bump 资源版本号（源文件为 `?v=dev` 占位符，打包时由 `build-dist.mjs` 注入内容指纹）；
- **记录一个可选的后续整治项（本轮未做）**：`.set-hint` 同时被当作「行内提示」和「独立段落」两种用途，而 `.set-hint { margin-top: 0 }` 这个全局覆盖把**独立段落**的顶距也一并归零，才导致下游要靠 5 处内联 `margin-top` 反补。若要根治，应把两种用途拆成两个类（行内 `.set-hint` / 独立段落 `.set-note`）并去掉内联补丁；因涉及多张设置卡片、需实际核对渲染效果，留待专门一轮处理。

### 期间能力后续决策记录（本轮未实施；目标形态按页面类型分，见下）
- **决策（就本轮排期作出的）**：跨期/季度口径**本轮不实施**，先固化单期契约；「查凭证」跨月检索亦暂不改。⚠️ 这是**排期**决定，**≠"金蝶不这么做"** —— 恰恰相反：金蝶的报表类页面就是月报/季报（见下方勘误）；
- **为什么不做的真正理由（不是省事）**：单期 + 现有「本年累计」列已覆盖绝大部分需求 —— 想看 1~3 月合计就选 3 月看累计列、想看全年就选 12 月看累计列、想看去年全年就选去年 12 月看累计列。**粒度能净增的只有"季度本期"这类中间区间**（现需两个累计数相减），代价却是"每页三种形态 + 逐页定档位 + 列头文案改造"一整套会反复讨论的面；
- **粒度与起止不是二选一（此框架由维护者对金蝶界面的确认修正）**：金蝶自己就**按页面类型分开用** —— 报表类（利润表等）用「粒度」（月报/季报 + 单月/单季），部分页面用起止区间。粒度对 16 个页面都成立（资产负债表在粒度下：年度→期初=年初、月度→期初=月初）；起止在**我们的**资产负债表列结构下则需改列语义（列头在 `年初余额`/`期初余额` 间切换），**不是"无解"**，但确实要额外改列义；
- **📌 勘误：推翻本条目早先的一句论断**。早先写的是「金蝶/jinbooks 的 `between` 只存在于后端枚举、前端字典只有月/季/年 —— 即**参考实现自己也不把起止暴露给用户**」。**这句是错的**，根源是把 **jinbooks（一个开源项目）当成了金蝶**：
  - **金蝶（商业软件）确实有完善的期间范围选择器**。本项目自己的历史注释就写着「仅 UI 对齐参考实现 range picker」（`Report.js` / `TrialBalance.js`）—— 即**当年对齐的就是金蝶那套范围选择器**；
  - ⚠️ **但金蝶并非所有页面都用起止**（据维护者对金蝶实际界面的确认）：**报表类页面（如利润表）用的是"粒度"** —— 可切换**月报 / 季报**，再选**单月 / 单季**查询；**起止区间只用于部分页面**。所以金蝶的做法既不是"全站起止"，也不是"全站粒度"，而是**按页面类型各用其合适的形态**；
  - `between` 只存在于后端枚举、前端字典不含它 —— **那只说明 jinbooks 这个开源项目**，不能用来推断金蝶的 UI 形态；
  - 而且**本仓库曾经完整实现过**这套范围选择器：`git show '2adc17b^:js/components/PeriodRangePicker.js'`（**428 行**），含双面板（`data-side="start"/"end"` 各带年份导航）、**互斥校正**（选 start 晚于 end 就推 end、选 end 早于 start 就拉 start，从而保证 `start <= end`）、按账套真实可用月份置灰、今年/去年快捷按钮，以及由 `data-single-period` 声明的**单期/范围双模式**。其注释明确写了范围模式的归属：**「未加该属性的容器仍是真范围模式（如查凭证、凭证汇总表、费用明细表）」**；
  - **所以"范围选择器怎么做"我们并非不知道 —— 是做过、又被 `2adc17b` 的 popover 重构砍掉了。** 现状：组件逻辑与 CSS（`ty-period-panel` / `data-side` / `data-shortcut` 现已 0 处）均已移除，pop DOM 退回单面板；但 3 个范围页的取数层仍完整支持区间。恢复属「从 git 取回 + 接线 + 回归」，不是从零设计；
  - 由此，"暂不做"的理由**收敛为纯粹的范围/排期决定**，其中"收益窄"这一条**已被维护者指正并予撤回**：季度口径是**常规的报送与管理需求**（如所得税按季预缴、季度财务报表），不是早先所称"几乎用不到的自由度"；现只保留"本轮未排期"这一事实性表述。**不再引用被推翻的论据**（既不用"参考实现不做起止"，也不用"收益窄"）；
  - ⚠️ **信息边界声明**：本机没有金蝶可查，**本条中涉及金蝶的表述原为二手信息**（维护者转述或本项目历史注释）—— 现**金蝶界面形态由维护者提供的截图作为直接证据**（见下方「金蝶期间控件形态」节）并已逐页验证 16 项；其余仍以金蝶实际界面为准。若将来落地方案，`2adc17b^` 的实现可直接作为**范围模式**的设计基线；
    - 补充（2026-09-14）：维护者说明该文件夹**名为「金蝶源码」、已被删除**。已三重搜索（① 按目录名含「金蝶/kingdee/kis/源码/截图」→ 全盘深 8；② 按图片密度「单目录 ≥12 张」→ 全盘深 6；③ 按递归图片数 → Documents/Desktop/财务软件/坚果云/Downloads/Pictures），**均无匹配**；`Documents/jinbooks*/images` 是 **jinbooks 自己的 README 截图**（非金蝶）；已挂载卷只有内置盘；
    - **恢复途径已逐项排查，全部不可用**：废纸篓（空）、Time Machine（**无本地快照、无备份目标**）、其它卷废纸篓（无）、坚果云「我的坚果云」（只有教程 PDF 与 `添钰财务同步`）、iCloud Drive（仅个人文件）→ **该截图集已不可恢复**。界面规格只能靠维护者**现提供截图**；**不必再搜**；
    - 本机可用的**金蝶数据层**对照资产（非 UI 规格）：`~/Desktop/金蝶账套 ais/*.ais`（添钰来客 2025/2026、绅蓝之星 2024/2025/2026）配合 `tools/read_ais.js` 可提取金蝶原始数据（如 FDC 方向）作口径对照 —— 但**不含界面形态信息**；
- **判据（比"按页面类型"更本质，本轮新得）**：决定一个页面该用哪种形态的，是**它的列结构是否随区间月数变化** ——

  | 列结构 | 该用什么 | 例 |
  |---|---|---|
  | **列随区间月数变**（一个月一列） | **必须支持区间**（区间就是列结构） | `ed` 费用明细表 |
  | 列固定，且"本期"是**一段区间**（流量） | **粒度**（月 vs 季 = 1 个月 vs 3 个月，真正不同） | `pl` 利润表、`cf` 现金流量表、`tx` |
  | 列固定，且"期末"是**一个时点**（存量） | **单期**（粒度冗余 —— 见下） | `bs` 资产负债表 |
  | 列固定、**检索 / 汇总** | 单期即够，跨期属便利 | `q` 查凭证、`sum` 凭证汇总表 |
  | 列固定、**账簿口径**（期初/本期/本年累计/期末） | 粒度或起止**都成立**（区间列与时点列并存，粒度使本期变长、期初/期末随之锚到期末月） | `tb`/`gl`/`dl`/`ml` |

- **为什么报表类用粒度而不是起止**（即"金蝶既有起止、利润表为何偏用粒度"的解答）：**报表的列已经用掉了"区间"这个自由度** ——「本年累计」列本身就是"年初~期末"这个区间。若再让用户选起止，等于**一个自由度要同时表达"本期有多长"和"累计从哪起"**，二者冲突：

  | 用户选 | 本期列 | 本年累计列 | 后果 |
  |---|---|---|---|
  | A=1月, B=1月 | 1月 | 1月 | **两列同值**（冗余） |
  | A=1月, B=6月 | 1~6月 | 1~6月 | **两列同值** |
  | A=2月, B=5月 | 2~5月 | 1~5月 | 本期 = **非会计期间** → 不可报送、逐期不可比 |
  | A=4月, B=6月 | 4~6月 | 1~6月 | 看似合理，但"本期"已变成"从 4 月起"，名实不符 |

  而**粒度只改「本期」这一列有多长**，「本年累计」恒为"年初~期末"，**两列关系恒定**（累计 = 本期 + 之前各期），且"本期"**恒为一个完整会计期间**（月/季）—— 可报送、可比、可复现；
- **📌 更正：时点报表该用单期（推翻本条早先"报表类无论流量还是时点都只能走粒度"的结论）** —— 由维护者指出"**金蝶的资产负债表只有单期间选择**"后核对确认：
  - **时点报表给起止**：起点**完全无用**（正是本项目最初那个 bug 的现场）；
  - **时点报表给粒度**：**同样无用、且是冗余** —— 资产负债表的两列「期末余额 / 年初余额」都是**时点**，"季末"本身就是某个月末，故**选「2026年第3季度」与选「2026-09」指向同一个时点**，粒度不改变任何数；
  - 实测佐证：**jinbooks 给资产负债表也上了粒度单选组**（取 `period_type` 字典 = 月/季/年），但其两列 `currentBalance` / `initialBalance` **均不随粒度变**（无 `periodType` 条件标签）→ 可验证的冗余。**金蝶只给单期间，是更简洁也更正确的做法**；
  - 结论修正：**时点报表两种形态都不需要，就该用单期** —— 这也是本项目 `bs` 现状（单期）即终局的依据；
- **目标形态（据金蝶做法整理，本轮未实施）**：**按上表判据分**，而非全站统一 ——
  - **流量报表类**（`pl` 利润表、`cf` 现金流量表、`tx`）→ **粒度**：月报/季报切换 + 单月/单季，对齐金蝶利润表。这正好落在我们现有的两列结构上：**粒度只改「本期」列的含义（本月数 ↔ 本期数），「本年累计」列不动** —— 是"换「本期」列的取数口径 + 换列头文案"，不是重做列结构；
  - **时点报表**（`bs` 资产负债表）→ **单期**（**现状即终局，不用动**）—— 粒度冗余、起止起点无用；
  - **真区间类**（`ed` 费用明细表：列即区间）→ **起止区间**（基线 `2adc17b^`），取数层已完整支持；
  - **账簿类**（`tb`/`gl`/`dl`/`ml`）→ 待金蝶确认（粒度与起止都成立）；
  - **检索 / 汇总类**（`q` 查凭证、`sum` 凭证汇总表）→ 单期即够，跨期属便利；
  - **其余**（`das`/`dad`/`acl`/`sal`/`sst`）→ 单期；
- **实施成本模型与分阶段（供决策，非承诺）**：
  - ⚠️ **澄清：形态数看起来比实际多**。**「月报」与「季报」不是两种形态** —— 是同一个粒度控件的**两个取值**（截图证明：同一个框，只有文案与浮层内容变）。故全站形态实为 **2 个**：**单期**（现有，是 13/16 个页面的终局）+ **粒度**（新增，含月报/季报）；**「起止」是第 3 个独立形态**（可选，`2adc17b^` 有实现）；
  - **成本随"形态数"增长，几乎不随"页面数"增长**：同一形态套到第 2 个页面只是多一行声明 + 页面读一下 —— 因为取数都是 `monthList` + `Σ cur`（已就位且有等价性测试兜底）。所以"只给 `pl` 做"与"4 个报表页都做"成本相差不大；**贵的是做出那一个形态，不是铺开**；
  - **阶段 1（只 `pl` 加月报/季报）估算**：组件 ~80–120 行（粒度下拉 / `renderGrid` 的 4 格分支 / 季度格式化 / 触发器两段）+ `Report.js` ~20–30 行（取数封装 ~10 行 + 读粒度 + 列头）+ 契约校验 ~20 行 + CSS ~3–10 行 + `index.html` 1 行；`setRptHead` **已有 `periodText` 参数（第 5 个），签名无需改**；**不碰 `store.js` 核心计算**。约**半天到一天**；
  - **可永久停在任意阶段**：只 `pl` 有粒度、其余仍单期，**不构成不一致** —— 金蝶自身各页档位就不同（利润表 月/季；现金流量表 月度\|季度、**年度被注释掉**；科目余额表 月度\|年度、**季度被注释掉**）；
  - **真正的风险**：每加一个形态，回归组合数上升（形态 × 页面 × 边界）。故**形态数是成本主因** —— 这也是"先不加起止形态"的理由；
- **由此显现的一处与金蝶的真实差距（优先级判断）**：我们的**利润表没有「月报 / 季报」切换**，金蝶有；而季度口径属常规报送需求，故这一项**应优先于"账簿类恢复起止"**来补。它也不推翻现契约 —— 粒度是把「月」这个粒度值换掉，取数层用 `monthList` 展开即可，属**增量加一维**；
- **为什么粒度不推翻"只产出一个报告期"的契约**：粒度下控件的产出仍是**一个报告期末**（`reportDate` 仍是一个月份），只是多了一个"这个报告期有多长"的维度。原契约的两条不变量（两端恒等、页面不得直接赋值）继续成立；
- **三个页面的当前状态**：`查凭证` / `凭证汇总表` 的取数层**仍完整支持区间**（`renderQuery(start,end)` / `renderSum(start,end)`）；UI 产生不出 `start ≠ end`，属**能力休眠、未删除**，无报错也无坏数据。若将来恢复，只需加能产生不同起止值的 UI，取数层一行不用改；
- **📌 更正（推翻本条早先对费用明细表的判断）**：早先写的是「`费用明细表` 与报表同构（本期 + 本年累计两列）本就该是单期」—— **错**。实测 `ExpenseDetail.js:433-440`：ED 的列是**区间内一个月一列**（`months.forEach(m => colHtml += '<th>' + monthLabel(m) + '</th>')`）+ 可选「`YYYY年合计`」+ 可选「较上期/较同期」；表头是动态渲染的（`<thead id="edGridHead">` 为空），标题为 `start 至 end`。**即区间就是 ED 的列结构本身**，所以它是**真正必须支持区间**的页面。这也解释了为何原设计里**只有 `ed` 不带 `data-single`** —— 原作者在这点上自洽；
- **附带发现：原设计有一处注释与实现不一致**：组件注释称范围页是「查凭证、凭证汇总表、费用明细表」3 个，但 `2adc17b^` 的 HTML 里**只有 `ed` 不带 `data-single`**，`sum` / `q` 都带着（即当时已声明为单期）。按下方判据，`sum` / `q` 的列结构不随区间月数变，声明单期是**对的**，过时的是那句注释；

### 期间范围选择器的实现沿革与基线（参考记录，非代码改动）
- **为什么记这一条**：本轮曾据一句错误论断（"参考实现不把起止暴露给用户"）把 `data-single` 当死属性删掉，事后由 git 历史推翻（见上方勘误）。为免日后再次从头考古，这里把三份实现的关系与"该以谁为基线"一次记清；
- **沿革**：

  | # | 位置 | 行数 | 形态 |
  |---|---|---|---|
  | ① 原型 | `/Users/chen/财务软件/xzys-cloud/js/components/PeriodRangePicker.js`（**另一项目，不属本仓库**） | 342 | 纯范围模式，无单期开关 |
  | ② 增强版 | 本仓库 `git show '2adc17b^:js/components/PeriodRangePicker.js'` | **428** | ① + 真禁用不可选格 + 账套真实月份 + `data-single-period` 单期/范围双模式 |
  | ③ 现状 | 本仓库 `js/components/PeriodRangePicker.js` | 290 | 单期 popover（即 ② 的单期模式）；双模式与范围 UI 已移除 |

- **① 的价值：它是"确实对齐过金蝶"的直接证据。** 其首行注释即写明「**金蝶风格起止期间选择器（复刻总账/明细账筛选栏）**」，且依赖 `__KINGDEE_HELPERS__`（去金蝶化之前的命名）；
- **② 是三份中最完善的，将来若要恢复范围模式应以它为基线**（不要从 ① 抄）。② 相对 ① 的两处实质改进：**真禁用不可选格**、**可用月份取自账套真实数据**（`store.allMonths()`：启用月 → 有凭证的最后一个月；而 ① 用 `periodRangeOptions()` 生成的**硬编码两年窗口**「上年 1 月 ~ 当年 12 月」，与账套实际进度无关）；
- **① 的两处缺陷（作为参考时的坑，勿照抄）**：
  ```js
  if (ym > maxP) cell.classList.add('disabled');               // 只加样式
  cell.addEventListener('click', () => onCellClick(side, ym)); // 无条件绑事件 → 灰格仍可点且生效
  ```
  - 一是**假禁用**：未设 `cell.disabled = true`、click 也照绑，用户看到灰色却选得中（比"没禁用"更糟，会被当成软件故障）；
  - 二是 **`maxP` 本身就算错了**：`maxAvailablePeriod()` 取硬编码列表末位 = 当年 12 月，与账套做到哪个月无关 → "哪些该灰"从一开始就没对；
  - ② 的正确写法：`availableSet`（账套真实月份）→ 不在集合内则 `cell.disabled = true` **且只有可选项才绑 click**（`else` 分支是关键）；
- **①② 共有、尚未做到的能力（"对齐金蝶"的候选待办）**：① **跨年区间不顺手** —— 左右面板年份互相独立，选 `2025-11 ~ 2026-02` 需在两列各翻一次年，没有「选完 start 自动把 end 面板跳到同期」这类联动；② **「今年」会被 clamp 到 `maxP`**（`e = 当年-12` 之后再夹到最后一月），账套只做到 8 月时即得 `01~08` —— 这可能正是期望行为，也可能不符金蝶，**需以金蝶实际界面确认**；③ **无季度/半年档**（金蝶是否有，现有资料无法判定，不臆测）；
- **若将来恢复范围模式，取回路径**：组件逻辑 `git show '2adc17b^:js/components/PeriodRangePicker.js'`；pop DOM `git show '2adc17b^:index.html'`（双面板 `data-side`、6 个快捷按钮「本期/上期/本月/上月/今年/去年」、遮罩 `data-close`、「查询」确认按钮 `data-act="periodRangeConfirm"`）；另需重建已删除的 CSS（`ty-period-panel` / `data-side` / `data-shortcut`，现均 0 处）。属「取回 + 接线 + 回归」，非从零设计；
- **本记录未改动任何代码**；① 位于另一项目目录，仅作参考，未对其做任何改动。

### 金蝶截图逐页比对（规格级 diff；截图位于 `金蝶截图/`）
- **来源**：维护者 2026-09-14 现截 10 张放入 `金蝶截图/`（标准现金流量表 / 查凭证 / 多栏账 / 费用明细表 / 明细帐 / 凭证汇总表 / 折旧汇总表 / 折旧明细表 / 主要应交税金明细表 / 总账）；
- **比对方**：截图（金蝶规格）× `renderXxx` 源码（我方实现）。**方法上必须"读渲染代码"，不能只看 HTML 列头**（见下方自我更正）；
- **折旧汇总表**（截图规格）：
  - 触发器 `2026年07期 ~ 2026年08期` → **期间起止** ✓（与验证结论一致）；
  - 列：`类别 | 原值 | 期初累计折旧 | 【本期折旧额：2026年7月 | 2026年8月】| 本年折旧额 | 期末累计折旧 | 期末减值准备`；
  - **「本期折旧额」是父表头**，下辖 **每期一列**，子列头格式为 **`2026年7月`**（年 + 月，**不带"期"字、不补零**）；
  - **口径自洽验证**（器具、工具、家具 行）：期初累计 56,424.59 + 本期 5,945.66 + 5,945.66 = 期末累计 **68,315.91** ✓ 即「期末 = 期初 + 区间各期折旧」；而「本年折旧额」47,228.02 是**年初~期末**的累计（≠ 区间累计）→ **「期初累计」锚区间首月月初、「本年折旧额」锚年初**，与科目余额表的四列口径同构；
  - **与我方 `das` 差异**：① 查询端我方**单期**、金蝶期间起止（→ 需"每期一列"）；② 列头文案：我方 `dasMonthTh` 写 **`2026-08折旧`**（`YYYY-MM` + "折旧"后缀，挂在单列上），金蝶是**父表头「本期折旧额」+ 子列 `2026年7月`**；③ 我方多出「编码 / 名称 / 部门 / 期末净值」四列，金蝶只有「类别」一列（+ 可选「按部门汇总」）——**属布局取舍，非缺陷**；
- **总账**（截图规格）：
  - 触发器 `2026年04期 ~ 2026年08期` → **期间起止** ✓；
  - 行结构：**一科目一段，段内按期 3 行**（`期初余额` / `本期合计` / `本年累计`），编码与名称列 `rowspan` 纵向合并；**只有区间首月有「期初余额」行**（5/6/7/8 月无）→ 再次印证「期初 = 区间首月月初」；
  - 列：`科目编码 | 科目名称 | 期间 | 摘要 | 借方 | 贷方 | 方向 | 余额`（「摘要」列放的是「期初余额/本期合计/本年累计」这类**段标签**）；
  - **与我方 `gl` 比对结果：结构一致** ✓ 我方 `renderGl` 同样是 **3 行/科目**（`gl-subject` 含 `rowspan="3"` 的编码/名称 + `期初余额`，随后 `本期合计`、`本年累计`），列头与段标签命名也与金蝶一致；
  - **真实差异**：① 查询端我方**单期** vs 金蝶**期间起止**（→ 需按期循环渲染多段）；② **期间列格式**：金蝶 `202604`（**`YYYYMM`，无连字符**），我方 `<td class="gl-period">' + escHtml(month) + '</td>` 输出 **`2026-08`**（带连字符）；
- **📌 自我更正（同类错误的第三次）**：我一度据**列头**「…|期间|摘要|借|贷|方向|余额」推断"我方总账是**逐笔分录**、与金蝶的按期汇总**结构完全不同**" —— **错**。读了 `renderGl` 才发现我方同样按期汇总 3 行、同样 `rowspan` 合并。**同类前两次**：把"无数据"读成"无此表"、只看列头骨架就断言"显示层完全一致"。**共同错误：用部分证据（列头/一处观察）替代实际实现（渲染代码/截图）。规矩：涉及我方行为的结论，必须先读渲染代码，不得从列头或命名推断**；
- **📌 附带纠正：五个"辅助页"其实与账务同模块**。总账截图顶部有一排页签，**同屏包含**：`总账 | 明细账 | 多栏账 | 凭证汇总表 | 查凭证 | 标准现金流量表 | 费用明细表 | 主要应交税金明细表 | 折旧汇总表 | 折旧明细表 | 资产变动记录 | 工资表 | 工资统计` —— 即**折旧/资产/工资页与账务页在同一模块、同一排页签内**。此前我以"它们属固定资产/工资模块，期间控件很可能不同套"为由**调低了预测可靠度**（并据此押错 `das`/`dad`/`sal`）—— **这个假设是错的**。❌ 的真正原因不是"模块不同"，而是我改用业务直觉替代了列结构判据；
- **凭证汇总表**（截图规格）：触发器为「**汇总日期** `2026-08-01 — 2026-08-31`」→ **起止日期** ✓（与验证一致，单位确为**日期**）；列 `科目编码 | 科目名称 | 借方金额 | 贷方金额` —— **与我方一致** ✓；副标题有 `凭证总张数：53张；附件总张数：0张` → **我方 `sum` 无此统计**（`Voucher.js` 只有单张凭证的附件张数，无汇总表级统计）→ **差异**；
- **主要应交税金明细表**（截图规格）：触发器为「**期间** `2026年08期`」**单期**；展开面板为 `« 2026年 »` 年份导航 + **12 格**，格子文案是「**1月** … **12月**」（用「**月**」而非「期」—— 与我方月格「N期」不同）；列 `行次 | 本月数 | 本年累计数` + 税项行标签（一、增值税 → 1、应交… → (1)(2)…）—— **与我方 `tx` 列一致** ✓；截图左侧另有相关按钮（维护者所述"**单期 + 年度**"的年度档可能在此，未展开）；
- **标准现金流量表**（截图规格）：触发器 `月报 ▾ | 2026年08期` → **粒度（月报）**，**与利润表完全同构** ✓ 验证了"流量报表 → 粒度"这条判据；列 `项目 | 行次 | 本月金额 | 本年累计金额` —— **与我方 `cf` 一致** ✓；
- **费用明细表**（截图规格）：触发器 `期间 2026年06期 ~ 2026年08期` → **期间起止** ✓（"存疑"确证为**期间**、非日期）；列 `操作 | 编码 | 名称 | 2026年6期 | 2026年7期 | 2026年8期 | 2026年合计` → **每期一列** ✓；筛选 `科目`（支持 `1001,1009;2121-2131` 多编码/范围）、`显示科目级次 至最末级`、`显示辅助核算`；选项 `年度合计 / 显示同环比 / 展开所有级次 / 明细科目显示全称 / 显示发生额为0的科目`；
  - **与我方 `ed` 比对**：① 列头格式 —— 金蝶 `2026年6期`（**不补零**），我方 `monthLabel` = `${y}年${parseInt(mm,10)}期` → **也是 `2026年6期`，格式一致** ✓；② `2026年合计` 列 —— 我方 `${年}年合计` ✓ 一致；③ **查询端我方单期 vs 金蝶期间起止** → **差异**；
- **查凭证**（截图规格）：筛选为「**凭证期间** `2026年07期 ~ 2026年08期`」起止（维护者另述还有一个「**凭证日期**」起止，截图中未展开，可能在 `过滤` / `更多` 内）；列 `操作 | 日期 | 凭证字号 | 摘要 | 科目 | 借方金额 | 贷方金额 | 附件 | 原单编号 | 制单人`；日期到天（`2026-07-01`）；一张凭证一块（凭证字号跨行合并）、分录用「编码 + 名称」；底部有 `合计` 行；
  - **与我方 `q` 比对**：我方列 = `[复选] | 操作 | 日期 | 凭证字号 | 摘要 | 科目 | 借方金额 | 贷方金额 | 附件 | 原单据编号 | 制单人 | 审核人` → **基本一致** ✓；差异：我方多「复选」「审核人」两列（金蝶截图未显示，可能被截断）；列名金蝶「原单**编号**」vs 我方「原单**据**编号」（一字之差）；**查询端我方只有"期间"单期，缺「凭证期间起止」与「凭证日期起止」**（金蝶为两个起止控件）→ **差异**；
- **📌 关键确认：13 个页签同屏**（总账 / 凭证汇总表两张截图顶部均完整可见）：`总账 | 明细账 | 多栏账 | 凭证汇总表 | 查凭证 | 标准现金流量表 | 费用明细表 | 主要应交税金明细表 | 折旧汇总表 | 折旧明细表 | 资产变动记录 | 工资表 | 工资统计` —— **正好是我方那 13 个"期间页"**，证明**页面划分与金蝶一一对应**（加上资产负债表/利润表/科目余额表即我方 16 个），且**折旧/资产/工资与账务同模块、同排页签**；
- **明细账**（截图规格）：触发器 `2026年第8期 至 2026年第8期` → **期间起止** ✓（与验证一致）；
  - ⚠️ **格式意外**：此处用「**2026年第8期**」（**带"第"字、不补零**）+ **「至」**连接；而同批截图里 `利润表/现金流量表` 用 `2026年08期`（补零、无"第"）、`费用明细表` 用 `2026年6期`（不补零、无"第"）、`折旧汇总表` 用 `2026年07期 ~ …`（补零、`~`）、`凭证汇总表` 用 `2026-08-01 — 2026-08-31`（日期、em dash）→ **金蝶自身各页格式并不统一**（我方 `fmtPeriod` 统一为 `2026年08期`，只与报表类对得上）；
  - 列：`日期 | 凭证字号 | 摘要 | 借方 | 贷方 | 方向 | 余额`（与我方 `dl` 列名一致，但**「方向」「余额」两列顺序相反**：我方 `余额 → 方向`，金蝶 `方向 → 余额`）；
  - **行结构：逐笔分录行 + 每期 3 行汇总**（`本期合计` / `本年累计`，**仅区间首期另有 `期初余额` 行**）；**汇总行的日期取该期期末日**（如 `2026-06-30`），**期初行日期取区间首月 1 号**（`2026-06-01`）；
  - 其余：右侧科目树「快速切换」、底部分页（`1 共 1 页` / 每页 `500`）、`连续打印` / `导出当前` / `导出全部`；
- **折旧明细表**（截图规格）：触发器 `2026年07期 ~ 2026年08期` → **期间起止** ✓；选项 `按部门汇总` / `显示已清理资产` / `显示变动信息`；列 `类别 | 编码 | 名称 | 部门 | 原值 | 期初累计折旧 | 【本期折旧额：2026年7月 | 2026年8月】| 本年折旧额 | 期末累计折旧 | 期末减值准备`；**按类别分组，组内明细行 + 组末「小计」+ 表末「合计」**；
- **📌 由此发现的实质差异：我方 `das`（折旧汇总表）与 `dad`（折旧明细表）的列完全相同**：
  | | 编码 / 名称 / 部门 | 小计 / 合计 | 期末净值 |
  |---|---|---|---|
  | 金蝶 **汇总表** | **✗ 无**（只有「类别」一列） | ✗ | ✗ |
  | 金蝶 **明细表** | ✓ 有 | ✓ 有 | ✗ |
  | 我方 `das` | ✓ 有 | 待核 | **✓ 有（金蝶无此列）** |
  | 我方 `dad` | ✓ 有 | 待核 | **✓ 有（金蝶无此列）** |
  → 即金蝶的**汇总表按「类别」汇总、不含编码/名称/部门**，而**我方 `das` 也带这三列**（做成了明细的样子）；两张表列完全一致，**未体现"汇总 vs 明细"的分工**。另我方两表都多「期末净值」列（金蝶只有 期末累计折旧 + 期末减值准备）；
- **未读**：`多栏账` 一张（此前维护者已单独发过同页截图并完成 diff，见上文 5 条差异）；

### 金蝶期间控件形态（维护者提供的界面截图 —— 直接证据）
- **意义**：这是本项目第一次拿到金蝶界面的**直接证据**（此前涉金蝶的判断都是二手转述或从 jinbooks 推断，也正因如此出过两次错）；
- **截图可见（月报态）**：

  ```
  ┌──────────────────────────────┐
  │  月报  ▾  │  2026年08期        │      ← 粒度与报告期在同一个控件框内，竖线分隔
  └──────────────────────────────┘
       ↓ 展开
     ┌────────┐
     │  月报   │   ← 当前选中（高亮）
     ├────────┤
     │  季报   │
     └────────┘
  ```

  - 粒度与报告期**同处一个控件框**，用**竖线**分隔 —— 即 `[月报 ▾] | [2026年08期]`；
  - 左侧是**粒度下拉菜单**，展开项为「**月报**」「**季报**」（选中项高亮）。注意：**是下拉，不是单选按钮组** —— jinbooks 用的是 radio group（月度\|季度\|年度），金蝶是下拉，**两者形态不同**，不可互推；
  - 右侧报告期文案为「**2026年08期**」；
- **截图可见（季报态 —— 后补充）**：

  ```
  ┌──────────────────────────────────┐
  │  季报  ▾  │  2025年第3季度   📅   │     ← 报告期文案随粒度变
  └──────────────────────────────────┘
       ↓ 展开（点报告期区域）
     ┌──────────────────────┐
     │  «     2025年    »   │   ← 年份导航（双箭头）
     ├──────────────────────┤
     │  Q1   Q2   Q3   Q4   │   ← 一行 4 格（不是 12 格网格）
     └──────────────────────┘
  ```

  - **季报态报告期文案 = `YYYY年第N季度`**（如「2025年第3季度」）—— **不是**先前猜测的「2025年02季」，故需新增季度格式化（我方 `fmtPeriod` 目前只处理月）；
  - **季度选择 UI = 年份导航 + 一行 4 格 `Q1`~`Q4`**（与月报的 12 格网格占同一浮层位置，只把 12 格换成 4 格）；年份导航为**双箭头** `«` `»`，我方现为单箭头 `‹` `›`（小样式差异）；
  - **一个控件框内有两个可点区域、各开各的浮层**：左侧粒度 → 下拉（月报/季报）；右侧报告期 → 月格 / 季度格。图标亦随之不同：粒度侧 `▾`，报告期侧**日历图标** `📅`；
  - 截图右侧的「⟳ 刷新」是**页面级操作**，不属本控件；
- **对本方的直接含义（三点）**：
  1. **月报态文案格式无需改动** —— 金蝶「2026年08期」与我方 `fmtPeriod()` 输出**完全一致**（含期数补零），Phase 1 刚统一过的口径正好对上；**季报态需新增** `YYYY年第N季度` 的格式化（`fmtPeriod` 目前只处理月）；
  2. **改动属"替换 + 加一支"而非"重构"** —— 我方触发器当前是 `[期间] [2026年08期]`（`ty-period-trigger-label` 标签 + 文本），金蝶是 `[月报 ▾] │ [2026年08期 📅]`：**左侧纯文本标签换成粒度下拉**，右侧报告期部分**原样保留**（文案与浮层定位均复用）；控件骨架（触发器框、grid、`data-on-change` 回调、浮层定位）都不用动；**新增的只有**"渲染 4 格 Q1~Q4"这一支分支 —— 且**连 CSS 都不用加**：我方 `.ty-period-grid` 本就是 `grid-template-columns: repeat(4, auto)`（12 格 = 3 行 × 4），故 4 格自然排成一行；
  3. **数据模型上契约不受影响** —— 报告期仍落为一个 `YYYY-MM`：**季报写该季末月**（Q3 → `2025-09`）。这样页面侧读 `End` 的既有逻辑一行不改、规则④（两端恒等）继续成立；粒度作为**独立的一维**写在控件上（如 `data-granularity`），由页面读取后决定取数口径与列头文案；
- **金蝶的粒度模型 = jinbooks 的 `periodType`（同一模型、两种形态）** —— 由维护者指出后核对确认。**此前把"金蝶粒度"与"jinbooks 粒度"并列讨论是多余的**：
  - **相同（模型层）**：都是「**粒度**（决定"本期"跨几个月）+ **报告期**」；区间由粒度派生，用户从不手选起止；
  - **不同（形态层）**：

    | | jinbooks | 金蝶 |
    |---|---|---|
    | 粒度控件 | **单选按钮组**（月度\|季度\|年度） | **下拉**（截图只见月报/季报两档） |
    | 粒度与报告期 | **分处两个控件**，报告期控件按粒度 `v-if` 切换 | **同处一个控件框**，竖线分隔 |
    | 档位 | 三档（月/季/年） | 截图两档（月报/季报），是否还有年报未知 |

  - **结论**：**这不是两套方案，是同一套模型的两种皮**。我方按**金蝶的形态**实现即可，模型层无需另立新说；
  - 附证：jinbooks 也有同类"列头随粒度变"的做法（科目余额表 `年初余额` ↔ `期初余额`），与金蝶「本月 ↔ 本期」属**同一模式**（列头文案随粒度切换）；
- **列头行为（已确认，维护者）**：**月报显示「本月」、季报显示「本期」**，「本年累计金额」列保持不变。故列头**要随粒度动态渲染**，且季报用「**本期金额**」—— 修正了先前草案里「本季金额」的假设；我方现有列头「本月金额」与金蝶月报态已一致，无需改；
- **全表预测与验证结果**：

  | # | 页面（我方） | 我的预测 | 实际（金蝶） | 判 |
  |---|---|---|---|---|
  | 1 | 科目余额表 `tb` | 起止（期初锚点随区间前移） | 起止 | ✅ |
  | 2 | 资产负债表 `bs` | 单期 | 单期 | ✅ |
  | 3 | 利润表 `pl` | 粒度（月报/季报） | 月报/季报 | ✅ |
  | 4 | 总账 `gl` | 期间 / 起止 / 单控件 | **只有期间起止**（无日期控件）；改起点→行数变、期间列出更多期 | ✅ |
  | 5 | 明细账 `dl` | 期间 / 起止 / 单控件 | **同总账** | ✅ |
  | 6 | 多栏账 `ml` | 期间 / 起止 | **期间起止**（查询）；**显示按日期**（1 号~31 号逐笔） | ✅ |
  | 7 | 凭证汇总表 `sum` | 起止（**未及单位**） | **起止「日期」**，不是起止期间 —— 是**汇总日期** | ✅ 形态对 / ⚠️ 单位未预测到 |
  | 8 | 查凭证 `q` | 起止（单位：日期） | **凭证「期间」起止 + 凭证「日期」起止 —— 两个控件** | ⚠️ 形态对 / 单位不全 |
  | 9 | 现金流量表 `cf` | 期间 / 粒度（月报/季报）/ 单控件 | **同利润表**（粒度） | ✅ |
  | 10 | 费用明细表 `ed` | 期间（存疑）/ 起止 | **期间** —— 存疑已解（不是日期）；形态未单独提及，按"未提异议"记为一致 | ✅ |
  | 11 | 应交税金表 `tx` | 单期 | **单期 + 年度** | ⚠️ 半对 |
  | 12 | 折旧汇总表 `das` | 单期 | **期间起止**（「本期折旧额」下每期一列） | ❌ |
  | 13 | 折旧明细表 `dad` | 单期 | **起止**（同折旧汇总表） | ❌ |
  | 14 | 资产变动记录 `acl` | 起止 | **起止**（同折旧汇总表） | ✅ |
  | 15 | 工资表 `sal` | 单期 | **月份起止** | ❌ |
  | 16 | 工资统计 `sst` | 起止 | **单月** | ❌ |

  **最终得分（16 项全部验完）：9 ✅ / 3 ⚠️ / 4 ❌**；其中 3 个 ⚠️ **全部**源于我漏了"单位 / 档位 / 控件个数"这些外围维度，而非形态判错。
  - **📌 另一处判断失误（我自己的，非判据问题）**：维护者报 `ml` "无数据"，我**直接读成了"金蝶无此表"**并写进记录 —— 实际金蝶**有**多栏账，只是当时那个账套没有数据。**这又是一次"超出证据去推断"**（与拿 jinbooks 推金蝶、把"range picker 对齐"读成"口径仍单期"同类）。**报什么就记什么，不要替对方补结论**——`ml` 事后已在另一账套上验明为**期间起止**（预测对）；
- **📌 新维度：查询单位 ≠ 显示单位**（由 `ml` 暴露）—— 金蝶**多栏账**的**查询**用「**期间起止**」，但**数据行**是按**日期**逐笔显示（1 号到 31 号）。这两件事是分开的：

  | 层 | 多栏账 | 总账 | 明细账 |
  |---|---|---|---|
  | **查询 / 口径单位** | 期间 | 期间 | 期间 |
  | **数据显示单位** | **日期**（逐笔流水） | 期间（期间是列） | 日期（逐笔流水） |

  - 故第〇层的"单位"须再分：**口径单位**（用户选的）与**显示单位**（数据排列方式）。前者由第 1–4 层判据定，后者由**该表的数据形态**定（账簿多为按日期流水，报表为固定列）；
  - **我方 `ml` 现状（意外的好消息）**：**显示层已与金蝶一致** —— 列头即 `日期 | 凭证字号 | 摘要 | 借 | 贷 | 方向 | 余额` + 分栏列（下级科目），数据取自 `S.detailLedger(code, month)`（按日期逐笔）。**差别只在查询端**：我方 `renderMl(code, month)` 收的是**单期**，金蝶是**期间起止**。即补齐成本 = 查询端换成期间起止 + 取数改为跨期循环，**显示层不用动**；
  - **📌 更正：上一条"显示层不用动"说过头了 —— 由维护者提供的金蝶多栏账截图推翻**。截图当规格逐项 diff 后，**显示层至少还有 3 处不一致**：

    | # | 项 | 金蝶（截图） | 我方 `renderMl` | 性质 |
    |---|---|---|---|---|
    | ① | 期初行**日期** | **`2026-07-01`**（区间首月 1 号） | **空** | 小（但语义上标明期初锚点） |
    | ② | 期初行**分栏列** | **各下级科目的期初余额有值**（182,095.62 / 0.05 / 282,185.40 / 202,375.98） | **全空**（`cols.forEach(() => initCells += '<td></td>')`） | **实质**：数据有、我方没显示 |
    | ③ | 分栏**表头** | **两行**：父表头「**借方**」+ 各列「`100201 基本户-青岛银行`」（**编码 + 名称**） | 单行、**只有名称**、无父表头 | 中 |
    | ④ | 分栏列**符号** | 贷方发生时显示**负数**（`-12.22`、`-15,525.00`）→ 按借方方向的有符号金额 | `money(r.dr \|\| r.cr)` → **始终正数** | **实质**：符号相反 |
    | ⑤ | 查询端 | **期间起止**（触发器显示 `2026年07期 ~ 2026年08期`） | **单期** | 已知 |

  - **一致的部分（可信）**：列头骨架（日期/凭证字号/摘要/方向/余额）、行按**日期排序**、**余额逐笔滚动**、**期初行与本期合计行的借/贷列强制空**（我方注释里也写了"金蝶口径"）、期初锚点＝**区间首月月初**（金蝶期初行日期 `2026-07-01` 正好印证，与科目余额表同口径）；
  - **另一处顺带佐证**：金蝶触发器在起止模式下显示 **`2026年07期 ~ 2026年08期`** —— 与我方**原始**范围选择器的 `fmtPeriod(s) + ' ~ ' + fmtPeriod(e)` **逐字一致**（Phase 1 里我按"不可达"删掉了该分支，对当前单期状态而言仍是对的，但**格式本身原本就对齐金蝶**）；
  - **教训（第二次同类）**：我上次只比对了**列头骨架**就下结论"显示层已一致、一行不用动" —— **又是基于部分证据下了完整结论**。与"把'无数据'读成'无此表'"同源。**要看实际渲染结果/截图，不能只看代码骨架**；
  - 附：截图还显示顶部有「过滤」「刷新」按钮与当前科目名（`银行存款`），我方为科目选择器 —— 属交互细节差异，未逐项比对；
- **📊 复盘：判据哪些可靠、哪些不可靠**：
  - **「列结构」这一层（第 1 层）可靠** —— 15 项里**形态判对 14 项**；唯一"半错"是 `tx`（我判"单期"，实际"单期 + 年度"，仍含单期）；
  - **4 个 ❌ 里有 3 个不是判据错，而是我没跑判据** —— `das` / `dad` / `sal` 我改用**业务直觉**猜（"折旧按月计提、无跨期语义"），而它们的列结构**明明指向起止**（`dasMonthTh` 每期一列、`sal` 列里有「期间」）。**若当时严格执行判据，这三项会判对**；
  - **唯一的真判据漏洞是 `sst`（工资统计）** —— 列结构允许区间（列里有「月份」），金蝶却取**单月**；属"产品语义"层，判据推不出，只能查；
  - **3 个 ⚠️ 全部是外围维度漏预测**：`tx` 漏「档位」→ 新增第 3 层；`sum` 漏「单位」→ 新增第〇层；`q` 漏「控件个数」→ 新增第 4 层；
  - **若剔除"我没跑判据"的 3 项**，成绩为 **11 ✅ / 3 ⚠️ / 1 ❌** —— 即**真正没判对形态的只有 `sst` 一项**；
- **📌 方法论总结（本轮最该带走的两条）**：
  1. **先查列结构，再下判断** —— 我的错例几乎都出在"用业务直觉替代列结构"。列结构是**可离线查证**的（看我方自己的代码即可），比任何外部推理都硬；
  2. **收敛重复实现要按语义搜、不能按名字搜** —— 见上文 Phase 1 遗漏（3 份具名 → 实为 7 份含内联）；
- **附：判据四层（最终版，可直接复用）**：
  1. **第〇层 单位**：口径类单一（凭证→日期、账务→期间、工资→月份）；检索类可并存；
  2. **第 1 层 列结构**：每期一列 → 起止；列里有「期间/日期」列 → 起止；固定列且本期是区间 → 粒度；固定列且期末是时点 → 单期；固定列检索/汇总 → 起止；
  3. **第 2 层 报表性质**：法定报送类才有粒度（"本期"须为完整会计期间）；
  4. **第 3 层 报送/业务周期**：决定粒度有哪几档（`tx` 只有月/年、`pl` 有月/季）；
  5. **第 4 层 口径 vs 检索**：决定能否多单位 / 多控件；
  - **不覆盖**：`sst` 类"产品语义"选择（判据推不出，只能查）；
- **📌 错误诊断（三条，按重要性排序）**：
  1. **判据原理没错，是我没"跑"它** —— `das` / `dad` / `sal` 三个错误**全部**源于我按**业务层**猜（"折旧按月计提、逐月事务无跨期语义"），而**没查它们的列结构**。而列结构明确指向起止：
     - `das` 的列头是 `<th class="ta-r" id="dasMonthTh">本月折旧</th>`，由 `Asset.js:517` 改写为 `(month || currentPeriod()) + '折旧'` —— **它本来就是"每期一列"**，与金蝶「本期折旧额」下每期一列**完全对应**；
     - `sal` 的列里就有「**期间**」一列 → 区间只增行数；
     - **答案一直在我方自己的代码里。** 这属执行纪律问题：判据要求"先查列结构"，我跳过了这一步；
  2. **判据是"必要不充分"的** —— `sst`（工资统计）是反例：其列里有「月份」列，按列结构推区间成立，但金蝶取**单月**。故**列结构能排除形态，不能唯一决定**；其上还有一层"该表的产品语义"（"统计"＝针对某一期做统计）；
  3. **我把两个层次混成了一层** —— `tx` = **单期 + 年度**说明："**有没有粒度**"由列结构／报表性质决定，而"**粒度有哪几档**"由**报送或业务周期**决定（增值税按月申报 → 只给月/年、无季度；利润表有法定季报 → 给月/季）。我先前把两层压成一条判据，才会在 `tx` 上"半对"；
- **修正后的判据（四层，逐层收窄）**：
  1. **第〇层：单位**（最先定）—— **控件单位 = 该数据自身的归集单位**：

     | 数据自身的归集单位 | 控件单位 | 例 |
     |---|---|---|
     | 凭证按「**日期**」记 | **日期** | 凭证汇总表（金蝶＝起止**日期**）、查凭证 |
     | 账务按「**会计期间**」记 | **期间** | 总账 / 明细账 / 科目余额表（起止**期间**） |
     | 工资按「**自然月**」发 | **月份** | 工资表（**月份**起止）、工资统计（单月） |
     | 折旧按「**期间**」计提 | **期间** | 折旧三表（**期间**起止） |

     ⚠️ 这三个单位**互不等价**：「期间」= 会计期间、「月份」= 自然月、「日期」= 到天。**只有会计期间与公历月对齐时，期间 ≡ 月份**；日期永远更细。
  2. **第 1 层：列结构** → 决定"用哪种形态"
     - 列**随区间月数变**（每期一列）→ **起止**（`ed`；金蝶折旧三表；我方 `dasMonthTh` 已验证）
     - 列里有**「期间 / 日期」列**（行＝分录/流水/单据）→ **起止**（区间只增行）；`gl`/`dl`/`sal` 皆属此类（金蝶工资表已证）
     - 列固定且"本期"是**一段区间**（流量报表）→ 有**粒度**
     - 列固定且"期末"是**一个时点**（存量报表）→ **单期**
     - 列固定、检索/汇总 → **起止**（区间改变合计内容）
  3. **第 2 层：报表性质** → 决定"能不能有粒度"（法定报送类才需要"完整会计期间"）；
  4. **第 3 层：报送 / 业务周期** → 决定"粒度有哪几档"（`tx` 只有月/年、`pl` 有月/季）；
  5. **第 4 层：该页是「口径」还是「检索」** → 决定**能不能有多个单位 / 多个控件**：
     - **口径类**（汇总、报表）：单位**必须唯一**（`sum` 凭证汇总表只给「日期」；`pl` 只有期间）—— 口径是**语义**，多一个就产生"以哪个为准"的歧义；
     - **检索类**（查凭证等）：单位**可以并存** —— 金蝶 `q` 同时给「**凭证期间起止**」+「**凭证日期起止**」**两个控件**；因为筛选是**条件**，多加一个只是收窄结果，不产生歧义；
     - ∴ 上面"第〇层：单位 = 数据归集单位"只对**口径类**构成单值约束；**检索类应预测"可能有多个"**；
  - **仍为反例（判据未覆盖）**：`sst` 工资统计（列结构允许区间，实际单月）—— 属"产品语义"层，判据推不出，只能查；
  - **`q` 连带查出的我方差距**：我方 `q` 只有**一个期间控件**，且 `queryVouchers(start,end,code)` 是**月级**（走 `S.periodVouchers(month)`）→ **既缺"日期"这个单位，也缺独立的日期区间控件**；金蝶是两个起止控件并存。补齐前提同样是"数据层已具备"（`v.date` 到天）；
- **📌 由 `sum` 的"日期"结论连带查出的两处我方现状**：
  1. **真实能力差距**：我方凭证**有 `date` 字段**（`v.date`，到天，`monthOf()` 只是取前 7 位），即**数据层支持按日期汇总**；但 `renderSum(start,end)` + `monthList` 只到**月**，控件也只给「期间」→ **金蝶可按 3/15~4/20 这类跨月日期区间汇总，我方做不到**。这是能力差距，不是形态差异（补齐成本：控件加日期档 + 取数按 `date` 过滤，数据层已具备）；
  2. **「月份」与「期间」在我方被合并**：工资页用的是 `periodRangeValue('salPeriod')`（单位＝期间 `YYYY-MM`），而金蝶工资表是「**月份**起止」。当前因会计期间与公历月对齐，两者数值等价、无实际影响；但**语义上应分开** —— 若将来支持自定义会计期间（不按公历月分期），工资页会算错；
- **据此上修 `q`（查凭证）的预测（属"依据新信息推论"，非改动已判分项）**：凭证级、数据自身单位为**日期** → 预测 **起止日期**（原预测只说"起止"，未及单位）。若金蝶的凭证查询是**期间**范围，则说明"单位＝数据归集单位"这条推断需修正；
- **第 4–10 项当时的预测依据（存档，现已全部验完）**：

  | 页面 | 单位 | 形态 | 列结构依据（我方实测） | 结果 |
  |---|---|---|---|---|
  | 总账 `gl` | 期间 | 起止 | 列里有「期间」列 → 区间只增行 | ✅ |
  | 明细账 `dl` | 期间 | 起止 | 流水式（日期\|凭证字号\|摘要\|借\|贷\|余额） | ✅ |
  | 多栏账 `ml` | 期间 | 起止 | 表头按科目动态生成（显示按日期） | ✅ |
  | 现金流量表 `cf` | 期间 | 粒度 | 项目\|行次\|本月金额\|本年累计金额 | ✅ |
  | 费用明细表 `ed` | **期间** | 起止 | 编码\|名称\|一月一列\|(年合计) | ✅ |

  - **`ed` 的"存疑"已解**：实际为**期间**（不是日期）—— 即"列结构（一月一列）→ 单位＝期间"这条**成立**，与折旧三表一致；
  - **`gl`／`dl`／`ml` 的"控件个数"也已验明**：**均为单控件（只有期间起止）**，没有像 `q` 那样再给一个日期控件 → 第 4 层的"口径类单位唯一"在账簿上成立；
  - **这 5 项是我第一轮"先查列结构再下判断"之后作的预测 —— 5 项全中**，与第 12–16 项那批（改用业务直觉猜的，4 个 ❌）形成直接对照；

  - ⚠️ **一处"结论对、理由错"自我更正**：`gl` 我原先的依据是"与科目余额表同族（期初/本期/累计/期末）"，但实测我方 `gl` 的列是**分录式**（…\|期间\|摘要\|借\|贷\|余额），并非余额表式。结论（起止）仍对，但**正确理由是"列里有「期间」列"**；
- **⚠️ 一个概念区分（维护者提示）**：「**月份**」≠「**期间**」—— 金蝶**工资表**用「**月份起止**」，账务表用「**期间**」；两者在**会计期间不与公历月对齐**时会不同。我方目前把「期间」当作 `YYYY-MM`（按公历月分期），故两者等价；若将来支持自定义会计期间，必须区分，否则期间控件会算错；
- **预先写死的修正方向（部分已触发）**：① 账簿三表不是起止 → "账簿口径→起止"降级为个案（**未触发**，`tb` 已证起止）；② `tx` 非纯单期 → 已按上述"第三层"修正；③ `sum`/`q` 若为单期 → 检索/汇总类改为"金蝶不给"（待验）；④ `ed` 若不是起止 → "列结构即区间"判据错（**未触发**，金蝶折旧三表已验证该模式）；
- **仍在确认中**：
  1. 粒度下拉里**是否只有月报/季报**（两张截图都只看到这两项；是否有年报/半年报，未知）；
  2. **账簿类页面**（总账/明细账/科目余额表）用的是起止还是粒度 —— 这决定"目标形态"中账簿类的归属。

---

## 2026-09-12 — 冻结「结转损益 / 计提折旧」重构（决策记录，非代码改动）

- **决定**：`js/store.js` 的 `carryForwardProfit`（65 行）与 `depreciateMonth`（67 行）逻辑正确、行为已由自动化守护锁定。本轮及后续默认**不**做「加花括号 / 改缩进 / 拆辅助函数」等纯可读性改动——不改业务口径，风险大于收益；
- **守护机制**（若将来确需改动这两函数，改后必须全部跑绿）：
  - 结转损益幂等 → `node tools/verify_invariants.js` 的 I6；
  - 结转损益口径 = 利润表口径（逐期）→ `node tools/audit_books.js`；
  - 折旧重复计提幂等（不产生额外凭证）→ `node tools/verify_e2e_snapshot.js` ②；
- **基线（2026-09-12 验证）**：I1–I10 不变量全 PASS；结转口径审计 4 账套 12/7/12/7 期全一致；
- **若确需重构**：单独开一轮，改完立即跑上述脚本，全部 PASS 才算完成。

---

## 2026-09-12 — 首页与利润表口径同源、指标下钻跳转、清理手机版设定

### 首页损益口径与利润表真正同源（消除第三套实现）
- 此前首页「收入/成本/费用/净利润」自带 5xxx/6xxx 编码清单、逐分录累加，是独立于利润表引擎的**第三套口径**：用户自定义利润表规则时首页不跟随，且两处一旦漂移没有任何机制能发现（利润表侧有 I10 恒等式保护，首页游离在外）；
- 利润表规则行补**语义 id**（`js/standards.js` 两套准则模板共 20 行，对应金蝶/jinbooks 的 `itemCode`）；老账套 `reportRules` 是模板快照、不会自动获得新字段，故在 `normalizeState` 中按 **codes 排序签名**回填（对科目顺序调整免疫；空 codes 的多行同签名无法唯一匹配，不参与回填）；
- 行计算下沉为 `store.incomeStatement(month)`（从 `Report.js` 逐字搬移、不改算法），`Report.js` 的 `computeIncomeRows` 改为薄封装；新增 `store.plSummary(month)` 按行 id 取数，返回 `{ cur, ytd, codes, ids, missing }`；
- 首页改调 `plSummary`：单月取 `cur`、整段取 `ytd`（等价金蝶 `periodType=MONTH/YEAR`，故无需给报表引擎新增该概念），删掉自带的 8 个编码常量与 `buildProfitRange`；
- **回归**：搬移前后逐行对比 940 项、新旧口径对比 752 项（2 真账套 × 全部有凭证月份 × 4 期间模式 × 4 指标）均 **0 差异**，现有数字一个未变；另补 6xxx 准则端到端用例（含结转凭证不使收入翻倍、所得税不进费用）。

### 首页指标 → 利润表 下钻跳转
- 收入/成本/费用/净利润四张卡点金额 → 跳「报表 → 利润表」并高亮对应项目行（`Report.js` 新增 `__plJumpToRow` / `highlightPlRows`），形成「首页指标 → 利润表 → 点行金额 → 总账明细」完整下钻；
- 「费用」是销售+管理+财务三行合计、「净利润」行无科目编码 —— 直接跳总账表达不了，故改为跳报表、再由报表行下钻总账；其余卡片（资金/应收应付）无对应报表，保持直接跳总账；
- 行定位复用项目既有的 `.row-hl` 高亮类；滚动沿用 `app.js` 搜索跳转的 60ms 延时（切页刚置可见时立即 `scrollIntoView` 会落空）；高亮 2.5s 渐隐，连续跳转先清旧目标；
- 跳转目标由取数处注入（`data-pl-rows` 与 `data-codes` 互斥），规则行缺失时取消可点态并给说明，不静默跳到与数字无关的位置。

### 清理手机版设定（本软件无手机版）
- 移除 `app.js` 的手机端检测（移动 UA / 触屏宽度 < 900px）、`window.__IS_MOBILE` 与 `html.is-mobile` 挂载；
- 移除 `goPage` 中「手机端任何跳转一律回首页」的强制重定向；
- 移除 `style.css` 中 `html.is-mobile ...` 只读模式样式整块（29 行）；
- `index.html` 移除 `<meta name="viewport">`（桌面 webview 无作用，与「无手机版」保持一致）；
- **保留** `style.css` 的窄屏 media query（≤1400/1200/1000/640px 时首页 6 卡自动降档）—— 那是**桌面窗口拖窄**时的响应式保护，非手机版设定；已核实 `tauri.conf.json` 未禁用 resize、未设 minWidth，拖窄窗口时这些规则会实际生效；
- 连带收益：不再存在手机端只读模式，首页卡片跳转在唯一目标环境（桌面）下均正常。

### 资源版本号改为构建时自动注入（根治手工 bump）
- **问题**：`index.html` 与各级 ESM import 的 `?v=` 全靠人工维护，且引用链有三层（`index.html` → `main.js` → 各页面 → `components/`）。改一个被引用文件，必须把所有引用者的版本号一起改；漏改任一环，webview 就命中磁盘缓存的旧文件，而且**故障是静默的**——改动看起来"没生效"，不报任何错。实测本轮改动就需同时改 `main.js`，而若改的是 `components/SubjectPicker.js` 则需改 6 个文件（`main.js` + `Ledger.js`/`Asset.js`/`Subject.js`/`Voucher.js`/`Settle.js`）；
- **修复**：`tauri/scripts/build-dist.mjs` 在同步 `dist/` 后自动注入版本号 —— 源文件的 `?v=` 一律改为占位符 `?v=dev`（不再代表任何含义、不需要再 bump），打包时统一改写为**本次构建的内容指纹**（dist 内全部待发布文件按相对路径排序、逐个喂「路径 + 原始字节」进 sha1，取前 12 位）；
- 为什么用单一全局指纹而非逐文件哈希：`dist/` 是本地磁盘资源，没有重复"下载"成本；单一指纹免去「被引用者先算、引用者后算」的拓扑排序，也就彻底不存在"改了子模块忘改引用者"的漏洞（任一文件变 → 全量 URL 变，中间层 `main.js` 因自身 import 文本被改写而自动跟着变）；
- **验证**：`dist` 内 39 处版本值统一为同一指纹；重复构建指纹不变（同源可复现）；新增/删除任一文件指纹随之变化；恢复后回到原指纹；
- 覆盖范围：`index.html` 16 处、`js/main.js` 17 处、`Ledger.js` 2 处、`Asset.js`/`Subject.js`/`Voucher.js`/`Settle.js` 各 1 处，共 39 处；
- **发布链路保证**：GitHub Actions 用 `tauri-action` 跑 `tauri build` → 必经 `beforeBuildCommand` → 本脚本，故发布版不可能因遗漏 bump 而陈旧；`tauri build` 与云端构建行为一致；
- 说明：`tauri dev` 走 `devUrl`（`python3 -m http.server`，源目录），不经本脚本；该服务会返回 `Last-Modified` 并按 `If-Modified-Since` 回 304，故 dev 下普通刷新即为最新（如遇极端缓存，硬刷新一次即可）。

### 发布流程去掉「本机打包」，全部改由 GitHub 云端产出
- `发布新版.command` 移除第 5 步「本机打 macOS 包」（`cd tauri && npm run tauri build`），步骤由 5 步收敛为 4 步，编号与提示同步更新；
- 依据：`.github/workflows/build-release.yml` 已由 `build-windows`（NSIS）+ `build-macos`（app,dmg）两个 job 串行产出双平台安装包并进同一 Release（`releaseDraft: false`），**本机打包并非必要环节**，删掉不损失任何产物；
- 收益：本机无需安装 Rust / Xcode，也不占用本机数分钟编译时间；
- 顺带修正两处过时文案：
  - 结尾原写「Release 草稿在 …… 记得去点一下 Publish release 才能正式发布」，但工作流已是 `releaseDraft: false`（自动发布），现改为「约 10~20 分钟后可直接下载」；
  - 第 4 步说明原写「自动打包 Windows 安装包」，实为 Windows + macOS 双平台，已更正；
- `README.md` 同步：把「本机手动打包（备选）」一节换成「想先拿个测试包（不用本机编译）」（指向手动触发的「构建预览包」工作流，产物进 Artifacts、不进 Release）；「日常发布入口」改为 4 步并标注**本机全程不编译**；常见问题补充「发布后安装包在哪」与资源版本号自动注入的说明。

### 清理本机打包相关残留
- 删除 `tauri/run-build.ps1`（Windows 本机打包脚本：跑 `npm run tauri build -- --bundles nsis` 并把 `*-setup.exe` 复制到项目根）—— 全库无活引用，仅 CHANGELOG 历史条目提及；
- 删除 `tauri/scripts/build-dist.sh` —— `build-dist.mjs` 的旧版 POSIX 前身（其自身注释已说明改用 Node 实现以摆脱 `sh` 依赖）；`tauri.conf.json` 的 `beforeBuildCommand` 只调 `.mjs`，全库无引用；
- 删除 `tauri/WINDOWS_BUILD.md`（51 行 Windows 本机手动打包手册）—— 与「本机不打包」流程矛盾；
- `tauri/README.md`：移除 `WINDOWS_BUILD.md` 指引（并将「本目录命令仅供本机自测/备选」改为「仅用于开发期 `tauri dev` 调试」）；「如何打包（macOS 本机）」一节改为「如何拿到安装包」（云端正式发布 / 云端预览包两条路径）；「注意事项」删除与新章节重复的一条；
- `README.md` 目录树：去掉 `WINDOWS_BUILD.md` 条目；`scripts/build-dist.mjs` 的说明补上「按内容指纹自动注入资源版本号」；
- `docs/go-live-blockers.md`（2026-09-05 审计快照）：「双平台打包」待办项原指向 macOS `npm run tauri build` / Windows `tauri/run-build.ps1`，改为云端构建路径并标注旧方式已弃用 —— 属修正死引用，不改动该文档的审查结论；
- 复检：全库已无 `run-build` / `build-dist.sh` / `WINDOWS_BUILD` / 本机打包 的活引用（仅存 CHANGELOG 历史条目与本次说明）；`tauri/scripts/` 现只剩 `build-dist.mjs`；构建脚本冒烟通过且指纹与本次改造前后一致（`4c14fa08b33a`，可复现）。

---

## 2026-09-10 — 常用功能设置优化（录凭证固定 + 默认顺序）

- 默认常用功能顺序：录凭证 → 查凭证 → 明细账 → 总账 → 科目余额表 → 资产负债表 → 利润表 → 标准现金流量表 → 费用明细表；点「恢复默认」即回到这套；
- 「录凭证」设为固定项：默认勾选、不可取消（首页大卡片本身也是静态入口，不受存储影响），且不占用其余功能 15 个名额；
- 设置弹窗顶部增加说明行，固定项带「固定」标签并置灰。

---

## 2026-09-10 — 科目栏「编码 + 全路径名」、搜索入口统一与发版卡口

### 录凭证科目栏显示编码与名称
- 科目单元格改为「编码输入框 + 右侧科目名称」，选中/手输/失焦三处同步，不再只显示一串编码；
- 名称显示**全路径名**（一级-二级-末级）：`100203` 显示「银行存款-建行（陈总）」，不再是看不出归属的「建行（陈总）」。

### 科目全路径名统一到所有搜索入口
- 新增共享模块 `js/common/subject-name.js`（唯一实现，末级名已含父名时不重复拼接）；
- 统一生效于：录凭证科目联想下拉、科目范围选择弹层（查凭证/明细账等）、顶部全局搜索的「科目/账簿」结果、费用明细科目选择弹层；
- 联想匹配同步增强：除编码前缀/末级名外，可按全路径名搜索（搜「银行存款」命中全部银行子户）。

### 科目余额表树形（确认与对齐）
- 与科目页同款：默认只显示一级（父级），行首三角逐级展开/收起；
- 页内「展开所有级次」勾选框（与科目页文案统一，原为「展开所有次级」）；
- 合计口径与展开状态自洽：父级展开时由子级明细贡献，收起时计本行（该支子树总额），杜绝父/子重复累加；
- 导出 Excel 与屏幕所见一致（同一折叠态）。

---

## 2026-09-09 — 内置凭证模板库、树形三角统一与资金余额口径修复

### 内置凭证模板库（常规/业务 20 个）
- 「从模板生成凭证」改为 jinbooks 风格：搜索条 + 分组行式列表（系统模板 / 我的模板）；
- 内置 20 个常用模板（金蝶默认 10 个：提现/付手续费/报销差旅费/收付款/赊购赊销/三计提 + 投资款/借款还款/办公费/水电费/银行利息/购固定资产/发工资等）；
- 套用时按本账套科目自动适配（精确编码 → 5xxx/6xxx 准则迁移 → 同名/前缀取最短编码），账套缺的科目标红提示并跳过；内置模板只读（仅「使用」），我的模板可存可删。

### 修复：首页「资金余额」翻倍（父子科目重复聚合）
- 根因：`generalLedger` 每行余额已含下级，首页又把「父级 + 子级」相加 → 有子户的科目成倍虚增（实测银行存款 639,306 显示成 1,278,613，资金余额 669,356 显示成 1,326,000）；
- 收敛为**唯一取数契约** `store.subjectEndBalance` / `store.cashBalance`（含踩坑说明），页面不再各自遍历子科目聚合；
- 新增 `tools/audit_books.js` 对账脚本（逐期借贷平衡 / 试算平衡 / 资金余额 / 上卷自洽），并接入「发布新版.command」作为**发版前卡口**（不通过则中止，可 y 强制继续）。

### 树形展开三角统一
- 科目页 / 明细账 / 费用明细（表内 + 科目弹层）/ 原始凭证 的三角统一为实心 `▶/▼`、12px、18×18 点击区、悬停变蓝；
- 顺带修掉原始凭证「CSS border 三角 + 字符三角」两个三角叠加的遗留问题；
- 只统一外观与手感，各页默认展开策略与点击语义保持原状。

---

## 2026-09-09 — 明细账双栏滚动优化与凭证保存防误触

### 明细账双栏布局（滚动条互不干涉）
- 主表由「整页长滚动」改为**工作区内自滚**，表头吸顶；右侧科目树与主区同高、独立自滚——与资产卡片/电子档案的成熟双栏一致，不再出现一长一短两条滚动条互相干扰；
- 科目树宽度**随科目名称内容自适应**（超长封顶 300px 后省略号），收起后正确收成窄条；
- 两处滚动条统一细条化（低饱和圆角，hover 加深）；
- 修复切换页面时明细账区块残留显示的 bug（容器规则漏 `.active` 限定导致与目标页叠层）。

### 凭证保存防误触（单人记账场景）
- 从明细账/账簿等穿透打开凭证后直接点「保存/暂存/保存并新增」：若内容**完全未修改**，提示「凭证未做修改」并跳过落库——不再产生虚假的「修改凭证」日志、也不会把已审核凭证误打回草稿；
- 真正改动内容后保存行为不变（原已审核的凭证按既有机制提示已自动撤销审核）。

---

## 2026-09-08 — 凭证打印与账表穿透完善

### 新增能力
- **凭证打印**：录凭证页可打印当前凭证（记账凭证纸版式：字号/日期/分录/合计/制单人）；可打印**空白记账凭证**（空行手填，字号/日期/附件留空，记账/审核栏），打印预览页统一带「Ctrl+P / Cmd+P」提示（打印时自动隐藏、不进纸张）。
- **账表穿透补全**（此前仅查凭证、明细账、总账局部可点）：
  - 凭证字号 → 打开该凭证：**多栏账、数量金额明细账、核算项目明细账、原始凭证**（已关联凭证）；
  - 科目编码 → 明细账定位：**科目余额表、凭证汇总表、查凭证科目列、核算项目明细账 / 余额表、费用明细表**；
  - 穿透按「日期 + 字-号」映射凭证，规避跨月同字-号串号；凭证已删除时自动退化为纯文本，不误跳。

### 修复与打磨
- 清理凭证中心遗留的不可见「入口首页」死代码（整块视图、相关样式与函数一并移除）。
- **账务健康体检去噪**：父级/末级科目重复告警只报末级；权益类借方余额（累计亏损/超额分配）不再误报为「大额资金问题」；大额凭证清单改为按账套金额分布取 TOP-30（原固定 5 万阈值在小账套全量命中）；「期末损益未结转」只检查**已结账期间**（当期未结账不再整屏误报）；提示文案改为中性「建议核实」，不做定性指控。
- 修复跨年结账一致性校验误报：推导上年期末时未累计往年结转凭证，导致跨年校验把账判为有差异。

---

## 2026-09-08 — 云备份 / 云同步（WebDAV）：账套多一份"不在本机"的副本

### 新增能力
- **云备份 / 云同步**：设置页新增「云同步」卡，用 WebDAV（坚果云等）在两台电脑间手动同步账套。
  一台点「云备份」（本机 → 云端），另一台点「云同步」（云端 → 本机）；
- 手动触发、软件**永不在后台联网**；同步只做补齐与覆盖，**永不删除**——本机删除不删云端，反之亦然；
- 取回覆盖本机前自动留 `pre_sync` 备份，可在「查看备份」中回滚；对端存在更新账套时先确认，不静默覆盖；
- 已配置且距上次备份 ≥1 天、期间有改动时，软件**启动**时静默自动备份一次（首备份需手动，避免刚配好就联网）。

### 首页提醒（被动一条，不打扰）
- 未配置 → 「尚未配置云备份，建议配置云备份。」可点「去配置」直达；
- 已配置但从未备份 → 提示立即备份一次（否则自动备份不会启动）；
- 距上次备份 ≥7 天且有改动 → 提示做一次云备份；
- 点「×」忽略后 7 天不再提醒；点了「去配置 / 去备份」但没办成，次日继续提醒。

### 修复与打磨
- 修复「取回后每次再取回都误报『本机比云端新』」：取回时把文件时间还原为云端记录的内容时间，
  并补了 A/B 两机往返的自动化联调测试（本地 WebDAV，不碰真实账套）；
- 云备份/云同步加互斥锁，避免启动时的自动备份与手动点击并发写坏索引；
- 设置页排版统一：操作按钮统一对齐内容竖线，「保存系统参数」归位到它所属的卡片，
  账套管理工具区整理（说明文字不再挤压操作链接）。

---

## 2026-09-06 — 备份收敛为极简模型：简单、稳定、只防"该防的"

### 产品判定（与使用者确认）
- **录错账**：不靠备份恢复，走财务手段（红字冲销 / 反结账 / 重做结转）——账本不做"时间旅行"；
- **错删账套**：走回收站（保留 7 天可还原）；
- **软件故障 / 文件损坏或丢失**：这才是备份唯一要防的场景。

### 最终机制（无 UI 术语，财务只看到「备份 · 时间」）
- **自动存档**：账套每次落盘自动留 1 份，最近 10 份滚动——文件坏/丢时的近时救援；
- **覆盖前存档**：仅在「导入 / 恢复备份」等整本覆盖动作前，自动留当前账本 1 份，保留 3 份——
  防这两个操作把账弄砸；
- **回收站**：删除账套保留 7 天（不变）；
- 备份列表平铺为时间行，恢复提示说明「仅用于文件意外，账务差错请用红冲/反结账」。

### 清理与修复（相对历史复杂实现）
- 取消「每日快照 30 天 / 关键节点快照 / 恢复前快照」分层概念与 UI 分类术语；
- 移除写前 .bak1~5 滚动（原子写已防写坏，纯高 I/O 冗余）；
- 孤儿备份全局收敛：启动与删除账套时执行，已删/换 id 账套只留最新 1 份兜底——
  backups/ 由 103 份收敛至 24 份并保持有界；
- 备份恢复改为按完整文件名取回（修复此前部分备份无法从列表恢复的缺陷）。

---

## 2026-09-05（第三轮）— 投产前审查：出纳残留清理 + 审计口径修正 + 阻塞清单落盘

投产前全局审查（目标平台 Windows + macOS）的第一轮落地，含 Phase 1「历史风险清单对账」。

### 产品决策
- **出纳板块整体下线**：UI/数据层此前已基本清理，本次清除残余——空目录 `js/pages/cashier/`、CSS 孤儿选择器 `#baGrid`。
  注意：`cashAccounts`（store.js，资金类科目 1001/1002/1012 口径）**在用于现金流量表/赤字检查，非出纳残留，未删**。
- H1 税金测算 / closedPeriods：对账确认无需删除——H1 提示文案已带「仅供提醒」；空结账期仅来自金蝶导入映射。

### 修复与修正
- `tools/_deep_audit.js`：结账期间「空结账期/跳期」仅可能来自金蝶导入闭账映射、软件自身 closePeriod 不会产生，
  第 5 段改为「导入映射特性说明」不再计 FAIL（添钰 43 PASS / 0 FAIL）。
- `tools/verify_idempotent.js` / `tools/verify_e2e_snapshot.js`：删除引用已下线 `exchangeAdjust` / `ensureBankAccounts` /
  `bankAccountsAll` / `adjustTable` 的过期断言（原运行必抛 TypeError）；e2e 脚本补 persist/addLog/backupNow no-op 杜绝写盘风险。
- `js/pages/report/Report.js`：加注释说明现金流量表「净增加额/期初/期末」行本年累计列口径——**勿单点误改**（详见 blockers M7）。

### 审查结论（详见 docs/go-live-blockers.md）
- 历史 H/L/M 逐条对账：H2/H3/M4 已修复；M1 为设计使然；M6 取数正确缺引导；M5 防呆缺失（P2）；
  **M2「本年累计缺期初累计」经产品确认不会遇上，记为已知限制（不修）**；
  绅蓝之星未分配利润勾稽 2 处差异（4719.87/2381.50）经逐月逐凭证定位（`tools/_diag_undist.js`）确认为金蝶
  「以前年度损益调整 6901 结转留存收益 + 损益跨期结转（所得税 5801）」业务特征，非引擎缺陷、非数据不平
  （借贷平衡/试算/明细账/资产负债表全 PASS）。`_deep_audit.js` 第 3 段相应改为可解释口径（差异 → 透明 WARN），
  三账套现均 0 FAIL。

### 补充（同日）：macOS 打包后「点关闭报 ACL 红字」修复
- 现象：打包版点窗口关闭报红字 `系统异常: Command plugin:window|destroy not allowed by ACL`（首页即可复现）。
- 根因：Tauri v2 capability 未授权 window 关闭/销毁命令（`core:default` 不含 `allow-destroy`）。
- 修复：`tauri/src-tauri/capabilities/default.json` 补 `core:window:allow-close`、`core:window:allow-destroy`；
  另 `js/app.js` 关闭守卫整链加异常保护（未保存询问任一环节失败即放行关闭，宁可不提示不可报错卡死）。
- 验证：mac 打包后首页直接关闭、凭证页未保存确认均正常（用户实测通过）。

### 补充（同日）：M5 防呆 + 兼容/性能验证 + M7 处置
- **M5 已修**：`updateSubject` 禁改「已有凭证/期初余额」科目的类别（新增 `_subjectUsed`，含子科目判定）。
  验证 `tools/_verify_m5.js`：未用可改、在用禁改但可改名，合成 + 添钰/绅蓝 真实账套全通过。
  `index.html` store.js 版本升至 `?v=2026090504`。
- **旧版兼容**：`tools/_verify_backcompat.js` 确认改造前无 `v.kind` 的账套/备份加载、`restoreFromData`
  恢复后均自动回填且识别分布与现版一致、不变量保持。
- **性能**：`tools/_perf_stress.js` 合成 2 万凭证，最重入口 generalLedger 217ms、其余报表/结账 <30ms；
  真实在用账套仅 1~2.5 千凭证，余量 10 倍以上。
- **M7 处置决定**：现金流量表净增加额「本年累计」列口径两难——投产前**挂起不改**（不影响数据正确性，
  已加注释防单点误改，见 blockers）。
- **运行态静态检查**：`test_stale_dom_refs`、`check_no_native_download` 通过；
  `check_no_native_dialog` 报的 `file-save-bridge.js` 2 处 `alert` 为导出结果提示的降级兜底（非业务
  确认/输入弹窗），记录不修。
- 新增工具：`_verify_m5.js` / `_verify_backcompat.js` / `_perf_stress.js` / `_diag_undist.js`（勾稽定位）。
- **`tools/import_check.js`（金蝶导入兼容性自检）**：一条命令输出 红/黄/绿 报告，覆盖借贷平衡/幽灵科目/
  科目类别vs准则模板/期初与逐月勾稽/明细账连续性/结账检查/结转识别/凭证字号。
  定位：金蝶正常账套应红灯恒 0——红=软件导入/引擎兼容缺口（报开发修复后重导，使用者零操作）；
  黄=金蝶映射说明或旧版导入器遗留。三账套试跑均红灯 0。
- **`js/kis-import.js` classify**：以前年度损益调整（6000/6901 及变体）由兜底 asset 改归 **equity**
  （权益调整科目，避免将来产生余额时污染资产负债表资产侧）——此类映射由导入器消化，不要求使用者在科目页改。
- **`tools/_verify_newbook.js`（场景 2「新建公司账套」全流程验证）**：新建→期初→7 月连续录凭证→
  逐期结转+结账→报表勾稽/反结账约束，16/16 通过。数据层两场景（导入/新建）均已自动验证。

---

## 2026-09-05（第二轮）— 系统性根治：期末业务凭证改用类型标记 `v.kind`，废弃摘要正则判定

### 背景：上一版修复只治了单点，底层是系统性缺陷
经全量勘察确认，全工程 **共 14 处**「某类期末凭证是否已存在」的判定，全部依赖凭证摘要正则
（`/结转.*损益/`、`/年度本年利润/`、`/计提.*折旧/`、`/(计提|发放).*工资/` …）。
而金蝶 KIS / Excel 导入的凭证**根本没有凭证级 `summary` 字段**（摘要只落在分录级
`entries[].summary`，见 `kis-import.js:192`），故 `re.test(v.summary || '')` 对导入凭证**恒为 false**。
除已修的结转损益外，还遗留：

| 位置 | 后果 | 等级 |
|---|---|---|
| `carryYearEnd` 幂等（纯摘要，无兜底） | 12 月可重复生成年结凭证 → 错账 | P0 |
| `Settle.js` `genOnceVoucher` 查重（纯摘要） | 附加税/所得税/增值税可重复生成 → 错账 | P0 |
| `settleChecklist` 12 月 `yearend` 项（纯摘要） | 12 月恒判「未年结」→ 结账阻塞 | P0 |
| `btnReCarryForward` 定位旧凭证（纯摘要） | 金蝶账套「重新结转」完全不可用 | P1 |
| `hasPayrollVoucher` | 双重失效（无摘要 + `v.period` 类型错误），保护从未生效 | P1 |
| 结账页/期末处理页 6 类统计与凭证号 | 恒显示「未生成 / 待结转」 | P2 |

### 更正上一版的根因描述
上一版称「3103 是 revenue cls，会被计入 totalRev，导致重复结转」——**该描述有误**。
实据：`standards.js:58/116` 与 `kis-import.js:43` 均将 3103 归为 **equity**，
`s.cls === 'revenue'` 永不命中，物理上不可能污染 totalRev。
真正的死锁成因是**两套取数口径打架**：`profitStatement` 取发生额（收入取 cr、费用取 dr，不轧差）、
`carryForwardProfit` 取轧差净额，已结转月份一个≠0、一个=0，互斥成死锁。

### 方案：以 `v.kind` 为单一事实源（禁止再新增摘要正则判定）
- **数据层**：`addVoucher` 原样保存传入对象（`store.js:1141`），故 `v.kind` 天然持久化，无需改存储。
- **生成端打标**：结转损益 / 年结 / 折旧 / 计提工资 / 发放工资 / 结转成本 / 转出增值税 / 计提附加税 / 计提所得税。
- **存量回填**：`normalizeState()` 中调用 `ensureVoucherKinds()`。选此钩点是因它是所有账套入口
  （本地加载 / 服务端加载 / 金蝶导入 / 备份恢复）的公共路径，一处覆盖全部。
- **结构识别**：按「科目角色组合」判定（经 `subjectRole()` 解析，**不硬编码费用类科目码**，两准则自动适配）。
  识别不出一律返回 `undefined`（按普通凭证处理）——宁可漏标，绝不错标。
- **同口径根治**：新增 `periodProfitNet()` 作为「是否需要结转」的唯一取数，
  `carryForwardProfit` 与 `settleChecklist` 共用，二者条件严格等价，死锁不可能再现。

### 改动文件
- `js/store.js`：新增 `VOUCHER_KINDS` / `_detectVoucherKind` / `voucherKind` / `periodVouchersOfKind` /
  `ensureVoucherKinds` / `periodProfitNet` / `carryForwardState`；改造 `carryForwardProfit`、`carryYearEnd`、
  `settleChecklist`（8 项）、`hasPayrollVoucher`、`_voucherRefs`；生成端打标 4 处。
- `js/pages/settle/Settle.js`：查重/重做/统计/凭证号全部改用 kind（`genOnceVoucher` 签名由正则改为 kind）。
- `js/pages/asset/Asset.js`：折旧凭证筛选改用 `periodVouchersOfKind`。
- 顺带修复：净额为零（收入=费用）时不再生成 0 金额的「结转本年利润」空分录。

### 验证（真实账套，只读 / 内存副本，磁盘账套未改动）
- `tools/_verify_kinds.js`（新增）：逐类型对比「结构识别 vs 旧摘要正则」——
  **漏识别 = 0（无回归）**，新增识别 **82 张**金蝶结转凭证（添钰 30 / 绅蓝 52，此前完全找不到）。
- `tools/_verify_scenarios.js`（新增）：**40 项全通过**，含此前失效的关键路径——
  金蝶已结转月「重新结转」可用（可定位并删除旧凭证后重做）、幂等拒绝提示能给出具体凭证号、
  真实未结转月（2026-08）正常结转、12 月可成功结账。
- `tools/_stability_test.js`：添钰 / 绅蓝 **各 24 PASS / 0 FAIL**。
- `tools/_diag_carryfix.js`：死锁场景全通，2025-12 提示已带凭证号（记-594、记-595）。
- `tools/_deep_audit.js`：**43 PASS / 2 FAIL**，2 个 FAIL 为既有历史数据问题
  （空结账期 2025-01/02、跳期 2025-11→2026-01），与本次改造无关，详见下一节。

### 已知行为变化
- 在金蝶账套上执行「重新结转损益」，会把金蝶原本拆分的**多张**结转凭证（如转收入、转费用各一张）
  合并生成为**本软件口径的 1 张**。净效果一致（净额归零、借贷平衡、试算平衡均已验证），符合软件既有设计。

---

## 2026-09-05 — P0 修复：金蝶导入账套「结转损益」判定误判导致结账死锁

> 注：本轮为应急止血，已于同日被上方「系统性根治」条目取代。
> 其根因描述有误已被更正（见上），其中 `hasCarryForward()` 的「净额轧平即视为已结转」判定
> 因会误锁「重新结转」入口，已由 `v.kind` 结构化判定取代。

### 问题（真实投产前审查发现）
- **根因**：金蝶 KIS 结转账凭证的**分录级摘要 FExp 为空**（金蝶只记凭证头摘要），导入后 `v.summary=''`。
  而引擎的「结转损益」判定与幂等保护均用摘要正则 `/结转.*损益/` 匹配：
  - `settleChecklist`（结账检查-结转损益）把已结转月份误判为「本期损益未结转，请先结转损益」→ 结账被硬性拦截；
  - `carryForwardProfit`（点「结转损益」按钮）因摘要空而**不触发幂等拦截**，金蝶已结转月份会被重复结转（潜在错账）。
  - 组合后果：**结账说「请先结转损益」、点结转又说「无需结转/重复结转」→ 该月永远无法结账（死锁）**。
- **影响面**：所有从金蝶 KIS 导入、且结转损益摘要为空的账套（添钰来客/绅蓝之星 2025、2026 历史月全部命中）。

### 修复（js/store.js）
- 新增 `hasCarryForward(month)`：统一「本期损益是否已结转」判定，双口径：
  1. **摘要命中**（本软件生成的「结转YYYY-MM损益」凭证）；
  2. **结构化判定**（兼容金蝶）：本期损益科目（revenue/expense，排除 3103/3104 结转科目）的
     借/贷净发生额均≈0 即视为已结转（金蝶结转凭证把损益科目结平，净额归零）。
- `carryForwardProfit()` 幂等检查改用 `hasCarryForward()`；`settleChecklist` 的「结转损益」检查同口径。
- 效果：摘要为空的已结转月 → 结账检查显示「损益已结转」，点结转被正确拦截；
  真实未结转月（如 2026-08）→ 仍正常要求先结转损益（不误放行）。

### 验证（真实账套，只读/内存副本，不改磁盘）
- `tools/verify_invariants.js`：添钰/绅蓝/鲁海丰 3 账套 I1-I10 全 PASS（修复无副作用）。
- `tools/_diag_carryfix.js`：2026-08 首次结转成功、二次幂等拦截；2025-12（金蝶已结转）正确拦截不重复；
  反结账 2026-07 后重新结账全链路走通（原死锁场景解除）。
- `tools/_stability_test.js`：T1-T10 全套高风险操作（结账/反结账/结转/凭证保护/高频循环/借贷平衡强制）
  在添钰/绅蓝内存副本上 **24 PASS / 0 FAIL**。
- 深度审计：逐月试算平衡、货币资金勾稽（现金流期末=货币资金1001/1002/1012）、
  已结账月末未分配利润勾稽、明细账余额连续、凭证无同号 全 PASS。

### 遗留（数据层说明，非本次修复范围）
- 已导入历史账套的 `closedPeriods` 存在金蝶映射的「空结账期」（期初 1-2 月无凭证却标记已结账）与
  「12 月未标记结账」现象——属导入时闭账状态映射口径，不影响报表正确性（报表全由凭证动态生成，
  已核验资产=负债+权益、货币资金勾稽、未分配利润勾稽逐月成立）。
- 两店历史账套含「以前年度损益调整 6901/6000」科目（金蝶结转至 3104 未分配利润）与负数红冲，
  未分配利润勾稽差额精确等于该科目结转额（4156.42）等业务处理，非引擎计算错误。
- 期末调汇功能已下线，`tools/verify_idempotent.js` 中调用 `exchangeAdjust` 的断言为历史遗留，
  与现版引擎无关（CHANGELOG 2026-09-01「期末调汇下线」已有记载）。

---

## 2026-09-05 — 审计复核：I10 科目类别错标 + Windows 打包就绪 + 关闭守卫

### Ⅰ. I10 恒等式复核（延续 H2，防漏网记录）
- **复核结论**：此前 H2 修复覆盖不全。实测 4 个真实账套中，导入科目类别与准则模板不一致共 13 处——
  4001 生产成本及子目 → `equity`（应 `expense`）、2401 递延收益 → `asset`（应 `liability`）、
  5301 营业外收入及子目 → `expense`（应 `revenue`）、4101 制造费用 → `equity`（应 `expense`）。
  表现：利润表「页面行合计 ≠ 数据层 netProfit」差额=错标科目当月发生额×2，仅在有发生额的月份出现，
  而借贷平衡/资产负债表恒等式全部正常，故此前只查勾稽与平衡的多次审查未发现。
- **代码修复**：`kis-import.js` `classify` 优先取 `standards.js` 权威一级科目类别（子目按编码前缀继承），
  模板未覆盖的自定义编码才回退 FDC/前缀启发式。
- **审计防漏**：`tools/verify_invariants.js` 的 I10 失败时自动输出「科目类别与准则模板核对」清单；
  复核结论已补记 `docs/audit-2026-09.md` H2 小节。
- **处置**：已导入的旧账套需重新导入（或手工修正科目类别）；详见 `docs/audit-2026-09.md`。

### Ⅱ. 单实例 + 退出确认（对应 Windows 分析清单 2.x）
- `Cargo.toml` 增加 `tauri-plugin-single-instance`；`lib.rs` 注册单实例插件，重复启动时唤出已有主窗口并退出新进程，杜绝双进程同时写同一账套。
- `js/app.js` 新增未保存编辑守卫：点窗口 X 时仅当凭证录入/期初余额存在「改动未保存」才弹确认，否则直接退出不打扰。
- `index.html`：`vEditView`、`page-opening` 增加 `data-unsaved-scope`；`app.js` 版本号升至 `2026090501`。
- `js/pages/settings/Opening.js`：期初余额保存前增加借贷平衡校验（不平衡即阻断并提示差额）。

### Ⅲ. Windows 打包脚本
- 重建 `tauri/run-build.ps1`：后台执行 `npm run tauri build -- --bundles nsis`，成功后将 `*-setup.exe` 复制到项目根。

---

## 2026-09-02 — 顶栏 + 应用图标：替换为紫色月牙叶 logo（原 TY 蓝渐变方块）

照用户提供的紫色月牙叶 logo 替换两处品牌图标，统一品牌视觉：
- **设计**：两片饱满紫色月牙/叶（右上+左下对角分布），紫色对角渐变（#a78bfa→#8b5cf6→#4f6ef7），透明背景
- **`tauri/src-tauri/icons/icon.png`**：用 image_gen 生成 1024 PNG 并去白底透明化；`npx tauri icon` 重新生成全套 .icns/.ico/各尺寸（iOS/Android/macOS/Windows）—— 应用图标与原图视觉一致
- **`css/leaf-icon.png`**：保存 1024 透明版供顶栏引用
- **顶栏品牌**（`index.html`）：原内联 TY 蓝渐变方块 SVG 替换为 `<img src="css/leaf-icon.png" width=30 height=30>`，与桌面 logo 同一张图、品牌三处一致（顶栏 / 桌面 / 应用）
- **`~/Desktop/添钰logo.png`**：复制一份供查看

**验证**：lint 无新增错误（8 个 warning 均原有）；图标透明度正确（月牙外透明、白顶栏上自然透出）。

---

## 2026-09-02 — 彻底清理侧栏 logo（含设置弹窗残留）

布局重构时侧栏 logo 仅用 CSS 隐藏（保留 DOM，因 app.js 的 logoEnd 切分依赖「首个 </div> 即 nav-logo 块结束」）。现彻底移除：
- `js/app.js`（renderSideNav）：删除 nav-logo 的 HTML 拼接；改写 logoEnd 切分逻辑——html 现即为纯菜单，直接包进 `.nav-menu-scroll`（不再依赖「首个 div 即 logo」）
- `js/app.js`（openQuickSettings）：**顺带发现并清除**常用功能设置弹窗 body 顶部误注入的 nav-logo 残留（本属复制粘贴错误，弹窗顶部多出个 TY logo 已被清理）
- 顶栏品牌返回首页逻辑：选择器由 `.nav-logo,#topbarBrand` 简化为 `#topbarBrand`（侧栏 logo 已不存在）
- CSS：nav-logo 相关规则已无残留（DOM 删除后成死代码）

**效果**：侧栏顶部不再有 logo（含 DOM），菜单从顶部开始；常用功能设置弹窗也不再有多余的 TY logo 残留。品牌仅保留在顶栏一处。

**验证**：app.js 0 lint 错误，CSS 8 个 warning 均原有。

---

## 2026-09-02 — 常用功能设置弹窗新增「恢复默认」按钮

「常用功能设置」弹窗（齿轮图标进入）的确定/取消旁新增「恢复默认」按钮：
- `index.html`：qs-footer 加 `<button id="qsReset">恢复默认</button>`（置于「确定」与「取消」之间）
- `js/app.js`：绑定 qsReset 点击——遍历 qsBody 全部 checkbox，按 `DEFAULT_QUICK_KEYS`（系统默认 10 项）勾选/取消；仅重置**当前弹窗勾选**，不立即保存（点「确定」才写入 localStorage 的 `quick_menu_keys`，点「取消」可放弃）
- 说明：常用功能配置存于浏览器 `localStorage`（非账套数据），恢复默认不触碰任何账套数据

**验证**：app.js / index.html lint 0 错误。

---

## 2026-09-02 — 首页「常用功能」：整行删除标题栏，设置图标下移

（承接上一条"删除常用功能标题文字"）将首页「常用功能」区块原本的标题栏**整行删除**，并把右上角「自定义」设置图标**下移**到内容区右上角：
- `index.html`：删除整个 `titleWrapper` 标题栏行（不再单独占一行）；设置图标 `homeQuickEdit` 移到 `.home-quick`（新增凭证卡+图标网格所在内容行）内部作为首元素
- `css/style.css`：`.home-quick` 加 `position:relative`（作为定位参考）；新增 `.home-quick .home-quick-edit{position:absolute;top:0;right:0;z-index:5;margin-left:0}` 使设置图标悬浮于内容区右上角、不再独占一行；清理无用的 `.home-quick-title` 规则与注释

**效果**：首页常用功能区不再有独立标题行，「自定义」齿轮图标下沉到新增凭证卡 / 图标网格那一行的右上角（悬浮），界面更紧凑。

**验证**：lint 无新增错误（8 个 warning 均原有）。

---

## 2026-09-02 — 删除暗色模式功能

移除侧栏底部的「月亮/太阳」主题切换按钮及深色主题应用逻辑：
- **CSS**（`css/style.css`）：删除 `.kd-theme-dark` 全部规则（深色侧栏变量、深色阴影、深色菜单文字、深色 logo 图样式）、删除 `.nav-theme-sun/.nav-theme-moon` 图标显隐规则
- **JS**（`js/app.js`）：删除侧栏底部 `navThemeBtn`（月亮/太阳按钮）DOM；删除 `applyTheme` + `savedTheme` + 主题按钮点击逻辑；新增启动清理——`localStorage.removeItem('kdThemeDark')` + `document.documentElement.classList.remove('kd-theme-dark')`，防止历史残留旧数据导致侧栏深色样式残留在浅色界面
- 侧栏底部仅保留「收起导航」按钮 + 作者信息

**验证**：CSS/JS 均无残留引用（除清理代码中的键名字符串），app.js 语法通过、lint 无新增错误。

---

## 2026-09-02 — 删除首页「常用功能」区块标题

首页「常用功能」区块（`contentItemWrapper--1cQtp`）原本有标题栏（左"常用功能"文字 + 右"自定义"编辑按钮）。删除标题文字，图标网格自明、更简洁：
- 删除 `<span>常用功能</span>` 标题文字（`index.html`）
- 保留右上角「自定义常用功能」编辑按钮（`homeQuickEdit`，是进入常用功能设置的唯一入口，不可删）
- 新增 `.titleWrapper--2yike.home-quick-title` 类：无标题文字时去掉原大下距（margin-bottom:14px→2px）、紧凑布局，让编辑按钮悬浮于图标网格右上角；`pull-right` 仍自动右推
- 用 CSS 类而非内联样式，符合项目规范

**验证**：lint 无新增错误（残留 8 个 warning 均原有）。

---

## 2026-09-02 — 清理侧栏与内容区交界的圆角与阴影过渡

布局已改为通栏直角平铺后，内容区左缘残留的「金蝶卡片式分层」视觉（内容区左缘 20px 圆角 + 柔和阴影、侧栏同色实心向右延伸的 box-shadow）显得多余/突兀。清理：
- `.content`：删除 `border-radius: 20px 0 0 20px` 与左缘 `box-shadow: -12px 0 15px rgba(198,209,239,.5)`
- `.sidenav`：删除主规则与 collapsed 态的 `box-shadow: 12px 0 0 0 var(--kd-nav-bg)`（同色实心延伸），改为干净的直角左列
- 侧栏与内容区现在**直角通栏相邻**，无圆角无阴影分层

**验证**：lint 无新增错误（残留的 8 个 warning 均为原有空规则集/未知属性），`.sidenav.collapsed` 的宽度收窄与文字/图标/操作区规则均保留（未被误删，仅移除了它里面原只有 box-shadow 的空块）。

---

## 2026-09-02 — 首页布局重构：顶栏上移横贯全宽 + 品牌统一（消除割裂）

**问题**：原布局为「方案 C」——侧栏占左列全高（顶部带 TY logo），顶栏在 content（右侧主区域）内部顶部，导致侧栏 TY 图标与顶栏「添钰财务记账系统」文字**不在同一行、视觉割裂**。

**改动**：
- **HTML 结构**（`index.html`）：`header.topbar` 从 `.content` 内移到 `.layout` 顶层（横贯全宽）；新增 `.layout-body` 包裹 `nav.sidenav` + `main.content`，置于顶栏下方
- **CSS**（`css/style.css`）：`.layout` 由 flex-row 改为 `flex-direction:column`；新增 `.layout-body{display:flex;flex:1;min-height:0}`（`min-height:0` 必需，否则内部滚动失效）；`.topbar` 取消左上圆角（原与侧栏相接设计）、改为全宽通栏 + 底部浅分隔线
- **品牌统一**：顶栏品牌 = **TY 图标 + 「添钰财务记账系统」**（恢复 `.topbar-brand-ico` 显示与图文间距 gap:10）；**侧栏 logo 用 CSS 隐藏**（`.nav-logo{display:none}`）——品牌只保留顶栏一处，消除两个品牌源

**关键注意（勿违）**：侧栏 logo 仅用 CSS 隐藏、**DOM 必须保留**——`js/app.js` 的 `logoEnd` 切分逻辑依赖「html 中首个 `</div>` 即 `.nav-logo` 块结束」来分离 logo 与菜单（`html.indexOf('</div>')+6`），若从 DOM 移除会导致侧栏菜单渲染错位。

**验证**：HTML 标签全部配对（div 881/881、header/main/nav 各 1/1）、结构顺序正确（layout→topbar→layout-body→sidenav→content）、页面 HTTP 200 可加载、CSS 规则确认生效、`logoEnd` 逻辑未被破坏。

**备份**：改动前已备份 `backups_layout_20260902-223604/`（index.html + style.css）。

---

## 2026-09-02 — 侧栏 logo 替换为 TY 字母图标（添钰首字母）

将侧栏顶部 logo 从原来的「心形 + 1+1=2」SVG（36×32px，深蓝描边，Comic Sans 文字，画风与财务软件不搭）替换为用添钰首字母 **TY** 设计的简洁专业图标：
- 内联 SVG：渐变圆角方块（蓝 `#3b82f6→#2563eb`）+ 白色 TY 字形（T 横梁竖干 + Y 分叉竖干，线帽圆润）
- 尺寸 34×34（方形适配，`js/app.js` 两处渲染模板均已替换；`css/style.css` `.nav-logo-svg` 由 36×32 改为 34×34）
- 侧栏仍只显示图标（品牌文字统一在顶栏"添钰财务记账系统"，避免重复）；`.nav-logo-text` 保留隐藏

**验证**：app.js 语法通过、SVG XML 结构配对正确（8 开 = 5 自闭合 + 3 闭）、lint 无新增错误。

---

## 2026-09-02 — 原始凭证页精简为附件台账视图

原始凭证页本质是**凭证附件的台账视图**：录凭证上传附件（实体存 `attachments/`）时由 `addOriginalFromAttachment` 登记一条元数据记录。经查，附件台账实际填充的字段有限，表格原 16 列中大量列是**恒定默认值**（金额恒 0、附件小类恒"其他"、凭证模板恒"—"、所属组恒"默认组"、是否发票恒否、上传人恒 admin、查验状态恒"未查验"等），无信息量且显得凌乱。

**精简为只显示附件关键信息**（保留并简化显示，不动数据层）：
- 表格 16 列 → 7 列：全选 / 操作 / **附件名称 / 文件大小 / 关联凭证 / 记账期间 / 上传时间**
- `setRptHead` 标题列数 16→7、空行 colspan 16→7
- 导出同步精简为 5 列（附件名称/文件大小/关联凭证/记账期间/上传时间）
- 筛选（名称/大类/小类/凭证状态/期间树）保留，仍可过滤；数据层字段保留，未来如需"独立原始凭证录入（识别发票/查验）"仍可扩展

**验证**：列数与渲染 td 数一致（7）、colspan 无 16 残留、导出表头 5 列。

---

## 2026-09-02 — 结账暂缓项修复：结转显式幂等 + 逐期结账 + 期末处理跟随选期

处理结账审查留下的暂缓项，发现并修复 1 个严重缺陷（结转损益重复生成）及 2 个规范性问题。

### 🔴 严重：结转损益「隐式幂等」失效 → 可重复生成结转凭证
- 原实现靠「损益科目余额归零」隐式幂等，但结转凭证里「贷 3103 本年利润」（3103 是 revenue cls）会被计入 totalRev，导致**第二次结转时 totalRev ≠ 0 而继续结转**
- 已在真实场景验证：**第 2 次结转未被拦截，生成 2 张结转凭证**（重复结转）
- 修复：① 显式幂等——本期已存在结转凭证（`/结转.*损益/`）则拦截，删旧凭证后可重做（与期末调汇/税费类一致）② 统计损益发生额时排除结转科目 3103/3104（与 profitStatement 修复同源）
- 验证：第 2 次结转被拦截、仅 1 张结转凭证、结转金额正确（收入8000-费用2000=净利6000）

### 一般：closePeriod 缺「上期必须已结账」校验（跳期结账）
- 原实现可直接结 2026-04 而不必先结 2026-03（跳期结账），导致中间期间数据可随意修改、账簿断档
- 修复：新增 `_prevPeriod()` 辅助 + `closePeriod` 校验「上一有凭证期间必须已结账」，否则拦截并提示先结上期
- 验证：跳期结账被拦截、顺序结账（3→4 月）成功、仅 1 期账套首期可直接结账、反结账后仍保持顺序约束

### 一般：期末处理按钮期间与结账 tab 选期不一致
- 原实现期末处理（结转损益/折旧/调汇/税费等）固定用 `currentPeriod()`，而结账按钮用 `selMonth`（结账 tab 跨期选期），导致「选 A 期却操作 B 期」的误解
- 修复：8 个期末处理按钮（`btnDepVoucher`/`btnFxVoucher`/`btnCarryCost`/`btnCarryVat`/`btnAccrueSurTax`/`btnAccrueIncTax`/`btnReCarryForward`/`btnCarryYearEnd`）统一用 `selMonth`（跟随结账 tab 选期）；`refreshSettle` 中 `curMonth` 同步跟随；store 侧各方法均有 `isPeriodClosed` 拦截，对已结账历史期安全

**验证**：`tools/verify_settle_done.js` —— 结转幂等/逐期结账/期末处理跟随选期全通过；既有 14 个脚本回归全通过。

---

## 2026-09-02 — 报表板块审查：修复结转损益后利润表翻倍虚增（严重）

审查「账簿/报表」板块（按会计准则逐项核对）发现并修复严重缺陷，并确认账簿/资产负债表/现金流量表逻辑正确。

### 🔴 严重：结转损益后利润表收入/净利润翻倍虚增
- 缺陷：`profitStatement` 按 `cls==='revenue'/'expense'` 统计损益类科目发生额，但「本年利润 3103」在科目模板中被归为 `revenue`，且「结转损益」凭证里 `贷 3103 本年利润 N` 会被误计入收入
- 后果：**结转损益后**利润表 `totalRevenue` 从 8000 虚增到 14000、`netProfit` 从 6000 虚增到 12000（翻倍）→ 结账页税费测算、利润表全错
- 根因：3103 是「权益/结转类」科目而非损益类，结转凭证不应计入利润表
- 修复：`profitStatement` 取数显式排除 `3103 本年利润` / `3104 利润分配`（符合会计准则：利润表只统计各损益类科目发生额）
- 验证：结转损益前后利润表均为收入8000/费用2000/净利6000，不再翻倍

### 审查结论（无缺陷，已核实正确）
- **账簿**：期初（`openingOf` 含子目上卷+期前凭证）、借贷方向（按 normal dr/cr）、总账明细账勾稽正确；科目余额表页面按末级科目合计，真实账套（371张凭证）末级借贷完全平衡（本期/期末均平衡）
- **资产负债表**：`balanceSheet` 按标准《小企业会计准则》项目取数；规范账套（正确结转损益）恒等式平衡（资产=负债+权益）；不平账套由页面 `bsWip` 诊断提示「结转损益未完整执行」，软件如实呈现不掩盖
- **利润表**：收入贷方/费用借方发生额口径（已结转损益账套取发生额而非净额），净利润正确
- **现金流量表**：`cashFlow` 三大活动分类 + 期初/期末现金 + 净增加勾稽正确；真实账套「期末现金=期初+净增加」平衡，且期末现金 = 1001/1002/1012 科目余额（与总账勾稽一致）

### 说明
- `S.trialBalance()` 是死方法（无人调用，返回 generalLedger 含父级），但页面 `TrialBalance.js` 直接用 `S.generalLedger` 且自己做了末级判定（isLeaf），合计正确，不影响功能

**验证**：`tools/verify_reports.js` —— 资产负债表恒等/利润表口径/现金流量表勾稽全通过；真实账套账簿/报表勾稽验证通过。

---

## 2026-09-02 — 凭证板块审查修复：工资正则失效 + 状态流转单向性 + 结账拦截一致性

审查「凭证」板块（按会计规范逐项核对）发现并修复以下问题：

### 严重：工资凭证识别正则失效（幂等/删除保护形同虚设）
- 缺陷：工资计提凭证摘要是「计提2026-03工资」（**月份在中间**），而 `hasPayrollVoucher`（L3351）与 `_voucherRefs`（L1127）用的是 `/工资(计提|发放)/`，要求"工资"紧跟"计提/发放"，**匹配不上**「计提…工资」
- 后果：① `hasPayrollVoucher` 恒 false → 工资计提凭证已生成却认为"未生成" → **可重复生成工资凭证**（此前"已修复工资幂等"实际因正则错误根本没生效）② `_voucherRefs` 不识别 → **工资凭证可被随意删除**
- 修复：改为 `/(计提|发放).*工资/`（两处）

### 一般：状态流转单向性
- `auditVoucher` 未检查「已出纳复核」：已复核（reviewed）凭证可被再次审核，状态从 reviewed 降回 audited，破坏"审核→复核"单向性。修复：已复核凭证不可再审核。

### 一般：出纳复核/撤销复核缺已结账拦截
- `cashierReview`/`unCashierReview` 未检查 `isPeriodClosed`，而已结账期间的审核/反审核/修改/删除都有拦截。已结账凭证复核状态仍可被改，违反"已结账期间凭证全程锁定"。修复：两处补 `isPeriodClosed` 拦截，与审核/修改/删除一致。

### 边界核对（结论：无缺陷）
- `S.subjects()` 返回全量（含停用）是合理的（报表/账簿需要）；录凭证 UI 用的 `SubjectCombo.subjects()` 已排除停用科目（`enabled !== false`），`pick()` 用全量保留历史停用值回显
- `num()` 对非数字返回 0（`isNaN(n)?0:n`），不产生 NaN；`addVoucher` 有借贷平衡强校验（任何路径写入都必须平衡）

**验证**：`tools/verify_voucher_flow.js` —— 状态流转完整、出纳复核结账拦截、引用校验含工资/出纳流水、不平衡凭证在 addVoucher 层即拦截。

---

## 2026-09-02 — 凭证 id 稳定性与引用完整性（严重，影响出纳两条线闭环）

审查「凭证」板块时发现严重缺陷：**凭证 id 在账套重新加载后会变化，导致所有按 `voucherId` 记录的引用失效**。

- 根因：`addVoucher` 用「V+时间戳」生成 id，而 `ensureVoucherIds()`（账套加载时执行）把 id 强制改写为 `word-no`，两套口径不一致
- 后果：出纳流水「生成凭证」后记录的 `voucherId`，在刷新/重启后指向不存在的 id →「已生成凭证」保护失效、流水可被随意删改 → **出纳账与总账脱节**；同样影响报销单/原始凭证/固定资产的引用
- 次要根因：原 id 仅按 `word-no` 判重，**未区分月份**。而凭证号按月编号（每月从 1 起），3月的「记-1」与4月的「记-1」是不同凭证却被判为重号加 `-N` 后缀，且每次重载重排 → id 反复变化。真实账套（添钰来客 371 张）存在 **58 个跨月重复的 word-no**，印证问题真实存在

**修复**：
- 新增 `_calcVoucherId(word, no, month)`，id 格式 `word-no@YYYY-MM`（含月份，跨月唯一）
- `addVoucher` 与 `ensureVoucherIds` 统一调用该函数，口径严格一致（两处必须同步，否则 id 又会在重载后变化）
- 真实账套首次加载做**一次性 id 规范化**（371 张老 id → 新格式），之后永久稳定；因真实账套无任何凭证引用数据，迁移无损失

**关于凭证断号**（已查证《会计基础工作规范》与金蝶行为）：规范要求记账凭证连续编号；金蝶做法是删除凭证后产生断号、**不自动回填补号**，由会计人员用「凭证整理」手动补齐（不可撤销）。本软件 `nextVoucherNo` 取现存最大号+1，删中间凭证后新凭证继续往后编（如 1,2,4,5 后新增为 6），**不复用已删号**，符合金蝶规范与可追溯性要求（已加验证固化此行为）。

**验证**：`tools/verify_voucher_id.js` —— 跨月同号 id 唯一、重载后 id 不变、出纳流水引用不失效且保护仍生效、断号不复用、真实账套 371 张 id 全唯一且稳定。

---

## 2026-09-02 — 总账缓存按账套隔离（修复切换账套/恢复备份显示旧数据，严重）

审查「凭证/账簿」板块时发现严重缺陷：`generalLedger` 的记忆化缓存 `_glCache` **仅以 month 为键**，而凭证增删改只在 `addVoucher`/`updateVoucher`/`removeVoucher` 三处清缓存。切换账套、恢复备份这些「整体替换 state」的路径均未清缓存，导致：

- **切换账套后**查询同月份 → 命中上一个账套的缓存，**账簿/报表显示旧账套数据**
- **恢复备份后**查询同月份 → 命中恢复前的缓存，**显示恢复前的旧数据**

这是财务软件最严重的一类缺陷（显示错误数据且用户难以察觉）。

**修复（双保险）**：
- 缓存键改为 `bookId + '|' + month`：切换账套自然失效，覆盖所有未预期的 state 替换路径（根治，不需记忆每个替换点）
- `switchBook`（临时空账套顶屏 + 加载成功后）、`restoreFromData`（恢复后）显式清空 `_glCache`

**验证**：`tools/verify_gl_cache.js` —— 账套 A(1000)/B(555) 同月份互不串数据、切回 A 仍为 1000、恢复备份后正确变为新值、缓存仍正常命中（性能未破坏）、凭证增删改仍使缓存失效。

---

## 2026-09-02 — 结账板块全面审查修复（幂等性 + 跨年结转 + 硬性检查可见化）

对「结账」板块做了财务逻辑审查，修复 9 处问题，重点是「重复操作导致重复记账」这类财务大忌。

### 严重缺陷修复
- **期末调汇可无限重复生成同额凭证**（`exchangeAdjust`）：调汇分录不带 qty、不会改变外币数量余额，原逻辑连点 N 次生成 N 张同额凭证。现加幂等拦截：本期已存在调汇凭证则拒绝重复，返回旧凭证供删除后重做。
- **结转损益摘要正则不匹配**：store 生成摘要为「结转2026-03损益」，Settle.js 四处用 `/结转损益/` 匹配不上 →「重新结转」功能失效、结账页恒显未结转。全部改为 `/结转.*损益/`。
- **反结账用错期间变量**：`btnReopenPeriod` 误用 `selMonth`（结账 tab 选期），反结账页自己用 `selReopenMonth`，导致选 A 月反 B 月。改用 `selReopenMonth`。
- **折旧同月重复计提**（`depreciateMonth`）：加 `if (fa.deprMonth === month) return;`，同月重复调用不再生成同额折旧凭证。

### 一般缺陷修复
- **#5 四个「生成凭证」按钮无查重**（结转成本/转出未交增值税/计提附加税/计提所得税）：新增 `genOnceVoucher(month, summaryRe, ...)` 统一查重入口，本期已生成同类凭证则拦截，须删旧凭证再重做。
- **#8 carryYearEnd 死代码接入**：年末结转本年利润（3103→3104）原已实现但全工程零调用，导致跨年 3103 未清零、未分配利润失真。现接入：
  - `settleChecklist` 12 月硬性检查：本年利润未结转为 fail（硬性拦截跨年失真）；无 3104 科目则跳过（兼容）
  - 结账页「结转本年利润」按钮（仅 12 月显示）
  - 补幂等保护：已生成年度结转凭证则拒绝重复
- **#9 结账页未调用 `S.settleChecklist()`**：结账硬性条件（凭证未审核/借贷不平/幽灵科目/损益未结转）此前只在 closePeriod 内部拦截、UI 不可见。现结账页检查清单追加展示 fail/warn 项。
- **#10 renderCashChecklist null 兜底**：`S.detailLedger(...).rows` 直接取会 TypeError，加 `|| {}` 兜底并合并两次调用。

### 验证
- `tools/verify_idempotent.js`：期末调汇/折旧幂等、结转损益摘要可匹配、反结账期间变量正确
- `tools/verify_settle_fixes.js`：#5查重/#8年末结转接入/#9硬性检查展示/#10空值兜底
- 既有 7 个脚本（含真实账套）回归全通过

---



## 2026-08-31 — 出纳「两条线」落地：账户独立核算 + 日记账录入 + 流水生成凭证 + 备份恢复修复

**背景**：本项目长期目标是逐步对齐金蝶精斗云「出纳两条线」——①出纳线（账户独立编码/期初/流水）②账务线（凭证按科目）。此前账户档案已建好，但 `openingBalance` 是「死字段」无任何取数消费，且全系统没有出纳流水这一数据主体，所谓「现金日记账」实为科目明细账换皮。本次补齐流水模型、报表账户口径、流水→凭证闭环，并修复备份恢复缺陷。

**关键设计原则（勿违）**：只改软件、不碰账套。凡是能从现有数据推导的（历史出纳流水源于凭证），一律动态推导、不落盘；只落盘「推导不出来的」手工流水（凭证里没有的新信息）。老账套报表数字零变化（由工具脚本回归保障）。

### 1. 出纳流水数据模型（`js/store.js`，版本 bump → `?v=2026083129`）
- `state.cashJournals` 新增：仅存手工流水；字段 `{id, accountId, date, period, summary, direction, amount, oppositeSubjects, oppositeSubject, settleType, settleNo, voucherId, voucherNo, source('manual'|'voucher'), status}`
- 推导层 `derivedJournals(accountId, month)`：由凭证派生账户流水，取数口径与 `detailLedger` 完全一致（含子科目上卷），自动带出对方科目（一借多贷保留全部、内部转账两侧互指）
- 合并层 `journalsOf()` / `accountBalance()`：期初关键规则「账户显式设非 0 期初才用账户值，否则回退科目期初」——老账套账户余额 ≡ 科目余额，报表零变化
- 手工流水 CRUD：`addCashJournal` / `updateCashJournal` / `removeCashJournal` / `genJournalVoucher`（流水→凭证），防护：已生成凭证的流水禁删禁改、重复生成拦截、已结账期间禁删
- `ensureBankAccounts` 已存在；账户 `openingBalance` 死字段被 `accountBalance` 接活

### 2. 报表切到账户口径（`js/pages/cashier/CashierExtra.js`，bump → `?v=2026083116`）
- 抽单一取数源 `cashierRows(month)`：账户余额表 / 收支汇总屏幕 / 收支汇总导出 三处共用，杜绝「屏幕走账户、导出走科目」的口径打架（此前 `exportIes` 走科目清单、屏幕走账户档案，两边行数金额不一致）
- 收支汇总表原 `sum` 对账户行直接累加，一科目多账户会虚增，随切账户口径一并修复
- 核对总账：左=账户余额（出纳线）、右=总账科目余额（账务线），改造前两侧同源自比恒相符、无核对价值，现在才有真实勾稽意义
- 重复计算告警：同科目挂多个未设独立期初的账户时，在表上方明确告警并给处理指引（不静默隐藏数据）

### 3. 日记账按账户取数 + 录入流水 UI（`js/pages/cashier/CashJournal.js`，bump → `?v=2026083117`）
- 下拉 value 由科目编码改为账户 id（此前同科目多账户选哪个结果都一样）；取数走 `journalsOf`/`accountBalance`/`accountYtd`
- 新增「新增流水」按钮 + 录入弹窗（账户/日期/收付方向/金额/对方科目联想/结算方式/票号/摘要），对方科目排除资金类科目自身
- 未生成凭证的手工流水显示「生成凭证」入口，生成后回写凭证号、不再显示

### 4. 银行对账单按账户区分（`js/store.js` + `js/pages/cashier/Cashier.js`，bump → `?v=2026083123`）
- 出纳结账对齐金蝶「结转未达账 + 结账」语义（`js/store.js` `carryUnreconciled` + `js/pages/settle/Settle.js`，bump → `?v=2026083113`）：
  - 结账前自动调用 `carryUnreconciled(per)`：本期未勾对的流水/对账单标记 `carriedFrom`（结转下期），已达账项 `settled=true`
  - `reconcile` 下期自动纳入结转未达项（跨期匹配放宽为「金额+摘要」），持续跟踪直到勾销结清
  - 已生成凭证的手工流水不结转（已入总账，非未达账项）
  - 结账检查项做实：该期未生成凭证的手工流水数、未勾对未达账项数（提示性）；修正历史误导注释「→生成凭证」为真实语义（金蝶日记账生成凭证是日常独立功能，非结账自动批量）
- 对账/余额调节表企业侧改走账户出纳流水（`reconcile`/`adjustTable` 传 accountId 时）：
  - 企业账侧 = `accountBalance` 账户流水（含凭证推导流水 + 手工流水），bookEnd = 账户期末余额
  - 手工录入的出纳流水因此能参与对账/勾稽，两条线真正打通
  - 不传 accountId 时仍走科目口径，老账套零影响（由 `verify_recon_account.js` 回归保障）
- `bankStatements` 新增 `accountId` 字段；新增 `statementsByAccount`/`statementOpeningByAccount`；`reconcile`/`adjustTable` 增加可选 `accountId` 参数
- 对账单/对账/余额调节表下拉 value 改账户 id；新增 `acctToCode()` 供对账取企业账
- 多账户下对账单/对账/余额调节表可区分归属；老记录无 accountId 自动回退按科目（一科目一账户等价），老账套零影响

### 5. 修复 `restoreFromData`（备份恢复，`js/store.js`）
- 关键 bug：原写 `this.emptyState()`，但 `emptyState` 是模块私有函数未挂 S 上 → 必抛 `TypeError`，备份恢复整体不可用
- 改为直接调用私有 `emptyState()`，并补「加载账套」一致的兜底链路（normalizeState 缺字段补全 + ensureVoucherIds 凭证 id 唯一 + ensureCashFlowFields + schemaVersion 对齐）

### 验证（`tools/` 下新增 3 个回归脚本）
- `verify_cashier_journals.js`：真实账套 66 组（账户×期间）账户余额 ≡ 科目余额 + 17 项合成用例（推导逻辑/对方科目/内部转账/手工流水/流水保护）
- `verify_cashier_report.js`：报表切账户口径后数字零变化、屏幕/导出同源、核对总账账户≡总账
- `verify_journal_voucher.js`：流水→凭证闭环、备份恢复修复、流水保护
- `verify_bank_stmt_account.js`：银行对账单按账户区分、老记录回退兼容

**验证结论**：全部通过，老账套报表数字零变化。

---

## 2026-08-28 — 报表导出/打印全面补齐与修复（Tauri 桌面版）

**背景**：软件从浏览器迁移到 Tauri v2 桌面版后，多项「导出/打印/打开文件夹」功能出现
「无反应」；同时报表域普遍存在导出按钮缺失、部分报表列错位、打印带出交互标记等问题。
本次一次性补齐导出能力并修复全部相关缺陷。

**改动文件与要点**：

### 1. 导出链路根治（`js/file-save-bridge.js`、`tauri/src-tauri/src/lib.rs`、`Cargo.toml`）
- **二进制传参改 base64（关键 bug）**：原前端用 `Array.from(u8)` 把二进制转普通数组传给
  Rust `Vec<u8>` 参数，Tauri v2 对 `Vec<u8>` 的 IPC 序列化有特殊要求，导致 `invoke` 在参数
  解析阶段被拒、静默失败（导出"无反应"）。改为 JS 端 `btoa` 编码 base64 字符串、Rust 端
  `base64` crate 解码还原写盘（`Cargo.toml` 加 `base64 = "0.22"`，`use base64::Engine`）。
- **导出结果提示可见化**：`toastExported` 改为弹出带「打开文件夹」按钮的结果浮层（替代
  纯文本 toast），用户可一键在系统文件管理器中打开导出目录；`failToast` 加 `alert` 兜底，
  任何失败都显式可见，杜绝"静默无反应"。
- **`showToast` 挂全局**：`app.js` 增加 `globalThis.showToast = showToast`，修复外部脚本
  （file-save-bridge 等）检查 `typeof showToast === 'function'` 时因作用域问题判 false、
  导出成功也不提示的缺陷。

### 2. 打印功能修复（`js/app.js`、`css/style.css`）
- **`kdPrint` 二进制参数 base64 化**：打印生成 HTML 走 `save_export_file` 时也改为 base64
  参数，与 Rust 端新签名一致（修复打印报错）。
- **打印隐藏交互标记**：`buildPrintHtml` 内联样式与主 CSS `@media print` 均增加
  `.bs-editformula`、`.btn`/`.kd-btn`/`button` 隐藏规则，打印/预览时不显示编辑态 UI。

### 3. 报表导出按钮全面补齐（`js/pages/report/Report.js`、`ProjectProfit.js`、`Original.js`、`TrialBalance.js`、`index.html`、`app.js`）
- **四大标准报表新增导出**：资产负债表（`exportBs`，双栏对照 Excel）、利润表（`exportPl`）、
  现金流量表（`exportCf`）、应交税金明细表（`exportTx`），各加「导出」按钮（`btnBsExport` 等）。
- **项目利润表修复**：查询按钮绑定 id 由错误的 `btnPpRefresh` 修正为 `btnPpQuery`；补缺失的
  `btnPpExport` 导出按钮；加固 `exportPP`（`ppBody` 判空）。
- **原始凭证**：导出由「演示环境暂未接入」占位改为 `exportOrig`（按当前筛选导出 Excel）；
  修复 `btnOrigPrint` 因 `data-print` + JS `onclick` 双重绑定导致的打印双触发。
- **科目余额表新增导出**：`exportTb`（含合计行）+ `btnTbExport` 按钮。

### 4. 报表列对齐修复（`js/pages/report/Report.js`、`index.html`、`js/pages/cashier/CashierExtra.js`、`css/style.css`）
- **四大报表移除 rpt-fill 占位列**：删除表头 `<th class="rpt-fill">`、数据行/合计行补的
  `td.rpt-fill`、`setRptHead` colspan 恢复业务列数（8/4），并清理 CSS 死样式，消除
  「右侧多出一列空白/整体多出一列空格」问题。
- **核对总账数据行补列**：表头 6 列（含「方向」「核对结果」），但数据行把两列合并为 1 个 td
  （5 个 td），导致右侧多出空白列；拆为独立两个 td 对齐表头。
- **应交税金空态占位**：`colspan="5"` 修正为 `colspan="4"`（与表头 4 列一致）。

### 5. 移除「编辑公式」fx 悬浮标记（`js/pages/report/Report.js`、`css/style.css`、`js/app.js`）
- **完全移除 `fx` 悬浮标记**：删除 `editFormulaBtn()` 函数定义及 3 处调用（资产负债表左右两列、
  利润表行），删除 `.bs-editformula` 悬浮样式与打印规则。该标记仅为装饰性悬浮提示（hover 显示、
  点击无实际编辑功能），且会被带入打印；现彻底移除，报表数据纯净。

**验证结果**：
- 费用明细表、资产负债表、利润表、现金流量表、应交税金、项目利润表、原始凭证、科目余额表
  导出均可用：点导出弹「导出完成」浮层（含「打开文件夹」按钮），文件写入
  `~/Documents/财务软件/exports/`。
- 打印正常（走生成 HTML + 系统打开），不再报错、不再双触发、不再带出 `fx` 等交互标记。
- 四大报表与核对总账表格右侧不再有多余空白列。

---

## 2026-08-21 — 金蝶 .ais 账套导入完整性审查（跨年 / 多币种 / 平衡校验 / 存储兜底）

**背景**：对导入金蝶 KIS 账套功能做完整导入审查。以真实导入结果（「添钰来客」账套：
398 科目 / 371 凭证 / 88 期初）验证单年账套借贷平衡、余额结构均正确；代码审查发现
跨年账套、多币种账套及大账套存储存在完整性缺陷，本次全部修复并补单元验证。

**改动文件与要点**：

### 1. `js/kis-import.js`（版本 bump → `?v=20260821160`）
- **跨年账套期初余额取错年份（关键 bug）**：原逻辑把 6 位期间号 `YYYYMM` 归一化为
  月份 1~12 后取最小期间，跨年账套中不同年份的同月行会互相覆盖，期初余额最终被
  「最后一年的 1 月」覆盖。现改为「完整期间号」比较：6 位期间号直接用 `YYYYMM`；
  标准版 1~12 用起始年份补全为 `YYYYMM`，且同期间重复行只保留第一条。
- **跨年账套结账进度推导错误**：原 `closedPeriods` 只按起始年份生成 1~(最大期间-1)，
  跨年账套会漏掉中间年份已结账月份，`currentPeriod` 也会错位。现改为按年份分组：
  每个年份的凭证最大期间之前的各月均标记为已结账，`currentPeriod` 取最后年份的最大期间。
- **期初余额币种回退**：原逻辑只认 `FCyID='*'` 综合币行，多币种/外币账套若无综合币
  汇总行则期初余额整体丢失。现回退到出现次数最多的币种行，并在 `stats.currencyFallback`
  中报告所用币种。
- **凭证聚合 key 加入日期**：key 由「年份-期间-字-号」改为「日期-期间-字-号」，
  源数据中同期间同字号重复出现的异常行不再被误合并。
- **借贷平衡校验**：逐张凭证核对借=贷，新增 `stats.unbalancedVouchers` 统计，
  源数据异常时在导入结果中明确提示，避免「导入成功但账不平」无人知晓。
- **完整性统计**：`stats` 增加 `acctRows / vchRows / balRows`（源表行数），
  探针 `_ais_probe.json` 增加 `GLBal_FCyID_dist / GLBal_FPeriod_dist / unbalancedVouchers`。

### 2. `js/pages/settings/Settings.js`
- **localStorage 容量静默失败（关键 bug）**：原 `loadServerBookIntoLocal` 对
  `localStorage.setItem` 异常静默吞掉，大账套（>5MB）会出现「导入成功但刷新后账套
  丢失」的假象。现改为写入失败时回滚容器并抛出明确错误，导入中止并提示大小。
- **导入结果提示增强**：成功提示展示「分录源行数 → 凭证数」「借贷不平凭证数」
  「币种回退」「重复科目去重」等完整性信息。

### 3. `index.html`
- 更新 `kis-import.js` 缓存 bump（`?v=20260821160`）。

### 4. `_diag_kis_import.js`（新增，只读验证脚本）
- 沙箱加载 `kis-import.js`，4 个场景验证：跨年 6 位期间号、标准版 1~12 期间号、
  无综合币行回退、凭证为空年份兜底。运行 `node _diag_kis_import.js` 全部通过。

**验证结果**：
- 单年账套真实数据：371 张凭证全部借贷平衡，借方总额 = 贷方总额 = 12,682,110.33；
  期初 88 科目、结账进度 1~6 月、startMonth=2026-01 均正确。
- 跨年 mock：期初取最早年份余额（10000 而非次年 12000）、closedPeriods 含
  2022-01~11 + 2023-01~02、currentPeriod=2023-03，全部符合预期。

### 5. `_diag_e2e.js`（新增，真实账套端到端验证脚本）
- 用 vm 沙箱加载 `mdb-reader.js` + `kis-import.js`，直接解析真实 `.ais` 文件：
  `node _diag_e2e.js "<账套.ais>"`。
- 验证项：表清单、导入统计（源行数/凭证/期初/结账进度）、凭证借贷平衡、
  凭证/期初科目引用完整性、日期与期间一致性、期初无同科目双向。

**真实账套端到端验证（`添钰来客_2026年_金蝶KIS格式.ais`，12MB，301 张表）**：
- 源分录 3133 行 → 371 张凭证，借贷全部平衡（unbalancedVouchers=0）；
- 源科目 411 行 → 398 科目（去重 13 个重复科目码）；
- GLBal 3244 行（FCyID `*|*` 与 `RMB|*` 各 1622）→ 88 个期初科目，取综合币行；
- 凭证日期与期间完全一致，期间分布 2026-01~07（58/43/58/53/53/58/48 张）；
- currentPeriod=2026-07，closedPeriods=2026-01~06，startMonth=2026-01；
- 与新逻辑输出对比旧导入结果（.bak）：371 凭证/398 科目/88 期初/结账进度/startMonth
  全部一致，**差异 0**——修复未影响单年账套既有正确行为。

---

## 2026-08-21 — 金蝶 .ais 账套导入完整性修复

**背景**：用户导入金蝶 KIS 导出的 `.ais` 账套后，出现「结账进度（currentPeriod）丢失/乱跳」问题。
经排查，根因是导入模块 `js/kis-import.js` 只读取了 `GLAcct / GLVch / GLBal` 三张表，
**丢失了结账历史（closedPeriods）与凭证记账/审核状态**，导致结账进度只能靠「最近凭证月份」兜底而乱跳。

**改动文件与要点**：

### 1. `js/kis-import.js`（版本 bump → `?v=20260821010`）
- **凭证字段补全**：每张凭证补全 `posted / checked / checker / poster` 字段，
  依据 `FPosted / FChecked / FChecker / FPoster` 推导，确保导入后凭证的记账/审核状态完整。
- **FPosted 布尔判断修复（关键 bug）**：
  原逻辑按 `parseInt(FPosted) === 1` 判定记账状态，但金蝶导出该字段为布尔 `true` 而非数字 `1`，
  导致导入后全部凭证被误判为「未记账」。修正为：
  `posted = (FPosted === true || FPosted === 1 || FPosted === 'true')`（同规则用于 `checked`）。
- **closedPeriods 推导**：
  由于 `.ais` 内部的 `GLPeriod`（期间表）为空、`GLOptions / GLBal` 因 mdb-reader.js 解析大表/特殊字段返回 `null`，
  标准结账字段不可直接读取。改为按业务规则推导：取凭证最大期间，其之前各月视为「已结账」。
  ```js
  var maxPeriod = 0;
  vouchers.forEach(function (v) { if (v.period > maxPeriod) maxPeriod = v.period; });
  var closedPeriods = [];
  if (maxPeriod >= 1) {
    for (var cp = 1; cp < maxPeriod; cp++) closedPeriods.push(startYear + '-' + pad(cp));
  }
  ```
  推导结果写入 ledger 的 `closedPeriods`，并为 `stats` 增加 `closedPeriods / currentPeriod` 报告项。
- **诊断探针**：临时加探针块，POST 到 `serve.py` 的 `/api/probe` 落盘 `_ais_probe.json`，用于核查 .ais 内部表结构。

### 2. `serve.py`
- 新增 `POST /api/probe` 端点，将前端探针上报的 .ais 内部结构信息落盘为 `_ais_probe.json`（固定文件名，便于核对）。

### 3. `js/pages/voucher/Voucher.js`（版本 bump → `?v=20260821003`，C 方案）
- 「录凭证」页默认日期逻辑优化（currentPeriod 驱动）：
  - 当前期间 == 当前自然月 → 默认今天；
  - 当前期间 != 当前自然月 → 默认期间末日（避免录入日期超出已结账/未结账闸门）。
  ```js
  var curP = currentPeriod();
  var natM = now.getFullYear() + '-' + ('0' + (now.getMonth() + 1)).slice(-2);
  dt.value = (curP === natM) ? (H.todayStr ? H.todayStr() : todayStr()) : U.lastDay(curP);
  ```

### 4. `index.html` / `js/main.js`
- 同步更新 `kis-import.js`（`?v=20260821010`）与 `Voucher.js`（`?v=20260821003`）的缓存 bump，强制浏览器刷新新逻辑。

**验证结果**（以账套「添钰来客」为例）：
- `startMonth = 2026-01`
- `closedPeriods = ['2026-01' … '2026-06']`（1~6 月推导为已结账）
- 共导入 371 张凭证，`posted` 全部为 `true`，无 8 月脏数据
- `currentPeriod` 正确落到推导的下一期

**遗留说明（重要）**：
结账进度为基于金蝶业务规则的**推断值**，而非从 `.ais` 原始字段直接读出（标准字段不可读）。
如需 100% 钉死，请在金蝶软件内查看「当前会计期间（第几期）」与导入结果比对。
若金蝶实际期数与推断不符，再微调 `closedPeriods` 推导规则。

---

## 备注：本软件定位
纯本地单机版财务软件（复刻金蝶精斗云云会计风格），纯前端 + `serve.py` 本地文件服务，
零云端依赖、零登录、数据落盘在 `data/books/<id>.json`。详见 `README.md`。
